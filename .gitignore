.worktrees/
.cursor/
.idea/
.gradle/
build/
*.iml
.DS_Store

# node
node_modules/
yanote-js/dist/

# local runtime residue
.bg-shell/

# local distribution/build residue (release assets and bundle outputs are not tracked)
dist/

# default recorder output (if generated during local runs)
/events.jsonl

# local proof/runtime artifacts
.yanote-ci/

# local planning caches
.planning/
Thumbs.db
*.swp
*.swo
*~
.vscode/
*.code-workspace
.env
.env.*
!.env.example
.next/
__pycache__/
*.pyc
.venv/
venv/
target/
vendor/
*.log
coverage/
!yanote-js/src/coverage/
!yanote-js/src/coverage/**
.cache/
tmp/

# ── GSD: local-only project state ───────────────────────────────────────────
# Keep the whole .gsd tree inside the repo working copy, but out of git.
/.gsd/

# ── GSD baseline (auto-generated) ──
.gsd
