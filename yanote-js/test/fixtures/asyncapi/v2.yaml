asyncapi: 2.6.0
info:
  title: yanote async v2 sample
  version: 1.0.0
channels:
  users.signedup:
    publish:
      message:
        payload:
          type: object
  users.deleted:
    subscribe:
      message:
        payload:
          type: object

