asyncapi: 3.0.0
info:
  title: yanote async v3 sample
  version: '1.0.0'
channels:
  hello:
    address: hello
    messages:
      sayHelloMessage:
        payload:
          type: string
operations:
  receiveHello:
    action: receive
    channel:
      $ref: '#/channels/hello'

