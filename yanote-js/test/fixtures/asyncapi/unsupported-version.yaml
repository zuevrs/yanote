asyncapi: 4.0.0
info:
  title: unsupported asyncapi version
  version: '1.0.0'
servers:
  kafkaLocal:
    host: localhost:9092
    protocol: kafka
channels:
  usersSignedUp:
    address: users.signedup
    messages:
      UserSignedUp:
        payload:
          type: object
operations:
  sendUserSignedUp:
    action: send
    channel:
      $ref: '#/channels/usersSignedUp'
    messages:
      - $ref: '#/channels/usersSignedUp/messages/UserSignedUp'
