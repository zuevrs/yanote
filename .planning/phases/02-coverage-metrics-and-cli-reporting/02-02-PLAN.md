---
phase: 02-coverage-metrics-and-cli-reporting
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - yanote-js/package.json
  - yanote-js/package-lock.json
  - yanote-js/src/report/schema.ts
  - yanote-js/src/report/normalize.ts
  - yanote-js/src/report/report.contract.test.ts
  - yanote-js/src/report/writeReport.ts
  - yanote-js/src/report/writeReport.determinism.test.ts
autonomous: true
requirements:
  - COVR-04
user_setup: []
must_haves:
    truths:
      - "Report JSON is versioned with an explicit schema contract independent of tool version."
      - "Unknown fields are rejected and required fields are enforced before artifacts are written."
      - "Equivalent normalized report DTO inputs serialize to byte-equivalent JSON output."
    artifacts:
      - path: "yanote-js/src/report/schema.ts"
        provides: "Strict report schema and validator entrypoint."
        exports: ["REPORT_SCHEMA_VERSION", "validateReport"]
      - path: "yanote-js/src/report/normalize.ts"
        provides: "Canonical sorting and shared rounding helpers for deterministic output."
        exports: ["normalizeReport", "roundCoverage"]
      - path: "yanote-js/src/report/writeReport.ts"
        provides: "Strict validate-then-serialize deterministic write boundary."
        contains: "json-stable-stringify|validateReport"
    key_links:
      - from: "yanote-js/src/report/writeReport.ts"
        to: "yanote-js/src/report/schema.ts"
        via: "Write boundary enforces strict schema before serialization."
        pattern: "validateReport"
      - from: "yanote-js/src/report/writeReport.ts"
        to: "yanote-js/src/report/normalize.ts"
        via: "Write boundary normalizes ordering/rounding before byte serialization."
        pattern: "normalizeReport"
      - from: "yanote-js/src/report/report.contract.test.ts"
        to: "yanote-js/src/report/schema.ts"
        via: "Contract tests lock required fields and unknown-field rejection."
        pattern: "additionalProperties|required"
---


<objective>
Harden the report artifact boundary to guarantee strict, versioned, deterministic JSON output.

Purpose: Deliver COVR-04 as a compatibility contract for local and CI report consumers.
Output: Schema validator, canonical normalization helpers, deterministic writer behavior, and contract tests. CLI summary contract scope (COVR-05) remains in 02-03 integration.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-coverage-metrics-and-cli-reporting/02-CONTEXT.md
@.planning/phases/02-coverage-metrics-and-cli-reporting/02-RESEARCH.md
@yanote-js/package.json
@yanote-js/src/report/report.ts
@yanote-js/src/report/writeReport.ts

<interfaces>
From `yanote-js/src/report/report.ts`:
- `buildReport(...)` is the canonical report DTO constructor consumed by write boundary.

From `yanote-js/src/report/writeReport.ts`:
- `writeYanoteReport(outDir, report)` is the only artifact write boundary and must enforce deterministic serialization.

From `yanote-js/package.json`:
- Existing dependencies already include Commander/OpenAPI parser/test stack; schema/serialization helpers should be added here.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add strict report schema and normalization utilities</name>
  <files>yanote-js/package.json, yanote-js/package-lock.json, yanote-js/src/report/schema.ts, yanote-js/src/report/normalize.ts, yanote-js/src/report/report.contract.test.ts</files>
  <behavior>
    - Test: Report schema requires v1 contract fields (`schemaVersion`, `generatedAt`, `toolVersion`, phase metadata, status enum) and rejects unknown fields.
    - Test: `schemaVersion` is validated independently from `toolVersion`.
    - Test: Shared rounding + normalization helpers produce deterministic ordering and fixed decimal outputs.
  </behavior>
  <action>Install `ajv` and `json-stable-stringify` in `yanote-js`. Implement strict schema validation (`additionalProperties: false`) and canonical normalization helpers (array ordering, key ordering, and single rounding helper) in dedicated report modules. Keep this plan focused on contract boundaries, not CLI formatting.</action>
  <verify>
    <automated>npm -C yanote-js run test -- src/report/report.contract.test.ts</automated>
  </verify>
  <done>Schema and normalization modules exist with passing contract tests, and dependencies are locked in package manifests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce deterministic validate-then-write report boundary</name>
  <files>yanote-js/src/report/writeReport.ts, yanote-js/src/report/writeReport.determinism.test.ts</files>
  <behavior>
    - Test: Report write fails with actionable error when schema validation fails.
    - Test: Repeated writes of equivalent report DTOs produce byte-equivalent JSON output.
    - Test: Serialization preserves canonical key ordering and trailing newline contract.
  </behavior>
  <action>Refactor `writeYanoteReport` to normalize report DTO, validate with strict schema, then serialize with stable key ordering before writing `yanote-report.json`. Ensure deterministic output and explicit error propagation for schema failures.</action>
  <verify>
    <automated>npm -C yanote-js run test -- src/report/writeReport.determinism.test.ts src/report/report.contract.test.ts</automated>
  </verify>
  <done>Write boundary deterministically serializes schema-valid reports and rejects invalid report shapes before file write.</done>
</task>

</tasks>

<verification>
- [ ] `npm -C yanote-js run test -- src/report/report.contract.test.ts src/report/writeReport.determinism.test.ts` succeeds.
- [ ] Deterministic write tests produce byte-identical artifacts on repeated runs.
</verification>

<success_criteria>
- COVR-04 report schema is explicit, strict, and independently versioned.
- Report serialization boundary is deterministic and regression-tested for byte stability.
- Downstream CLI integration can rely on stable schema enforcement at one write boundary.
- COVR-05 ownership remains in 02-03 CLI integration; this plan remains COVR-04 contract boundary work.
</success_criteria>

<output>
After completion, create `.planning/phases/02-coverage-metrics-and-cli-reporting/02-02-SUMMARY.md`
</output>
