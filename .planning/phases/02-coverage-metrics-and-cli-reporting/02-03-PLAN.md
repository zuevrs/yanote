---
phase: 02-coverage-metrics-and-cli-reporting
plan: 03
type: execute
wave: 2
depends_on: ["02-01", "02-02"]
files_modified:
  - yanote-js/src/spec/openapi.ts
  - yanote-js/src/coverage/coverage.ts
  - yanote-js/src/coverage/coverage.test.ts
  - yanote-js/src/report/report.ts
  - yanote-js/src/report/report.test.ts
  - yanote-js/src/cli.ts
  - yanote-js/src/cli.summary.contract.test.ts
  - yanote-js/src/cli.report.test.ts
  - yanote-js/src/cli.test.ts
autonomous: true
requirements:
  - COVR-01
  - COVR-02
  - COVR-03
  - COVR-05
  - DELV-01
user_setup: []
must_haves:
    truths:
      - "Developers can run standalone `yanote report` and receive deterministic operation/status/parameter coverage output in local and CI workflows."
      - "CLI prints compact plain-text summary in fixed section order and emits exactly one final `YANOTE_SUMMARY` line."
      - "Fail-closed semantic/input outcomes emit typed, actionable stderr diagnostics while preserving deterministic report snapshots when possible."
    artifacts:
      - path: "yanote-js/src/coverage/coverage.ts"
        provides: "Integrated layered coverage results (operation/status/parameter + weighted aggregate)."
        exports: ["computeCoverage", "CoverageResult"]
      - path: "yanote-js/src/report/report.ts"
        provides: "Versioned report assembly including diagnostics block and per-operation identity fields."
        exports: ["buildReport", "YanoteReport"]
      - path: "yanote-js/src/cli.ts"
        provides: "Standalone CLI coverage command with fixed stdout/stderr contract and machine summary line."
        contains: "YANOTE_SUMMARY"
    key_links:
      - from: "yanote-js/src/spec/openapi.ts"
        to: "yanote-js/src/coverage/coverage.ts"
        via: "OpenAPI loader exposes per-operation response/parameter metadata keyed by canonical operation identity."
        pattern: "operationKey|responses|parameters"
      - from: "yanote-js/src/coverage/coverage.ts"
        to: "yanote-js/src/report/report.ts"
        via: "Layered coverage result is mapped into deterministic report DTO."
        pattern: "status|parameter|aggregate"
      - from: "yanote-js/src/cli.ts"
        to: "yanote-js/src/report/writeReport.ts"
        via: "CLI writes validated report, prints fixed summary sections, and appends machine summary line."
        pattern: "writeYanoteReport|YANOTE_SUMMARY"
---


<objective>
Integrate Phase 2 coverage dimensions and deterministic report/CLI contracts into a runnable standalone analyzer flow.

Purpose: Fulfill operation-level coverage plus readable deterministic CLI reporting for local and CI usage.
Output: Integrated compute/report/CLI behavior with deterministic contract tests and build verification.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-coverage-metrics-and-cli-reporting/02-CONTEXT.md
@.planning/phases/02-coverage-metrics-and-cli-reporting/02-RESEARCH.md
@.planning/phases/01-specification-semantics-contract/01-05-SUMMARY.md
@yanote-js/src/spec/openapi.ts
@yanote-js/src/coverage/coverage.ts
@yanote-js/src/report/report.ts
@yanote-js/src/cli.ts

<interfaces>
From `02-01` outputs:
- `statusCoverage` and `parameterCoverage` modules expose deterministic pure scoring APIs with explicit `N/A` states.
- `HttpEvent` parsing can provide optional normalized query/header evidence keys.

From `02-02` outputs:
- `validateReport` and `normalizeReport` are the strict report contract boundary before write.
- Report schema version is independent from tool version and enforces unknown-field rejection.

From Phase 1 (`01-05`):
- Canonical operation identity remains `METHOD + normalized templated route` and must not be changed in this phase.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Integrate layered coverage computation over canonical operation identities</name>
  <files>yanote-js/src/spec/openapi.ts, yanote-js/src/coverage/coverage.ts, yanote-js/src/coverage/coverage.test.ts</files>
  <behavior>
    - Test: Operation-level denominator stays scoped to canonical v1 OpenAPI operations and remains deterministic across repeated runs.
    - Test: Status and parameter dimensions are computed per operation and expose `COVERED`/`PARTIAL`/`UNCOVERED` plus explicit `N/A` where applicable.
    - Test: Aggregate coverage uses fixed weights (operations 60%, status 25%, parameters 15%). If any weighted dimension is `N/A`, aggregate coverage is reported as `N/A` (no weight redistribution) with deterministic explanation field.
  </behavior>
  <action>Extend OpenAPI loading to expose per-operation response and parameter metadata keyed by canonical operation key while preserving existing canonical identity behavior. Update `computeCoverage` to integrate `statusCoverage` and `parameterCoverage`, produce per-operation layered status, and compute deterministic aggregate coverage. Keep suite/run breakdown optional and deterministic; do not introduce governance threshold/regression expansion in this phase.</action>
  <verify>
    <automated>npm -C yanote-js run test -- src/coverage/coverage.test.ts src/coverage/coverage.matching.test.ts src/coverage/coverage.parity.test.ts</automated>
  </verify>
  <done>Coverage engine returns deterministic layered operation/status/parameter outputs and aggregate coverage aligned with fixed weighting policy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build schema-aligned deterministic report payload from layered coverage</name>
  <files>yanote-js/src/report/report.ts, yanote-js/src/report/report.test.ts</files>
  <behavior>
    - Test: Report includes required schema header (`schemaVersion`, `generatedAt`, `toolVersion`, phase metadata) and top-level diagnostics block.
    - Test: Per-operation report entries include `method`, `route`, and canonical `operationKey`.
    - Test: Report status enum (`ok | partial | invalid`) is deterministic from coverage + diagnostics outcomes.
  </behavior>
  <action>Refactor `buildReport` to emit the strict report DTO expected by `02-02` schema validation. Set deterministic `generatedAt` policy from input evidence: use minimum event timestamp from parsed events; fallback is fixed epoch `1970-01-01T00:00:00.000Z` when no event timestamps are available. Include operation/status/parameter dimensions, and preserve deterministic diagnostics ordering (severity then canonical operation key/candidate order).</action>
  <verify>
    <automated>npm -C yanote-js run test -- src/report/report.test.ts src/report/report.contract.test.ts src/report/writeReport.determinism.test.ts</automated>
  </verify>
  <done>Report builder emits schema-valid deterministic payloads with explicit layered coverage and diagnostic semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Enforce standalone CLI summary and typed exit contract</name>
  <files>yanote-js/src/cli.ts, yanote-js/src/cli.summary.contract.test.ts, yanote-js/src/cli.report.test.ts, yanote-js/src/cli.test.ts</files>
  <behavior>
    - Test: Default CLI output is compact plain text with fixed section order `Summary -> Coverage Dimensions -> Top Issues -> Report Path -> Machine Summary Line`.
    - Test: Top issues are deterministically sorted (severity, then canonical operation key) and truncated with `... +N more; see report` marker.
    - Test: Summary stdout remains plain text with explicit assertion that no ANSI escape sequences are emitted.
    - Test: CLI emits typed exit classes for input, semantic, gate, and io/runtime failures with actionable stderr hints and final stdout `YANOTE_SUMMARY` grammar.
  </behavior>
  <action>Update `report` command output contract: maintain plain-text stdout/stderr channel split (including no ANSI color/format escapes in summary output), add deterministic compact summary plus optional `--verbose` details, and append one stable machine-readable line (`YANOTE_SUMMARY ...`) as the final stdout line. Keep semantic invalid/ambiguous diagnostics fail-closed with non-zero exits, preserve report artifact writing when a deterministic snapshot is possible, and keep wording locked by golden tests.</action>
  <verify>
    <automated>npm -C yanote-js run test -- src/cli.summary.contract.test.ts src/cli.report.test.ts src/cli.test.ts && npm -C yanote-js run build</automated>
  </verify>
  <done>Standalone CLI delivers deterministic local/CI output contract with layered summary, typed exit behavior, and machine-readable final line.</done>
</task>

</tasks>

<verification>
- [ ] `npm -C yanote-js run test -- src/coverage/coverage.test.ts src/report/report.test.ts src/cli.summary.contract.test.ts src/cli.report.test.ts src/cli.test.ts` succeeds.
- [ ] `npm -C yanote-js test` succeeds.
- [ ] `npm -C yanote-js run build` succeeds.
</verification>

<success_criteria>
- COVR-01/COVR-02/COVR-03 are delivered as deterministic layered coverage outputs in standalone CLI flow.
- COVR-05 compact summary and machine summary line are stable, parseable, and contract-tested.
- DELV-01 standalone CLI path is deterministic and CI-friendly with typed fail-closed semantics.
</success_criteria>

<output>
After completion, create `.planning/phases/02-coverage-metrics-and-cli-reporting/02-03-SUMMARY.md`
</output>
