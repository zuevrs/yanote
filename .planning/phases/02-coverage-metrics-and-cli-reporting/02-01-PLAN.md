---
phase: 02-coverage-metrics-and-cli-reporting
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - yanote-js/src/coverage/dimensions.ts
  - yanote-js/src/model/httpEvent.ts
  - yanote-js/src/events/readJsonl.ts
  - yanote-js/src/events/readJsonl.parameters.test.ts
  - yanote-js/src/coverage/statusCoverage.ts
  - yanote-js/src/coverage/statusCoverage.test.ts
  - yanote-js/src/coverage/parameterCoverage.ts
  - yanote-js/src/coverage/parameterCoverage.test.ts
autonomous: true
requirements:
  - COVR-02
  - COVR-03
user_setup: []
must_haves:
    truths:
      - "Status-code coverage per operation is computed from OpenAPI-declared response definitions, not observed statuses."
      - "Parameter coverage scores required parameters while optional parameters remain informational."
      - "Query evidence preserves observed key case identity while header evidence may normalize to lowercase; missing query/header evidence is represented explicitly and deterministically, not silently treated as covered."
    artifacts:
      - path: "yanote-js/src/coverage/statusCoverage.ts"
        provides: "Deterministic status denominator and observed-status scoring logic."
        exports: ["computeStatusCoverage", "StatusCoverageResult"]
      - path: "yanote-js/src/coverage/parameterCoverage.ts"
        provides: "Required/optional/N/A parameter coverage scoring logic."
        exports: ["computeParameterCoverage", "ParameterCoverageResult"]
      - path: "yanote-js/src/events/readJsonl.ts"
        provides: "Optional parameter-evidence ingestion from events.jsonl with query key case preservation and normalized header keys."
        contains: "queryKeys|headerKeys"
    key_links:
      - from: "yanote-js/src/events/readJsonl.ts"
        to: "yanote-js/src/model/httpEvent.ts"
        via: "JSONL parser emits normalized parameter-evidence fields consumed by parameter scoring."
        pattern: "queryKeys|headerKeys"
      - from: "yanote-js/src/coverage/statusCoverage.ts"
        to: "yanote-js/src/coverage/dimensions.ts"
        via: "Status scorer consumes canonical per-operation declared response contract."
        pattern: "declaredStatuses|default"
      - from: "yanote-js/src/coverage/parameterCoverage.ts"
        to: "yanote-js/src/coverage/dimensions.ts"
        via: "Parameter scorer consumes merged required/optional parameter definitions."
        pattern: "required|optional|na"
---


<objective>
Define deterministic status and parameter coverage primitives so Phase 2 can compute layered coverage with explicit evidence semantics.

Purpose: Establish COVR-02/COVR-03 math and evidence contracts before CLI/report integration.
Output: Coverage-dimension contracts, parameter-evidence ingestion, and focused status/parameter test suites.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-coverage-metrics-and-cli-reporting/02-CONTEXT.md
@.planning/phases/02-coverage-metrics-and-cli-reporting/02-RESEARCH.md
@.planning/phases/01-specification-semantics-contract/01-05-SUMMARY.md
@yanote-js/src/model/httpEvent.ts
@yanote-js/src/events/readJsonl.ts
@yanote-js/src/coverage/coverage.ts

<interfaces>
From `yanote-js/src/model/httpEvent.ts`:
- `HttpEvent` currently contains canonical method/route/status and suite/run identities.

From `yanote-js/src/events/readJsonl.ts`:
- `readHttpEventsJsonl(filePath)` streams JSONL and normalizes into `HttpEvent[]` plus `invalidLines` count.

From `yanote-js/src/coverage/coverage.ts`:
- `computeCoverage(...)` currently computes operation match coverage and diagnostics and is the integration target for new dimensions.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define dimension contracts and ingest optional parameter evidence</name>
  <files>yanote-js/src/coverage/dimensions.ts, yanote-js/src/model/httpEvent.ts, yanote-js/src/events/readJsonl.ts, yanote-js/src/events/readJsonl.parameters.test.ts</files>
  <behavior>
    - Test: JSONL parser ingests optional query/header parameter evidence with query keys preserving observed case identity and header keys normalized to deterministic lowercase; both collections remain deduped and sorted.
    - Test: Missing or malformed parameter-evidence fields normalize to empty collections without breaking existing event parsing.
    - Test: Existing invalid-line counting and required method/route guards remain unchanged.
  </behavior>
  <action>Create `src/coverage/dimensions.ts` with shared contracts for status and parameter inputs/outputs (`N/A` handling included). Extend `HttpEvent` and `readHttpEventsJsonl` to support optional parameter-evidence keys (`queryKeys`, `headerKeys`) while preserving backward compatibility with existing event fixtures. Preserve query key case identity semantics; header keys may normalize to lowercase for deterministic matching. Do not add recorder/runtime capture changes in this plan; only lock ingestion and normalization behavior on analyzer input.</action>
  <verify>
    <automated>npm -C yanote-js run test -- src/events/readJsonl.test.ts src/events/readJsonl.parameters.test.ts</automated>
  </verify>
  <done>Coverage dimension contracts exist, JSONL ingestion supports deterministic optional query/header evidence with query key case preserved and normalized header keys, and ingestion tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement deterministic status-code coverage scorer</name>
  <files>yanote-js/src/coverage/statusCoverage.ts, yanote-js/src/coverage/statusCoverage.test.ts</files>
  <behavior>
    - Test: Status denominator uses declared OpenAPI responses (explicit code, range token, and default policy), not observed statuses.
    - Test: Operations with declared statuses compute `COVERED`/`PARTIAL`/`UNCOVERED` deterministically from observed evidence.
    - Test: Operations with no declared status expectations emit explicit `N/A` status-dimension state.
  </behavior>
  <action>Implement `computeStatusCoverage` as a pure function over coverage dimension contracts. Lock deterministic token ordering and precedence rules for explicit status codes, wildcard ranges (for example `2XX`), and `default` matching. Keep scoring isolated from CLI/report formatting concerns.</action>
  <verify>
    <automated>npm -C yanote-js run test -- src/coverage/statusCoverage.test.ts</automated>
  </verify>
  <done>Status coverage scorer and tests enforce declared-response denominator semantics and deterministic per-operation state outputs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement required-vs-optional parameter coverage scorer</name>
  <files>yanote-js/src/coverage/parameterCoverage.ts, yanote-js/src/coverage/parameterCoverage.test.ts</files>
  <behavior>
    - Test: Required parameters determine the primary parameter score; optional parameters are reported separately as informational.
    - Test: Path/query/header locations are scored independently and merged into deterministic per-operation parameter output.
    - Test: Operations with no defined parameters emit explicit `N/A` parameter-dimension state.
  </behavior>
  <action>Implement `computeParameterCoverage` as a pure function using merged parameter definitions (`name` + `in` uniqueness) and observed evidence keys from events. Preserve explicit `N/A` semantics when no parameters are defined and deterministic ordering for reported missing/optional entries.</action>
  <verify>
    <automated>npm -C yanote-js run test -- src/coverage/parameterCoverage.test.ts</automated>
  </verify>
  <done>Parameter coverage scorer produces deterministic required/optional outputs with explicit `N/A` semantics and passing focused tests.</done>
</task>

</tasks>

<verification>
- [ ] `npm -C yanote-js run test -- src/events/readJsonl.test.ts src/events/readJsonl.parameters.test.ts src/coverage/statusCoverage.test.ts src/coverage/parameterCoverage.test.ts` succeeds.
- [ ] New scorer modules are pure and deterministic across repeated test runs.
</verification>

<success_criteria>
- COVR-02 denominator and status-state rules are codified in deterministic unit tests.
- COVR-03 required/optional parameter semantics are codified with explicit `N/A` behavior.
- Integration plan can consume a stable dimension contract without reinterpreting event evidence semantics.
</success_criteria>

<output>
After completion, create `.planning/phases/02-coverage-metrics-and-cli-reporting/02-01-SUMMARY.md`
</output>
