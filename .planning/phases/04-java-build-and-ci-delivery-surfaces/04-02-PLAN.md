---
phase: 04-java-build-and-ci-delivery-surfaces
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - .github/workflows/yanote-ci.yml
  - scripts/ci/render-yanote-summary.mjs
  - scripts/ci/render-yanote-summary.test.mjs
  - scripts/ci/collect-yanote-artifacts.sh
autonomous: true
requirements:
  - DELV-03
  - QUAL-03
user_setup: []
must_haves:
    truths:
      - "Pull requests and merge-queue runs receive deterministic GitHub Checks outcomes from a dedicated Yanote validation job."
      - "GitHub job summary remains concise (single-screen) while preserving CLI parity fields and deterministic top-5 issue ordering."
      - "Stable Yanote artifacts are uploaded whenever a snapshot exists, including failing runs."
      - "GitHub channel defaults to strict CI profile with a limited explicit override set aligned to Gradle DSL controls."
    artifacts:
      - path: ".github/workflows/yanote-ci.yml"
        provides: "Two-check CI workflow (`build-and-test`, `yanote-validation`) with Java 21 baseline and deterministic status surfaces."
        contains: "pull_request|merge_group|build-and-test|yanote-validation"
      - path: "scripts/ci/render-yanote-summary.mjs"
        provides: "Summary renderer that emits metrics, fail reason, deterministic top issues, and artifact references."
        exports: ["renderSummary"]
      - path: "scripts/ci/collect-yanote-artifacts.sh"
        provides: "Deterministic artifact path collection and naming for report + diagnostic support files."
        contains: "yanote-report.json"
      - path: "scripts/ci/render-yanote-summary.test.mjs"
        provides: "Contract tests for summary size, top-5 ordering, and no raw payload leakage."
        contains: "Top Issues"
    key_links:
      - from: ".github/workflows/yanote-ci.yml"
        to: "yanote-gradle-plugin/src/main/kotlin/dev/yanote/gradle/tasks/YanoteCheckTask.kt"
        via: "Workflow executes aggregate Gradle check path introduced in 04-01."
        pattern: "./gradlew .*yanote.*Check"
      - from: "scripts/ci/render-yanote-summary.mjs"
        to: "yanote-js/src/cli.ts"
        via: "Summary output maps existing CLI/report compatibility fields into `GITHUB_STEP_SUMMARY`."
        pattern: "status|operations|aggregate|primary"
      - from: ".github/workflows/yanote-ci.yml"
        to: "scripts/ci/collect-yanote-artifacts.sh"
        via: "Workflow uploads deterministic artifact bundle on success and failure paths."
        pattern: "if: \\$\\{\\{ always\\(\\) \\}\\}"
---

<objective>
Deliver the GitHub Actions channel for Yanote checks with deterministic PR feedback and artifact retention.

Purpose: Satisfy DELV-03 by exposing GitHub-native check/summarization/artifact surfaces that preserve existing CLI semantics and strict CI defaults.
Output: New workflow + summary/artifact helper scripts that create reliable merge-review feedback without PR-comment dependency.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-java-build-and-ci-delivery-surfaces/04-CONTEXT.md
@.planning/phases/04-java-build-and-ci-delivery-surfaces/04-RESEARCH.md
@.planning/phases/04-java-build-and-ci-delivery-surfaces/04-VALIDATION.md
@.planning/phases/04-java-build-and-ci-delivery-surfaces/04-01-PLAN.md
@yanote-js/src/cli.ts
@yanote-js/src/report/report.ts
@examples/docker-compose.yml

<interfaces>
From `04-01` outputs:
- Aggregate Gradle Yanote tasks are available for CI invocation and preserve local/ci profile defaults.

From `yanote-js/src/cli.ts`:
- Deterministic summary sections and machine line (`YANOTE_SUMMARY`) already exist; GitHub summary should map these fields instead of inventing new semantics.

From locked phase decisions:
- PR UX contract is Checks status + concise summary + artifacts; PR comments are explicitly deferred and must not be added here.
- Summary must cap to deterministic top 5 issues and avoid raw payload dumps.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add GitHub workflow with two stable required-check job surfaces</name>
  <files>.github/workflows/yanote-ci.yml</files>
  <behavior>
    - Test: Workflow defines stable job names `build-and-test` and `yanote-validation` for branch-protection compatibility.
    - Test: `pull_request` path runs fast blocking checks (build/tests + Yanote validation) without forcing full compose e2e.
    - Test: Workflow includes `merge_group` trigger support so required checks still report in merge-queue flows.
    - Test: Required jobs pin Java to version 21.
  </behavior>
  <action>Create `yanote-ci.yml` using official actions (`actions/checkout`, `actions/setup-java`, `gradle/actions/setup-gradle`). Wire `yanote-validation` to the aggregate Yanote Gradle check path from 04-01 and keep check names stable. Keep this plan focused on PR and merge-group surfaces; full `main`/release e2e hardening is handled in 04-03.</action>
  <verify>
    <automated>rg "build-and-test:" .github/workflows/yanote-ci.yml && rg "yanote-validation:" .github/workflows/yanote-ci.yml && rg "merge_group:" .github/workflows/yanote-ci.yml && rg "java-version: '21'|java-version: 21" .github/workflows/yanote-ci.yml</automated>
  </verify>
  <done>GitHub workflow exists with deterministic two-check contract and Java 21 pinning in required jobs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement concise GitHub summary renderer with deterministic top-5 issue ordering</name>
  <files>scripts/ci/render-yanote-summary.mjs, scripts/ci/render-yanote-summary.test.mjs</files>
  <behavior>
    - Test: Summary includes status, core coverage metrics, deterministic fail reason, and top 5 issues (default) in stable order.
    - Test: Summary remains single-screen oriented and excludes raw event payload/body data.
    - Test: Missing/invalid report input fails with actionable diagnostics and non-zero exit.
  </behavior>
  <action>Implement a small Node script that reads report + CLI diagnostics artifacts and writes markdown to `GITHUB_STEP_SUMMARY`. Preserve CLI parity terminology (`status`, `operations`, `aggregate`, primary failure) and deterministic issue ranking. Keep script transport-only; no new coverage/governance logic.</action>
  <verify>
    <automated>node --test scripts/ci/render-yanote-summary.test.mjs</automated>
  </verify>
  <done>Workflow can publish concise deterministic summaries that match established CLI semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add deterministic artifact bundle publication with failure-path retention</name>
  <files>.github/workflows/yanote-ci.yml, scripts/ci/collect-yanote-artifacts.sh</files>
  <behavior>
    - Test: Artifact upload runs under `if: always()` and still publishes deterministic report artifacts when validation fails after snapshot generation.
    - Test: Artifact bundle includes deterministic JSON report and lightweight diagnostics needed for triage.
    - Test: Artifact naming is stable/human-recognizable and retention is set to medium-term range.
    - Test: Workflow override inputs are limited and explicit (no arbitrary CLI pass-through).
  </behavior>
  <action>Add an artifact collection helper that gathers known deterministic outputs (report JSON, summary input, diagnostics) and expose stable artifact names in workflow upload steps. Configure workflow-dispatch or action inputs only for approved override knobs so GitHub and Gradle channels remain conceptually aligned.</action>
  <verify>
    <automated>npm -C yanote-js run test -- src/cli.summary.contract.test.ts src/cli.report.test.ts</automated>
  </verify>
  <done>GitHub runs consistently retain actionable Yanote artifacts and bounded override behavior on both pass and fail paths.</done>
</task>

</tasks>

<verification>
- [ ] `./gradlew test && npm -C yanote-js test` succeeds.
- [ ] `node --test scripts/ci/render-yanote-summary.test.mjs` succeeds.
- [ ] Workflow emits only the two required-check job names and deterministic artifact names.
</verification>

<success_criteria>
- DELV-03 is satisfied with a GitHub Actions delivery channel that exposes deterministic check outcomes, summary, and artifacts.
- QUAL-03 baseline is enforced in required CI jobs by explicit Java 21 setup.
- PR-facing contract remains concise and parity-aligned with existing CLI/report behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/04-java-build-and-ci-delivery-surfaces/04-02-SUMMARY.md`
</output>
