---
phase: 04-java-build-and-ci-delivery-surfaces
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - settings.gradle.kts
  - yanote-gradle-plugin/build.gradle.kts
  - yanote-gradle-plugin/src/main/kotlin/dev/yanote/gradle/YanoteExtension.kt
  - yanote-gradle-plugin/src/main/kotlin/dev/yanote/gradle/YanotePlugin.kt
  - yanote-gradle-plugin/src/main/kotlin/dev/yanote/gradle/tasks/YanoteReportTask.kt
  - yanote-gradle-plugin/src/main/kotlin/dev/yanote/gradle/tasks/YanoteCheckTask.kt
  - yanote-gradle-plugin/src/test/kotlin/dev/yanote/gradle/YanotePluginContractTest.kt
  - yanote-gradle-plugin/src/test/kotlin/dev/yanote/gradle/YanoteMultiModuleWiringTest.kt
  - yanote-gradle-plugin/src/test/kotlin/dev/yanote/gradle/YanoteTaskExecutionContractTest.kt
autonomous: true
requirements:
  - DELV-02
user_setup: []
must_haves:
    truths:
      - "Java teams can run stable Gradle tasks `yanoteReport` and `yanoteCheck` without invoking raw CLI flags."
      - "`yanoteCheck` is blocking and fail-closed on missing/invalid required inputs, while `yanoteReport` stays report-first and non-blocking."
      - "Multi-module builds support per-module troubleshooting plus one aggregate root task path for CI."
      - "Channel precedence remains deterministic: explicit Gradle DSL/task override > policy file > built-in defaults."
    artifacts:
      - path: "yanote-gradle-plugin/src/main/kotlin/dev/yanote/gradle/YanotePlugin.kt"
        provides: "Plugin entrypoint, task registration, and opt-in `check` lifecycle wiring."
        exports: ["YanotePlugin"]
      - path: "yanote-gradle-plugin/src/main/kotlin/dev/yanote/gradle/YanoteExtension.kt"
        provides: "Limited override DSL surface with module exclude controls."
        exports: ["YanoteExtension"]
      - path: "yanote-gradle-plugin/src/main/kotlin/dev/yanote/gradle/tasks/YanoteReportTask.kt"
        provides: "Local/report-first analyzer wrapper task."
        exports: ["YanoteReportTask"]
      - path: "yanote-gradle-plugin/src/main/kotlin/dev/yanote/gradle/tasks/YanoteCheckTask.kt"
        provides: "Blocking governance analyzer wrapper task."
        exports: ["YanoteCheckTask"]
      - path: "yanote-gradle-plugin/src/test/kotlin/dev/yanote/gradle/YanoteTaskExecutionContractTest.kt"
        provides: "Task behavior tests for missing-input handling, profile defaults, and override limits."
        contains: "yanoteReport|yanoteCheck"
    key_links:
      - from: "yanote-gradle-plugin/src/main/kotlin/dev/yanote/gradle/tasks/YanoteReportTask.kt"
        to: "dist/node-analyzer/bin/yanote.cjs"
        via: "Task shells into existing analyzer report command with local defaults."
        pattern: "node .*yanote\\.cjs report"
      - from: "yanote-gradle-plugin/src/main/kotlin/dev/yanote/gradle/tasks/YanoteCheckTask.kt"
        to: "yanote-js/src/cli.ts"
        via: "Task reuses existing deterministic CLI/governance semantics instead of reimplementation."
        pattern: "--profile ci|--policy|--min-coverage"
      - from: "yanote-gradle-plugin/src/main/kotlin/dev/yanote/gradle/YanotePlugin.kt"
        to: "settings.gradle.kts"
        via: "Plugin discovers Java subprojects, applies excludes, and wires aggregate root tasks."
        pattern: "subprojects|exclude|aggregate"
---

<objective>
Deliver the Java-native Gradle delivery surface for Yanote without changing analyzer semantics.

Purpose: Satisfy DELV-02 by exposing stable v1 Gradle tasks that wrap the deterministic CLI/governance contract with low-friction local defaults and explicit blocking checks.
Output: New `yanote-gradle-plugin` module with stable tasks, multi-module aggregate wiring, and contract tests.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-java-build-and-ci-delivery-surfaces/04-CONTEXT.md
@.planning/phases/04-java-build-and-ci-delivery-surfaces/04-RESEARCH.md
@.planning/phases/04-java-build-and-ci-delivery-surfaces/04-VALIDATION.md
@build.gradle.kts
@settings.gradle.kts
@yanote-js/src/cli.ts
@yanote-js/src/gates/policy.ts

<interfaces>
From `build.gradle.kts`:
- Existing distribution task `distNodeAnalyzer` already places the runtime analyzer at `dist/node-analyzer/bin/yanote.cjs`; plugin tasks must execute this path rather than creating a second analyzer runtime.

From `yanote-js/src/cli.ts`:
- Analyzer entrypoint is `yanote report` with deterministic output/failure contracts and explicit options (`--policy`, `--profile`, `--min-coverage`, `--min-aggregate`, `--critical-operation`, `--exclude`).

From locked phase decisions:
- `yanoteReport`/`yanoteCheck` names and compatibility must remain stable across v1 minor versions.
- `check` lifecycle hookup is opt-in only (no default auto-hook).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold Gradle plugin module and freeze public DSL/task contracts</name>
  <files>settings.gradle.kts, yanote-gradle-plugin/build.gradle.kts, yanote-gradle-plugin/src/main/kotlin/dev/yanote/gradle/YanoteExtension.kt, yanote-gradle-plugin/src/main/kotlin/dev/yanote/gradle/YanotePlugin.kt, yanote-gradle-plugin/src/test/kotlin/dev/yanote/gradle/YanotePluginContractTest.kt</files>
  <behavior>
    - Test: Plugin registers stable task names `yanoteReport` and `yanoteCheck` as v1 compatibility surface.
    - Test: Gradle extension exposes only the limited override set (policy path, profile, min coverage knobs, critical operations, exclusions, check hook toggle, module excludes).
    - Test: Plugin defaults keep `check` unmodified unless explicit boolean opt-in is enabled.
  </behavior>
  <action>Create a dedicated `yanote-gradle-plugin` module and register a plugin ID + extension that encodes the locked delivery contract. Keep the override surface intentionally small (no unrestricted CLI pass-through) so precedence and parity remain deterministic. Treat DSL keys and task names as versioned API; avoid future-breaking naming churn in v1.</action>
  <verify>
    <automated>./gradlew :yanote-gradle-plugin:test --tests "*YanotePluginContractTest"</automated>
  </verify>
  <done>Plugin module exists, compiles, and contract tests lock stable names plus opt-in check wiring behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Java-module discovery, exclude filtering, and aggregate task orchestration</name>
  <files>yanote-gradle-plugin/src/main/kotlin/dev/yanote/gradle/YanotePlugin.kt, yanote-gradle-plugin/src/test/kotlin/dev/yanote/gradle/YanoteMultiModuleWiringTest.kt</files>
  <behavior>
    - Test: Plugin auto-discovers Java subprojects by default and creates per-module `yanoteReport`/`yanoteCheck` tasks.
    - Test: Explicit exclude list removes selected modules from both per-module and aggregate execution scope.
    - Test: Root aggregate tasks depend on discovered module tasks in deterministic project-path order.
  </behavior>
  <action>Use lazy task configuration (`tasks.register`/`tasks.named`) to avoid eager realization. Register one root aggregate report task and one root aggregate check task that orchestrate participating modules while preserving per-module debug workflows. Maintain deterministic wiring order and no duplicate task attachment.</action>
  <verify>
    <automated>./gradlew :yanote-gradle-plugin:test --tests "*YanoteMultiModuleWiringTest"</automated>
  </verify>
  <done>Multi-module builds support deterministic per-module and aggregate Yanote task execution with explicit exclusion control.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add task execution wrappers with local-vs-ci defaults and missing-input behavior split</name>
  <files>yanote-gradle-plugin/src/main/kotlin/dev/yanote/gradle/tasks/YanoteReportTask.kt, yanote-gradle-plugin/src/main/kotlin/dev/yanote/gradle/tasks/YanoteCheckTask.kt, yanote-gradle-plugin/src/test/kotlin/dev/yanote/gradle/YanoteTaskExecutionContractTest.kt</files>
  <behavior>
    - Test: `yanoteReport` defaults to local/report-first profile and returns non-blocking behavior for missing optional inputs while still generating diagnostics/artifacts.
    - Test: `yanoteCheck` defaults to strict ci profile and fails hard with actionable diagnostics when required inputs are missing/invalid.
    - Test: Effective policy precedence remains override > policy file > defaults and maps to existing CLI options only.
    - Test: Output paths include per-module artifacts and one aggregate report artifact contract for CI consumption.
  </behavior>
  <action>Implement thin task wrappers that execute `node dist/node-analyzer/bin/yanote.cjs report ...` with deterministic argument ordering and compact default console output. Do not re-implement analyzer logic in Kotlin; preserve CLI parity by delegating semantics to existing Node contract. Keep detailed diagnostics in artifacts/verbose mode while default logs remain concise.</action>
  <verify>
    <automated>./gradlew :yanote-gradle-plugin:test && npm -C yanote-js run test -- src/gates/policy.test.ts</automated>
  </verify>
  <done>Gradle tasks reliably transport existing CLI/governance semantics with locked profile behavior and deterministic output contracts.</done>
</task>

</tasks>

<verification>
- [ ] `./gradlew :yanote-gradle-plugin:test` succeeds.
- [ ] `./gradlew distNodeAnalyzer` succeeds so plugin runtime path is valid.
- [ ] `./gradlew tasks --all` shows stable `yanoteReport` / `yanoteCheck` surfaces plus aggregate variants.
</verification>

<success_criteria>
- DELV-02 is satisfied through a Gradle plugin with stable tasks integrated into Java build workflows.
- Locked local-vs-ci strictness split is implemented without semantic drift from the existing CLI.
- Multi-module orchestration provides both per-module and aggregate execution with deterministic behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/04-java-build-and-ci-delivery-surfaces/04-01-SUMMARY.md`
</output>
