---
phase: 04-java-build-and-ci-delivery-surfaces
plan: 04
type: execute
wave: 4
depends_on: ["04-03"]
files_modified:
  - .github/workflows/yanote-ci.yml
  - scripts/ci/yanote-ci-workflow.contract.test.mjs
  - scripts/ci/run-yanote-gradle-check.sh
  - scripts/ci/collect-yanote-artifacts.sh
  - scripts/ci/collect-yanote-artifacts.test.mjs
autonomous: true
requirements:
  - DELV-02
  - DELV-03
  - QUAL-02
  - QUAL-03
gap_closure: true
user_setup: []
must_haves:
  truths:
    - "GitHub `yanote-validation` executes an aggregate Gradle `yanoteCheck` delivery path (or equivalent rooted Gradle wrapper path), not only plugin tests plus direct Node CLI."
    - "Failure triage remains always-on: summary rendering and artifact upload still run under `if: ${{ always() }}` after validation execution."
    - "Parity intent is explicit and test-locked: workflow contract tests fail if `yanoteCheck` invocation disappears or direct CLI-only validation returns."
    - "Java 21 baseline enforcement remains present in required checks while gap wiring is corrected."
  artifacts:
    - path: ".github/workflows/yanote-ci.yml"
      provides: "Gap-fixed validation path that invokes aggregate Gradle `yanoteCheck` while preserving deterministic triage flow."
      contains: "yanote-validation|./gradlew .*yanoteCheck|if: ${{ always() }}"
    - path: "scripts/ci/run-yanote-gradle-check.sh"
      provides: "Reusable CI helper that runs rooted Gradle `yanoteCheck`, captures logs/exit code, and feeds existing summary/artifact pipeline."
      contains: "./gradlew .*yanoteCheck"
    - path: "scripts/ci/yanote-ci-workflow.contract.test.mjs"
      provides: "Contract assertions for aggregate Gradle invocation, no direct CLI-only bypass, and always-on triage retention."
      contains: "yanoteCheck|always\\(\\)"
    - path: "scripts/ci/collect-yanote-artifacts.sh"
      provides: "Artifact collection compatibility with the adjusted Gradle validation output path."
      contains: "build/yanote/aggregate/check/yanote-report.json"
  key_links:
    - from: ".github/workflows/yanote-ci.yml"
      to: "scripts/ci/run-yanote-gradle-check.sh"
      via: "yanote-validation run step delegates execution to Gradle parity helper."
      pattern: "run: bash scripts/ci/run-yanote-gradle-check\\.sh"
    - from: "scripts/ci/run-yanote-gradle-check.sh"
      to: "yanoteCheck"
      via: "Helper invokes explicit rooted Gradle `yanoteCheck` command and captures exit code."
      pattern: "./gradlew .*yanoteCheck"
    - from: ".github/workflows/yanote-ci.yml"
      to: "scripts/ci/collect-yanote-artifacts.sh"
      via: "Artifact collection remains always-on after validation execution."
      pattern: "if: \\$\\{\\{ always\\(\\) \\}\\}"
    - from: ".github/workflows/yanote-ci.yml"
      to: "scripts/ci/render-yanote-summary.mjs"
      via: "Summary rendering remains always-on and consumes deterministic artifact bundle."
      pattern: "Render Yanote GitHub summary"
---

<objective>
Close the Phase 4 blocker by wiring GitHub validation to the aggregate Gradle `yanoteCheck` path while preserving deterministic failure-triage behavior.

Purpose: Restore DELV-02/DELV-03 channel parity and keep QUAL-02/QUAL-03 merge-blocking CI guarantees intact.
Output: One focused CI wiring patch that replaces CLI-only validation execution with rooted Gradle check execution and keeps always-on summary/artifact retention.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-java-build-and-ci-delivery-surfaces/04-CONTEXT.md
@.planning/phases/04-java-build-and-ci-delivery-surfaces/04-RESEARCH.md
@.planning/phases/04-java-build-and-ci-delivery-surfaces/04-VERIFICATION.md
@.planning/phases/04-java-build-and-ci-delivery-surfaces/04-02-SUMMARY.md
@.planning/phases/04-java-build-and-ci-delivery-surfaces/04-03-SUMMARY.md
@.github/workflows/yanote-ci.yml
@scripts/ci/yanote-ci-workflow.contract.test.mjs
@scripts/ci/collect-yanote-artifacts.sh
@scripts/ci/render-yanote-summary.mjs

<interfaces>
From `.github/workflows/yanote-ci.yml` (current gap):
- `yanote-validation` currently runs plugin test filtering plus direct `node yanote-js/dist/yanote.cjs report ...`, which bypasses aggregate Gradle `yanoteCheck` invocation.
- `Collect Yanote artifacts`, `Render Yanote GitHub summary`, and `Upload Yanote artifacts` already run with `if: ${{ always() }}` and must remain so.

From `04-VERIFICATION.md`:
- Missing link is explicit: invoke aggregate `yanoteCheck` (or equivalent rooted Gradle delivery task) in `yanote-validation`.
- Retention contract is already correct and must be preserved after rewiring.

From `scripts/ci/collect-yanote-artifacts.sh`:
- Collector already prioritizes aggregate Gradle output locations (`build/yanote/aggregate/check` then report/module fallbacks), so the gap fix should feed this path instead of inventing a new artifact contract.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the gap as failing CI contract tests before workflow rewiring</name>
  <files>scripts/ci/yanote-ci-workflow.contract.test.mjs</files>
  <behavior>
    - Test: `yanote-validation` contains an explicit Gradle invocation targeting `yanoteCheck` (allowing rooted/equivalent aggregate path forms).
    - Test: direct CLI-only validation path (`node yanote-js/dist/yanote.cjs report`) is not the primary validation execution path.
    - Test: artifact collection, summary render, and artifact upload steps remain `if: ${{ always() }}`.
    - Test: Java 21 assertion calls remain present in both required checks (no QUAL-03 regression).
  </behavior>
  <action>Strengthen workflow contract tests to encode the blocker and guardrails in executable form. Keep assertions focused on parity wiring and triage retention only; do not broaden scope to unrelated topology changes. This task should fail against current workflow before implementation starts (RED), then pass once Task 2 lands (GREEN).</action>
  <verify>
    <automated>node --test scripts/ci/yanote-ci-workflow.contract.test.mjs</automated>
  </verify>
  <done>Contract test suite fails on current gap and becomes a permanent guard against future regression of Gradle parity wiring.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewire `yanote-validation` to execute rooted aggregate Gradle `yanoteCheck` while preserving always-on triage</name>
  <files>.github/workflows/yanote-ci.yml, scripts/ci/run-yanote-gradle-check.sh, scripts/ci/collect-yanote-artifacts.sh, scripts/ci/collect-yanote-artifacts.test.mjs</files>
  <behavior>
    - Test: Validation execution path runs explicit `./gradlew ... yanoteCheck` (or rooted equivalent) and records exit code for final gate enforcement.
    - Test: Summary/artifact steps still execute under `if: ${{ always() }}` after Gradle validation execution.
    - Test: Artifact collector still yields deterministic bundle names and manifest fields for both snapshot and no-snapshot paths.
    - Test: Existing branch-protection job names and Java 21 setup/assertion flow remain unchanged.
  </behavior>
  <action>Create a dedicated CI helper script that performs the Gradle parity execution path, including any minimal rooted fixture/bootstrap required to invoke aggregate `yanoteCheck` deterministically in CI. Update `yanote-validation` to call this helper (or direct rooted `./gradlew ... yanoteCheck` command) instead of direct Node CLI execution. Preserve current failure-triage contract by keeping collect/render/upload/enforce sequence and `always()` semantics intact. If output locations shift due to rooted execution context, update artifact collector candidates and collector contract tests so deterministic artifact naming and manifest behavior remain stable.</action>
  <verify>
    <automated>bash -n scripts/ci/run-yanote-gradle-check.sh && node --test scripts/ci/yanote-ci-workflow.contract.test.mjs scripts/ci/collect-yanote-artifacts.test.mjs</automated>
  </verify>
  <done>`yanote-validation` uses aggregate Gradle `yanoteCheck` parity path, keeps always-on triage retention, and passes updated contract tests.</done>
</task>

</tasks>

<verification>
- [ ] `node --test scripts/ci/yanote-ci-workflow.contract.test.mjs` passes with explicit `yanoteCheck` workflow wiring.
- [ ] `node --test scripts/ci/collect-yanote-artifacts.test.mjs` passes with deterministic artifact contract intact.
- [ ] `./gradlew test && npm -C yanote-js test` remains green (no cross-surface regression).
</verification>

<success_criteria>
- The Phase 4 verification blocker is closed: GitHub `yanote-validation` now invokes aggregate Gradle `yanoteCheck` parity path.
- Failure triage parity is preserved: deterministic summary + artifacts remain always-on after validation execution.
- Required-check and Java 21 contracts remain stable (`build-and-test`, `yanote-validation`, `assert-java21` usage intact).
</success_criteria>

<output>
After completion, create `.planning/phases/04-java-build-and-ci-delivery-surfaces/04-04-SUMMARY.md`
</output>
