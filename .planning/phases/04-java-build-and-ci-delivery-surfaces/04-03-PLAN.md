---
phase: 04-java-build-and-ci-delivery-surfaces
plan: 03
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - .github/workflows/yanote-ci.yml
  - scripts/ci/assert-java21.sh
  - scripts/ci/run-v1-e2e.sh
  - .github/BRANCH_PROTECTION.md
  - examples/docker-compose.yml
autonomous: true
requirements:
  - QUAL-02
  - QUAL-03
user_setup:
  - service: github
    why: "Enable merge-blocking required checks in repository branch protection."
    dashboard_config:
      - task: "Set `build-and-test` and `yanote-validation` as required status checks for protected branches."
        location: "Repository Settings -> Branches -> Branch protection rules"
      - task: "If merge queue is enabled, verify required checks are reported for merge-group runs."
        location: "Repository Settings -> Branches -> Merge queue"
must_haves:
    truths:
      - "Pull requests are merge-blocked when either `build-and-test` or `yanote-validation` fails."
      - "Main/release flows run full v1 e2e workflow checks in addition to PR-fast-path checks."
      - "All required checks enforce Java 21 baseline and fail with actionable diagnostics on mismatch."
      - "Failure triage remains deterministic across status, summary, and retained artifacts."
    artifacts:
      - path: ".github/workflows/yanote-ci.yml"
        provides: "Branch-aware CI topology with PR fast path and main/release full-e2e hardening."
        contains: "pull_request|merge_group|main|release|build-and-test|yanote-validation"
      - path: "scripts/ci/assert-java21.sh"
        provides: "Explicit Java runtime major-version guard for required CI jobs."
        contains: "java -version"
      - path: "scripts/ci/run-v1-e2e.sh"
        provides: "Deterministic script wrapper for compose-based v1 end-to-end validation."
        contains: "docker compose -f examples/docker-compose.yml"
      - path: ".github/BRANCH_PROTECTION.md"
        provides: "Required-check naming contract and merge-blocking setup guidance."
        contains: "build-and-test|yanote-validation"
    key_links:
      - from: ".github/workflows/yanote-ci.yml"
        to: "scripts/ci/run-v1-e2e.sh"
        via: "Main/release job executes full deterministic v1 workflow validation."
        pattern: "run-v1-e2e\\.sh"
      - from: ".github/workflows/yanote-ci.yml"
        to: "scripts/ci/assert-java21.sh"
        via: "Each required job enforces Java 21 baseline before execution."
        pattern: "assert-java21\\.sh"
      - from: ".github/BRANCH_PROTECTION.md"
        to: ".github/workflows/yanote-ci.yml"
        via: "Documentation maps exact required-check names to workflow job IDs."
        pattern: "build-and-test|yanote-validation"
---

<objective>
Harden merge-blocking CI behavior for Phase 4 with branch-aware workflows and Java 21 enforcement.

Purpose: Complete QUAL-02 and QUAL-03 by making required checks deterministic, actionable, and branch-protection ready while preserving PR speed.
Output: Updated CI workflow topology, Java 21 assertion tooling, and required-check contract documentation.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-java-build-and-ci-delivery-surfaces/04-CONTEXT.md
@.planning/phases/04-java-build-and-ci-delivery-surfaces/04-RESEARCH.md
@.planning/phases/04-java-build-and-ci-delivery-surfaces/04-VALIDATION.md
@.planning/phases/04-java-build-and-ci-delivery-surfaces/04-01-PLAN.md
@.planning/phases/04-java-build-and-ci-delivery-surfaces/04-02-PLAN.md
@.github/workflows/yanote-ci.yml
@examples/docker-compose.yml

<interfaces>
From `04-02` outputs:
- Stable required-check job names are already established: `build-and-test` and `yanote-validation`.
- Summary/artifact contracts are already deterministic and should be reused, not redesigned.

From locked phase decisions:
- PR path stays fast and blocking with the two required checks.
- Full e2e v1 workflow is mandatory and blocking for `main` and release-oriented flows.
- Java 21 is hard baseline across all required checks.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend workflow to enforce PR fast path and main/release full e2e path</name>
  <files>.github/workflows/yanote-ci.yml, scripts/ci/run-v1-e2e.sh</files>
  <behavior>
    - Test: PR/merge-group runs execute only `build-and-test` + `yanote-validation` as blocking checks.
    - Test: `main` and release-oriented refs run additional full v1 e2e validation and fail on any e2e break.
    - Test: E2E script uses deterministic compose command and exits with report-container status code.
  </behavior>
  <action>Update workflow trigger/ref logic and job conditions to preserve the locked split: fast PR blocking checks, heavier main/release confidence checks. Add a script wrapper around `examples/docker-compose.yml` so e2e invocation and diagnostics are stable/reusable in CI logs and local reruns.</action>
  <verify>
    <automated>bash -n scripts/ci/run-v1-e2e.sh && rg "docker compose -f examples/docker-compose.yml" scripts/ci/run-v1-e2e.sh && rg "pull_request:" .github/workflows/yanote-ci.yml && rg "merge_group:" .github/workflows/yanote-ci.yml && rg "run-v1-e2e\\.sh" .github/workflows/yanote-ci.yml</automated>
  </verify>
  <done>CI topology enforces fast PR gating and strict main/release e2e gating using deterministic execution paths.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce Java 21 baseline in all required jobs with explicit mismatch diagnostics</name>
  <files>.github/workflows/yanote-ci.yml, scripts/ci/assert-java21.sh</files>
  <behavior>
    - Test: Required jobs fail early if runtime major version is not 21.
    - Test: Failure message tells maintainers exactly how to fix setup-java configuration.
    - Test: Java baseline guard runs in both PR and main/release required-check paths.
  </behavior>
  <action>Add a small Java-version assertion script and invoke it immediately after `actions/setup-java` in required workflow jobs. Keep enforcement explicit even though toolchain is configured in Gradle so CI/runtime mismatch cannot silently pass.</action>
  <verify>
    <automated>bash scripts/ci/assert-java21.sh && ./gradlew test</automated>
  </verify>
  <done>Required check jobs are hard-pinned to Java 21 with actionable mismatch failures.</done>
</task>

<task type="auto">
  <name>Task 3: Publish branch-protection required-check contract for merge blocking</name>
  <files>.github/BRANCH_PROTECTION.md, .github/workflows/yanote-ci.yml</files>
  <action>Document exact required check names, expected triggers, and failure-handling expectations so repository branch protection can be configured without ambiguity. Keep contract aligned to two required checks (`build-and-test`, `yanote-validation`) and include merge-group reporting notes. Do not add PR comment workflows or non-GitHub CI channels (deferred scope).</action>
  <verify>
    <automated>rg "build-and-test|yanote-validation|merge_group" .github/workflows/yanote-ci.yml .github/BRANCH_PROTECTION.md</automated>
  </verify>
  <done>Required-check naming and merge-blocking setup are explicit, deterministic, and ready for GitHub branch-protection configuration.</done>
</task>

</tasks>

<verification>
- [ ] `./gradlew test && npm -C yanote-js test` succeeds.
- [ ] `bash scripts/ci/assert-java21.sh` succeeds under Java 21.
- [ ] `bash scripts/ci/run-v1-e2e.sh` succeeds and preserves deterministic report artifacts.
</verification>

<success_criteria>
- QUAL-02 is satisfied with deterministic, branch-protection-ready required checks and blocking CI behavior.
- QUAL-03 is satisfied by explicit Java 21 enforcement across required delivery channels.
- Main/release flows gain blocking end-to-end v1 workflow confidence without degrading PR fast path.
</success_criteria>

<output>
After completion, create `.planning/phases/04-java-build-and-ci-delivery-surfaces/04-03-SUMMARY.md`
</output>
