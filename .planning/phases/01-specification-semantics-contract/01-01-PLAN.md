---
phase: 01-specification-semantics-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - yanote-js/src/spec/openapi.ts
  - yanote-js/src/spec/diagnostics.ts
  - yanote-js/src/spec/semantics.ts
  - yanote-js/src/spec/openapi.test.ts
  - yanote-js/src/spec/semantics.diagnostics.test.ts
  - yanote-js/test/fixtures/openapi/simple.yaml
autonomous: true
requirements:
  - SPEC-01
  - SPEC-02
user_setup: []
must_haves:
  truths:
    - "Node analyzer emits stable canonical HTTP operation keys for identical OpenAPI input."
    - "Malformed or unsupported OpenAPI operations emit explicit `invalid` diagnostics with path/method context."
    - "Duplicate canonical operation keys are deduplicated deterministically in insertion order."
  artifacts:
    - path: "yanote-js/src/spec/semantics.ts"
      provides: "Semantic bundle assembly for canonical key extraction and invalid diagnostics."
      exports: ["buildHttpSemantics"]
    - path: "yanote-js/src/spec/diagnostics.ts"
      provides: "Semantic diagnostics model for invalid semantic states."
      exports: ["SemanticDiagnostic", "SemanticDiagnosticsBundle"]
    - path: "yanote-js/src/spec/openapi.test.ts"
      provides: "Regression tests locking deterministic canonical key extraction."
      contains: "canonical"
  key_links:
    - from: "yanote-js/src/spec/openapi.ts"
      to: "yanote-js/src/spec/semantics.ts"
      via: "OpenAPI extraction delegates canonicalization and invalid diagnostics to semantic bundle assembly."
      pattern: "loadOpenApiOperations|buildHttpSemantics"
    - from: "yanote-js/src/spec/semantics.ts"
      to: "yanote-js/src/spec/diagnostics.ts"
      via: "semantic bundle emits first-class invalid diagnostics"
      pattern: "invalid|SemanticDiagnostic"
    - from: "yanote-js/src/spec/semantics.diagnostics.test.ts"
      to: "yanote-js/src/spec/semantics.ts"
      via: "fixture tests lock deterministic diagnostics behavior"
      pattern: "invalid"
---

<objective>
Implement the TypeScript semantic extraction contract for canonical OpenAPI identity and invalid-state diagnostics.

Purpose: Deliver the SPEC-01 and SPEC-02 extraction/diagnostics baseline before route-matching and CLI wiring.
Output: Deterministic canonical operation extraction, first-class invalid diagnostics, and focused Node tests.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-specification-semantics-contract/01-CONTEXT.md
@.planning/phases/01-specification-semantics-contract/01-RESEARCH.md
@yanote-js/src/model/operationKey.ts
@yanote-js/src/spec/openapi.ts

<interfaces>
From `yanote-js/src/model/operationKey.ts`:
- `OperationKey` must keep HTTP keys as `{ kind: "http", method, route }`.

From `yanote-js/src/spec/openapi.ts`:
- `loadOpenApiOperations(specPath: string): Promise<OperationKey[]>` remains the extraction entry point.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define Node semantic diagnostics contract and bundle assembly</name>
  <files>yanote-js/src/spec/diagnostics.ts, yanote-js/src/spec/semantics.ts, yanote-js/src/spec/semantics.diagnostics.test.ts</files>
  <behavior>
    - Test: Malformed/unsupported OpenAPI content produces `invalid` diagnostics with method/path context.
    - Test: Equivalent templated paths dedupe into stable insertion-ordered canonical keys.
    - Test: Repeated semantic bundle builds from the same spec produce byte-equivalent diagnostics/key ordering.
  </behavior>
  <action>Create `SemanticDiagnostic` and `SemanticDiagnosticsBundle` types in `src/spec/diagnostics.ts`, then implement `buildHttpSemantics` in `src/spec/semantics.ts` to canonicalize `METHOD + templated_route` keys and surface first-class `invalid` diagnostics. Preserve strict HTTP-only scope and do not use `operationId` as canonical identity.</action>
  <verify>
    <automated>npm -C yanote-js run test -- src/spec/semantics.diagnostics.test.ts</automated>
  </verify>
  <done>Node semantic bundle API exists with deterministic canonical keys plus actionable invalid diagnostics, and diagnostics-focused tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor OpenAPI extraction entry point onto semantic bundle contract</name>
  <files>yanote-js/src/spec/openapi.ts, yanote-js/src/spec/openapi.test.ts, yanote-js/test/fixtures/openapi/simple.yaml</files>
  <behavior>
    - Test: `loadOpenApiOperations` returns stable canonical keys from normalized templated routes.
    - Test: Duplicate canonical operations are deduped without changing insertion-order determinism.
    - Test: Extraction output stays deterministic across repeated loads of identical fixture input.
  </behavior>
  <action>Refactor `loadOpenApiOperations` to consume `buildHttpSemantics` output while preserving deterministic ordering and strict canonical identity. Keep behavior explicit and fail-closed for invalid semantic states in the returned bundle contract; do not add event-matching logic in this plan.</action>
  <verify>
    <automated>npm -C yanote-js run test -- src/spec/openapi.test.ts src/spec/semantics.diagnostics.test.ts</automated>
  </verify>
  <done>Node OpenAPI extraction is wired to semantic bundle contract and targeted extraction/diagnostic tests pass deterministically.</done>
</task>

</tasks>

<verification>
- [ ] `npm -C yanote-js run build` succeeds.
- [ ] `npm -C yanote-js run test -- src/spec/openapi.test.ts src/spec/semantics.diagnostics.test.ts` succeeds.
- [ ] Re-running the same tests yields deterministic key ordering and diagnostic content.
</verification>

<success_criteria>
- SPEC-01 is satisfied in Node runtime for canonical extraction determinism and dedupe.
- SPEC-02 invalid semantic diagnostics are explicit and reproducible in Node extraction flow.
- Node extraction contract is ready input for deterministic matcher/CLI integration in `01-03`.
</success_criteria>

<output>
After completion, create `.planning/phases/01-specification-semantics-contract/01-01-SUMMARY.md`
</output>
