---
phase: 01-specification-semantics-contract
plan: 05
type: execute
wave: 3
depends_on: ["01-03", "01-04"]
files_modified:
  - test/fixtures/spec-semantics/operation-cases.json
  - test/fixtures/spec-semantics/matching-cases.json
  - test/fixtures/spec-semantics/README.md
  - yanote-js/src/spec/semantics.parity.test.ts
  - yanote-js/src/coverage/coverage.parity.test.ts
  - yanote-core/src/test/java/dev/yanote/core/openapi/SemanticParityFixtureTest.java
  - yanote-core/src/test/java/dev/yanote/core/openapi/OperationMatcherParityFixtureTest.java
autonomous: true
requirements:
  - SPEC-01
  - SPEC-02
  - SPEC-03
user_setup: []
must_haves:
  truths:
    - "A single parity fixture corpus exists for semantic extraction and matching edge cases."
    - "Node and Java produce equivalent canonical keys and diagnostics for the same parity fixtures."
    - "Parity checks fail deterministically when either runtime drifts from the contract."
  artifacts:
    - path: "test/fixtures/spec-semantics/operation-cases.json"
      provides: "Shared canonical-operation extraction parity cases for both runtimes."
      min_lines: 20
    - path: "test/fixtures/spec-semantics/matching-cases.json"
      provides: "Shared deterministic matching parity cases including ambiguous and unmatched scenarios."
      min_lines: 20
    - path: "yanote-core/src/test/java/dev/yanote/core/openapi/SemanticParityFixtureTest.java"
      provides: "Java adapter test suite that validates shared parity fixtures."
      contains: "operation-cases.json"
  key_links:
    - from: "test/fixtures/spec-semantics/operation-cases.json"
      to: "yanote-js/src/spec/semantics.parity.test.ts"
      via: "Node parity tests consume canonical fixture expectations"
      pattern: "operation-cases\\.json"
    - from: "test/fixtures/spec-semantics/matching-cases.json"
      to: "yanote-core/src/test/java/dev/yanote/core/openapi/OperationMatcherParityFixtureTest.java"
      via: "Java parity tests consume shared matching fixture expectations"
      pattern: "matching-cases\\.json"
    - from: "yanote-js/src/spec/semantics.parity.test.ts"
      to: "yanote-core/src/test/java/dev/yanote/core/openapi/SemanticParityFixtureTest.java"
      via: "both adapters assert identical semantic outcomes from the same fixture IDs"
      pattern: "caseId"
---

<objective>
Create and enforce a cross-runtime parity fixture contract for Phase 1 semantic behavior.

Purpose: Prevent Java/Node semantic drift and lock deterministic SPEC-01/02/03 behavior before downstream coverage and governance phases depend on it.
Output: Shared parity fixture corpus plus runtime adapter tests that assert equivalent canonical keys and diagnostics.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-specification-semantics-contract/01-CONTEXT.md
@.planning/phases/01-specification-semantics-contract/01-RESEARCH.md
@yanote-js/src/spec/semantics.ts
@yanote-js/src/coverage/coverage.ts
@yanote-core/src/main/java/dev/yanote/core/openapi/OpenApiSemantics.java
@yanote-core/src/main/java/dev/yanote/core/openapi/OperationMatcher.java

<interfaces>
Expected from `01-03`:
- Node `computeCoverage(...)` exposes deterministic matching diagnostics for ambiguous/unmatched events.
- Node CLI + coverage flow fails closed on invalid/ambiguous semantic states.

Expected from `01-04`:
- Java `OpenApiSemantics` extraction is deterministic and diagnostic-aware.
- Java `OperationMatcher` performs exact-first then deterministic template fallback matching.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Establish a single shared parity fixture corpus</name>
  <files>test/fixtures/spec-semantics/operation-cases.json, test/fixtures/spec-semantics/matching-cases.json, test/fixtures/spec-semantics/README.md</files>
  <action>Create one fixture source of truth under `test/fixtures/spec-semantics` covering canonical key extraction, equivalent-template dedupe, invalid parse diagnostics, deterministic fallback matching, ambiguity, unmatched behavior, and encoded-segment edge cases. Include stable `caseId` keys and explicit expected outcomes so both runtimes can consume the same files without translation drift.</action>
  <verify>
    <automated>node -e "const fs=require('node:fs'); JSON.parse(fs.readFileSync('test/fixtures/spec-semantics/operation-cases.json','utf8')); JSON.parse(fs.readFileSync('test/fixtures/spec-semantics/matching-cases.json','utf8'));"</automated>
  </verify>
  <done>Both shared fixture JSON files parse cleanly, include documented schema/fields, and cover all locked edge-case categories from phase context.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add TypeScript parity adapter tests against shared fixtures</name>
  <files>yanote-js/src/spec/semantics.parity.test.ts, yanote-js/src/coverage/coverage.parity.test.ts</files>
  <behavior>
    - Test: Every `operation-cases.json` entry produces expected canonical key and diagnostic outputs in Node.
    - Test: Every `matching-cases.json` entry produces expected exact/fallback/ambiguous/unmatched outcomes in Node.
  </behavior>
  <action>Create Node parity tests that load shared fixture files and assert full semantic outputs (not partial snapshots). Keep assertions deterministic by sorting candidate lists before comparison and by validating all `caseId` entries. Avoid runtime-specific fixture forks; Node tests must read the shared root fixture files directly.</action>
  <verify>
    <automated>npm -C yanote-js run test -- src/spec/semantics.parity.test.ts src/coverage/coverage.parity.test.ts</automated>
  </verify>
  <done>Node parity tests pass using shared fixtures and fail when any semantic case diverges from expected contract outputs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add Java parity adapter tests against shared fixtures</name>
  <files>yanote-core/src/test/java/dev/yanote/core/openapi/SemanticParityFixtureTest.java, yanote-core/src/test/java/dev/yanote/core/openapi/OperationMatcherParityFixtureTest.java</files>
  <behavior>
    - Test: Java canonical extraction and diagnostics match every expected result in `operation-cases.json`.
    - Test: Java matcher results match every expected result in `matching-cases.json`, including ambiguous/unmatched cases.
  </behavior>
  <action>Implement Java parity tests that load the same root fixture files used by Node tests and assert canonical keys, diagnostics, and matching outcomes by `caseId`. Keep evaluation deterministic by normalizing candidate ordering before assertions. Do not duplicate fixture content into Java-only resources; use the shared fixture corpus directly to preserve parity guarantees.</action>
  <verify>
    <automated>./gradlew :yanote-core:test --tests "dev.yanote.core.openapi.SemanticParityFixtureTest" --tests "dev.yanote.core.openapi.OperationMatcherParityFixtureTest"</automated>
  </verify>
  <done>Java parity tests pass against shared fixtures and fail on semantic contract drift, matching Node fixture coverage breadth.</done>
</task>

</tasks>

<verification>
- [ ] `npm -C yanote-js run test -- src/spec/semantics.parity.test.ts src/coverage/coverage.parity.test.ts` succeeds.
- [ ] `./gradlew :yanote-core:test --tests "dev.yanote.core.openapi.SemanticParityFixtureTest" --tests "dev.yanote.core.openapi.OperationMatcherParityFixtureTest"` succeeds.
- [ ] Combined parity gate command succeeds: `npm -C yanote-js run test -- src/spec/semantics.parity.test.ts src/coverage/coverage.parity.test.ts && ./gradlew :yanote-core:test --tests "dev.yanote.core.openapi.SemanticParityFixtureTest" --tests "dev.yanote.core.openapi.OperationMatcherParityFixtureTest"`.
</verification>

<success_criteria>
- Shared parity fixtures exist as the single semantic contract source for both runtimes.
- Node and Java parity tests both pass on identical fixture files with deterministic outputs.
- Contract drift between runtimes is caught automatically by parity test failures.
</success_criteria>

<output>
After completion, create `.planning/phases/01-specification-semantics-contract/01-05-SUMMARY.md`
</output>
