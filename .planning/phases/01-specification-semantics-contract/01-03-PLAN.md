---
phase: 01-specification-semantics-contract
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - yanote-js/package.json
  - yanote-js/package-lock.json
  - yanote-js/src/coverage/coverage.ts
  - yanote-js/src/cli.ts
  - yanote-js/src/coverage/coverage.test.ts
  - yanote-js/src/coverage/coverage.matching.test.ts
  - yanote-js/src/cli.test.ts
autonomous: true
requirements:
  - SPEC-02
  - SPEC-03
user_setup: []
must_haves:
  truths:
    - "Node analyzer maps events to operations deterministically with exact-first then same-method template fallback."
    - "Ambiguous and unmatched route matches emit explicit diagnostics rather than heuristic selection."
    - "CLI report command fails closed on invalid or ambiguous semantic states."
  artifacts:
    - path: "yanote-js/src/coverage/coverage.ts"
      provides: "Deterministic exact-first and template-fallback operation matching."
      contains: "ambiguous"
    - path: "yanote-js/src/cli.ts"
      provides: "Fail-closed report behavior on semantic invalidity/ambiguity."
      contains: "diagnostic"
    - path: "yanote-js/src/coverage/coverage.matching.test.ts"
      provides: "Regression coverage for deterministic fallback, ambiguity, and unmatched outcomes."
      contains: "ambiguous"
  key_links:
    - from: "yanote-js/src/coverage/coverage.ts"
      to: "yanote-js/src/spec/semantics.ts"
      via: "coverage matching consumes canonical semantic bundle and diagnostics"
      pattern: "buildHttpSemantics|diagnostic"
    - from: "yanote-js/src/cli.ts"
      to: "yanote-js/src/coverage/coverage.ts"
      via: "report command enforces semantic fail-closed policy"
      pattern: "report|diagnostic|exit"
    - from: "yanote-js/src/cli.test.ts"
      to: "yanote-js/src/cli.ts"
      via: "CLI tests lock non-zero exit and diagnostic output for invalid/ambiguous states"
      pattern: "non-zero|diagnostic"
---

<objective>
Implement deterministic Node event-to-operation matching and fail-closed CLI semantics on top of the extraction contract.

Purpose: Deliver Node-side SPEC-03 matching behavior and SPEC-02 ambiguity/unmatched enforcement for standalone CLI users.
Output: Two-stage deterministic matcher integration, semantic diagnostics propagation, and CLI failure-policy tests.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-specification-semantics-contract/01-CONTEXT.md
@.planning/phases/01-specification-semantics-contract/01-RESEARCH.md
@yanote-js/src/spec/semantics.ts
@yanote-js/src/coverage/coverage.ts
@yanote-js/src/cli.ts

<interfaces>
Expected from `01-01`:
- `buildHttpSemantics(...)` returns canonical HTTP operation keys plus invalid diagnostics.
- OpenAPI extraction contract is deterministic and deduplicated.

From `yanote-js/src/coverage/coverage.ts`:
- `computeCoverage(operations, events, excludePatterns)` currently performs exact matching and must extend to deterministic fallback matching.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement deterministic exact-first plus template-fallback coverage matching</name>
  <files>yanote-js/package.json, yanote-js/package-lock.json, yanote-js/src/coverage/coverage.ts, yanote-js/src/coverage/coverage.test.ts, yanote-js/src/coverage/coverage.matching.test.ts</files>
  <behavior>
    - Test: Existing templated event routes match via exact canonical key equality first.
    - Test: Concrete event routes fall back to deterministic same-method template matching.
    - Test: Multiple fallback candidates emit `ambiguous` diagnostics and never auto-select; unmatched routes emit `unmatched` diagnostics.
  </behavior>
  <action>Add `path-to-regexp` via npm and implement two-stage matching in `computeCoverage`: exact key equality first, deterministic same-method template fallback second. Extend coverage result shape to carry ambiguity/unmatched diagnostics and keep matching deterministic and explainable (no first-match-wins heuristics).</action>
  <verify>
    <automated>npm -C yanote-js run test -- src/coverage/coverage.test.ts src/coverage/coverage.matching.test.ts</automated>
  </verify>
  <done>Node coverage matching supports deterministic fallback with explicit ambiguity/unmatched diagnostics and targeted coverage tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce fail-closed semantic behavior in Node CLI report command</name>
  <files>yanote-js/src/cli.ts, yanote-js/src/cli.test.ts</files>
  <behavior>
    - Test: `report` exits non-zero when semantic state includes `invalid` diagnostics.
    - Test: `report` exits non-zero when semantic state includes `ambiguous` diagnostics.
    - Test: Successful report output still emits for deterministic non-ambiguous/non-invalid inputs.
  </behavior>
  <action>Wire semantic diagnostics through CLI report flow and fail closed on `invalid` or `ambiguous` semantic states before writing successful coverage output. Preserve deterministic diagnostics in CLI output and avoid permissive fallback behavior.</action>
  <verify>
    <automated>npm -C yanote-js run test -- src/coverage/coverage.test.ts src/coverage/coverage.matching.test.ts src/cli.test.ts</automated>
  </verify>
  <done>Node CLI enforces fail-closed semantic diagnostics policy and all targeted matching/CLI tests pass.</done>
</task>

</tasks>

<verification>
- [ ] `npm -C yanote-js run build` succeeds.
- [ ] `npm -C yanote-js run test -- src/coverage/coverage.test.ts src/coverage/coverage.matching.test.ts src/cli.test.ts` succeeds.
- [ ] Repeated report runs with unchanged fixtures produce deterministic diagnostics content and ordering.
</verification>

<success_criteria>
- SPEC-03 is satisfied in Node runtime: event matching is deterministic with exact-first then template-fallback behavior.
- SPEC-02 is satisfied in Node runtime for ambiguous/unmatched route diagnostics and fail-closed CLI enforcement.
- Node matcher/CLI semantics are ready for cross-runtime parity validation in `01-05`.
</success_criteria>

<output>
After completion, create `.planning/phases/01-specification-semantics-contract/01-03-SUMMARY.md`
</output>
