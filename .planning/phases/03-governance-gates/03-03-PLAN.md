---
phase: 03-governance-gates
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - yanote-js/src/cli.ts
  - yanote-js/src/cli.report.test.ts
  - yanote-js/src/cli.summary.contract.test.ts
  - yanote-js/src/cli.test.ts
  - yanote-js/src/report/report.ts
  - yanote-js/src/report/report.test.ts
  - yanote-js/src/report/schema.ts
autonomous: true
requirements:
  - GATE-03
  - GATE-04
user_setup: []
must_haves:
    truths:
      - "Automated checks enforce deterministic pass/fail governance from one integrated policy path with strict CI behavior and warn-capable local behavior."
      - "Failure output contract is stable: one primary `YANOTE_ERROR` line plus deterministic ordered secondary diagnostics block."
      - "Machine summary on failure includes status, primary code, class counts, and explicitly emits `report=none` when report snapshot write fails."
      - "Applied exclusions are transparent and auditable in output artifacts with rationale and matched-operation counts, while stale rules are warnings."
    artifacts:
      - path: "yanote-js/src/cli.ts"
        provides: "Integrated governance orchestration and deterministic stdout/stderr contract."
        contains: "YANOTE_ERROR|YANOTE_SUMMARY|report=none"
      - path: "yanote-js/src/report/report.ts"
        provides: "Report artifact includes exclusion transparency and governance diagnostics in deterministic order."
        exports: ["buildReport", "YanoteReport"]
      - path: "yanote-js/src/cli.summary.contract.test.ts"
        provides: "Golden contract tests for fixed-order summary and failure rendering grammar."
        contains: "YANOTE_SUMMARY"
      - path: "yanote-js/src/cli.report.test.ts"
        provides: "Integration coverage for threshold/regression/exclusion outcomes and typed exits."
        contains: "threshold|regression|exclude"
    key_links:
      - from: "yanote-js/src/cli.ts"
        to: "yanote-js/src/gates/evaluator.ts"
        via: "CLI computes final gate verdict from threshold/regression outputs after exclusions."
        pattern: "evaluateGateFailures"
      - from: "yanote-js/src/cli.ts"
        to: "yanote-js/src/gates/failureOrder.ts"
        via: "Primary and secondary failure rendering uses deterministic precedence sorter."
        pattern: "sortFailuresByPrecedence"
      - from: "yanote-js/src/report/report.ts"
        to: "yanote-js/src/gates/exclusions.ts"
        via: "Report includes grouped applied exclusions and unmatched-rule warning diagnostics."
        pattern: "appliedExclusions|unmatchedRules"
---


<objective>
Integrate policy, evaluator, baseline, and exclusion behaviors into one deterministic governance contract for CLI/report automated checks.

Purpose: Close Phase 3 by locking stable pass/fail behavior and machine/human output contracts for threshold, regression, exclusions, and fail-closed outcomes.
Output: Deterministic integrated governance behavior with contract tests and full verification command cadence.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-governance-gates/03-CONTEXT.md
@.planning/phases/03-governance-gates/03-RESEARCH.md
@.planning/phases/03-governance-gates/03-VALIDATION.md
@.planning/phases/03-governance-gates/03-01-PLAN.md
@.planning/phases/03-governance-gates/03-02-PLAN.md
@yanote-js/src/cli.ts
@yanote-js/src/report/report.ts
@yanote-js/src/cli.summary.contract.test.ts
@yanote-js/src/cli.report.test.ts

<interfaces>
From `03-01` outputs:
- Policy resolution and exclusion-rule audit structures are available and deterministic.

From `03-02` outputs:
- Evaluator + precedence modules and fail-closed integrity checks are available for integration into CLI/report flow.

From existing contracts:
- `YANOTE_ERROR` and `YANOTE_SUMMARY` are public compatibility surfaces and must remain parse-stable.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Integrate governance verdict rendering into report command with profile-aware strictness</name>
  <files>yanote-js/src/cli.ts, yanote-js/src/cli.report.test.ts, yanote-js/src/cli.test.ts</files>
  <behavior>
    - Test: CLI consumes evaluator verdicts from `03-02` without mutating failure class/code semantics.
    - Test: CI/local profile handling only affects allowed warning surfaces and keeps deterministic summary grammar intact.
    - Test: Aggregate `N/A` skip diagnostics and gate outcome rendering remain deterministic in integrated CLI output.
  </behavior>
  <action>Wire pre-evaluated governance outcomes into `yanote report` rendering path while keeping gate verdict selection and formatting concerns separated. Ensure integrated output preserves deterministic ordering and typed exits from earlier waves.</action>
  <verify>
    <automated>npm -C yanote-js run test -- src/cli.report.test.ts src/cli.test.ts</automated>
  </verify>
  <done>Integrated CLI governance output path preserves deterministic verdict semantics across CI/local profiles.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock failure rendering contract (primary plus deterministic secondary block)</name>
  <files>yanote-js/src/cli.ts, yanote-js/src/cli.summary.contract.test.ts, yanote-js/src/cli.test.ts</files>
  <behavior>
    - Test: Multi-failure runs emit exactly one primary `YANOTE_ERROR` line and deterministic ordered secondary diagnostics.
    - Test: Secondary ordering follows class precedence, then error code, then operation key where present.
    - Test: Failure machine summary includes status, primary code, counts by class, and `report=none` when report write fails.
  </behavior>
  <action>Finalize error rendering and machine-summary line grammar so deterministic parsing remains stable while adding secondary diagnostics block. Preserve no-ANSI, fixed-order, and final-line guarantees of existing CLI summary contract.</action>
  <verify>
    <automated>npm -C yanote-js run test -- src/cli.summary.contract.test.ts src/cli.test.ts</automated>
  </verify>
  <done>Failure output contract is deterministic, parse-stable, and fully covered by summary/CLI contract tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Publish exclusion transparency in report artifacts and summary output</name>
  <files>yanote-js/src/report/report.ts, yanote-js/src/report/schema.ts, yanote-js/src/report/report.test.ts, yanote-js/src/cli.report.test.ts</files>
  <behavior>
    - Test: Report includes grouped applied exclusions (rule metadata + matched count) and unmatched-rule warning diagnostics.
    - Test: Critical-operation exclusion override usage is surfaced explicitly in output artifacts.
    - Test: Output ordering for exclusion and gate diagnostics is deterministic.
  </behavior>
  <action>Extend report DTO/schema to include exclusion transparency blocks needed for auditability while preserving strict schema validation and deterministic serialization. Ensure CLI summary references these artifacts without duplicating non-deterministic detail rendering.</action>
  <verify>
    <automated>npm -C yanote-js run test -- src/report/report.test.ts src/cli.report.test.ts src/report/report.contract.test.ts</automated>
  </verify>
  <done>Exclusion transparency and governance diagnostics are visible and deterministic in report and CLI artifacts.</done>
</task>

</tasks>

<verification>
- [ ] `npm -C yanote-js run test -- src/cli.report.test.ts src/cli.summary.contract.test.ts src/cli.test.ts src/report/report.test.ts` succeeds.
- [ ] `npm -C yanote-js test` succeeds.
- [ ] `npm -C yanote-js run build` succeeds.
- [ ] `./gradlew test` succeeds.
</verification>

<success_criteria>
- GATE-03 exclusion policy transparency is present in output artifacts with deterministic ordering.
- GATE-04 fail-closed outcomes and machine-readable diagnostics are stable and contract-tested.
- Integrated CLI/report rendering preserves deterministic contract semantics from prior evaluator and precedence waves.
</success_criteria>

<output>
After completion, create `.planning/phases/03-governance-gates/03-03-SUMMARY.md`
</output>
