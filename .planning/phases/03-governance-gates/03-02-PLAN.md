---
phase: 03-governance-gates
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - yanote-js/src/baseline/baseline.ts
  - yanote-js/src/baseline/baseline.v2.test.ts
  - yanote-js/src/gates/evaluator.ts
  - yanote-js/src/gates/failureOrder.ts
  - yanote-js/src/gates/failureOrder.test.ts
  - yanote-js/src/gates/evaluator.threshold.test.ts
  - yanote-js/src/gates/evaluator.regression.test.ts
  - yanote-js/src/cli.ts
  - yanote-js/src/cli.report.test.ts
  - yanote-js/src/cli.failclosed.contract.test.ts
  - yanote-js/src/events/readJsonl.ts
  - yanote-js/src/events/readJsonl.test.ts
autonomous: true
requirements:
  - GATE-01
  - GATE-02
  - GATE-04
user_setup: []
must_haves:
    truths:
      - "Baseline comparison contract is versioned and includes covered operation set plus dimension summary snapshot."
      - "Any loss of previously covered operation is a hard regression failure; removed spec operations are neutral."
      - "Failure precedence is deterministic: input > semantic > gate > runtime, and gate-internal precedence is regression > threshold."
      - "Invalid/incompatible evidence is fail-closed with actionable diagnostics; no passing outcome is allowed."
    artifacts:
      - path: "yanote-js/src/baseline/baseline.ts"
        provides: "Baseline format adapter/validator and explicit baseline update helpers."
        exports: ["readBaseline", "writeBaseline", "compareRegressionAgainstBaseline"]
      - path: "yanote-js/src/gates/evaluator.ts"
        provides: "Threshold and regression gate evaluator over post-exclusion scope."
        exports: ["evaluateThresholdGate", "evaluateRegressionGate", "evaluateGateFailures"]
      - path: "yanote-js/src/gates/failureOrder.ts"
        provides: "Deterministic ordering and primary/secondary failure selection."
        exports: ["sortFailuresByPrecedence", "selectPrimaryFailure"]
      - path: "yanote-js/src/cli.ts"
        provides: "Fail-closed governance orchestration and typed diagnostic emission."
        contains: "YANOTE_ERROR|YANOTE_SUMMARY"
    key_links:
      - from: "yanote-js/src/cli.ts"
        to: "yanote-js/src/gates/failureOrder.ts"
        via: "CLI emits one primary failure and deterministic secondary ordering."
        pattern: "sortFailuresByPrecedence|selectPrimaryFailure"
      - from: "yanote-js/src/gates/evaluator.ts"
        to: "yanote-js/src/baseline/baseline.ts"
        via: "Regression gate consumes versioned baseline snapshot and post-exclusion scope."
        pattern: "compareRegressionAgainstBaseline"
      - from: "yanote-js/src/cli.ts"
        to: "yanote-js/src/events/readJsonl.ts"
        via: "Invalid-line evidence integrity is promoted to fail-closed input-class outcome."
        pattern: "invalidLines"
---


<objective>
Implement deterministic governance gate evaluation, baseline regression contract, and fail-closed evidence integrity behavior.

Purpose: Deliver the core pass/fail policy engine and precedence logic required for CI-trustable governance outcomes.
Output: Versioned baseline v2 support, threshold/regression evaluator, deterministic failure ordering, and fail-closed contract tests.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-governance-gates/03-CONTEXT.md
@.planning/phases/03-governance-gates/03-RESEARCH.md
@.planning/phases/03-governance-gates/03-VALIDATION.md
@.planning/phases/03-governance-gates/03-01-PLAN.md
@yanote-js/src/cli.ts
@yanote-js/src/baseline/baseline.ts
@yanote-js/src/events/readJsonl.ts
@yanote-js/src/report/schema.ts

<interfaces>
From `03-01` outputs:
- Effective gate policy object and exclusion scope are available before evaluator invocation.

From `yanote-js/src/baseline/baseline.ts`:
- Existing baseline comparison supports covered-operation regression checks and is the migration target for versioned snapshots.

From `yanote-js/src/events/readJsonl.ts`:
- Reader already reports `invalidLines`; this plan promotes it to fail-closed policy behavior.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Upgrade baseline contract to versioned snapshot and explicit update workflow</name>
  <files>yanote-js/src/baseline/baseline.ts, yanote-js/src/baseline/baseline.v2.test.ts, yanote-js/src/cli.ts</files>
  <behavior>
    - Test: Baseline format version includes covered operations and dimension summary snapshot with strict compatibility checks.
    - Test: Incompatible baseline schema fails with actionable regeneration hint (input-class failure).
    - Test: Baseline updates occur only through explicit command-driven flow; report command never auto-overwrites baseline.
    - Test: Regression output remains sorted by canonical operation key.
  </behavior>
  <action>Extend baseline module with v2 read/write/compare support while preserving deterministic ordering and clear compatibility diagnostics. Add an explicit baseline update command path in CLI flow and lock behavior with focused baseline contract tests.</action>
  <verify>
    <automated>npm -C yanote-js run test -- src/baseline/baseline.v2.test.ts src/gates/evaluator.regression.test.ts</automated>
  </verify>
  <done>Versioned baseline snapshot behavior is deterministic, explicit-update-only, and regression-compare ready.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement threshold/regression evaluator and deterministic precedence sorter</name>
  <files>yanote-js/src/gates/evaluator.ts, yanote-js/src/gates/failureOrder.ts, yanote-js/src/gates/failureOrder.test.ts, yanote-js/src/gates/evaluator.threshold.test.ts, yanote-js/src/gates/evaluator.regression.test.ts</files>
  <behavior>
    - Test: Threshold comparisons use raw decimals, apply after exclusions, and skip aggregate gate when aggregate coverage is `N/A`.
    - Test: Critical-operation coverage loss is always emitted as hard gate failure (not warning) when critical coverage drops below policy.
    - Test: Regression gate hard-fails on lost covered operations and treats dimension regressions as warning-level in Phase 3.
    - Test: Global and gate-internal precedence ordering is deterministic and stable across repeated runs.
    - Test: `failureOrder` unit tests verify primary selection plus deterministic secondary ordering (`input > semantic > gate > runtime`, then gate-internal `regression > threshold`).
  </behavior>
  <action>Build pure evaluator modules for threshold and regression checks and a dedicated precedence sorter that enforces `input > semantic > gate > runtime` with gate-internal `regression > threshold`. Keep evaluator output structured for CLI/report rendering without presentation coupling.</action>
  <verify>
    <automated>npm -C yanote-js run test -- src/gates/evaluator.threshold.test.ts src/gates/evaluator.regression.test.ts src/gates/failureOrder.test.ts</automated>
  </verify>
  <done>Evaluator and precedence modules produce deterministic gate outcomes with passing unit tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Enforce fail-closed evidence integrity in CLI governance path</name>
  <files>yanote-js/src/cli.ts, yanote-js/src/cli.report.test.ts, yanote-js/src/cli.failclosed.contract.test.ts, yanote-js/src/events/readJsonl.ts, yanote-js/src/events/readJsonl.test.ts</files>
  <behavior>
    - Test: Invalid JSONL lines produce non-zero fail-closed input-class outcome with actionable diagnostics.
    - Test: Incompatible baseline schema cannot pass as warning; run exits non-zero with deterministic primary failure code.
    - Test: CLI report path exits non-zero with deterministic gate-class primary failure when evaluator reports critical-operation coverage loss.
    - Test: Multi-failure runs keep one primary `YANOTE_ERROR` line and deterministic secondary ordering contract.
  </behavior>
  <action>Wire evidence integrity checks into CLI report flow before final gate pass decision. Ensure fail-closed behavior integrates with precedence sorter and typed failure classes without breaking existing machine summary grammar.</action>
  <verify>
    <automated>npm -C yanote-js run test -- src/cli.failclosed.contract.test.ts src/cli.report.test.ts src/events/readJsonl.test.ts src/cli.test.ts</automated>
  </verify>
  <done>Fail-closed evidence integrity and deterministic precedence behavior are enforced end-to-end in CLI path.</done>
</task>

</tasks>

<verification>
- [ ] `npm -C yanote-js run test -- src/baseline/baseline.v2.test.ts src/gates/evaluator.threshold.test.ts src/gates/evaluator.regression.test.ts src/cli.failclosed.contract.test.ts` succeeds.
- [ ] Regression and threshold evaluator outputs are deterministic across repeated runs.
</verification>

<success_criteria>
- GATE-02 baseline regression fail logic is versioned, explicit-update-only, and contract-tested.
- GATE-04 fail-closed behavior is enforced for invalid/incompatible evidence with actionable diagnostics.
- Deterministic precedence engine is stable and ready for final CLI/report contract integration in Wave 3.
</success_criteria>

<output>
After completion, create `.planning/phases/03-governance-gates/03-02-SUMMARY.md`
</output>
