---
phase: 03-governance-gates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - yanote-js/package.json
  - yanote-js/package-lock.json
  - yanote-js/src/gates/policy.ts
  - yanote-js/src/gates/policy.schema.ts
  - yanote-js/src/gates/exclusions.ts
  - yanote-js/src/gates/policy.test.ts
  - yanote-js/src/gates/exclusions.test.ts
  - yanote-js/src/cli.ts
autonomous: true
requirements:
  - GATE-01
  - GATE-03
user_setup: []
must_haves:
    truths:
      - "Effective governance policy resolution is deterministic with precedence CLI > policy file > defaults."
      - "Default thresholds remain explicit and stable: min coverage 95% in CI and optional aggregate gate 85% with warning band at threshold minus 2%."
      - "Exclusion rules are auditable records requiring rationale, owner, and expiry/review date; unmatched rules produce deterministic warning diagnostics."
      - "Critical operations are protected from exclusion unless an explicit per-rule override is present."
    artifacts:
      - path: "yanote-js/src/gates/policy.ts"
        provides: "Policy DTOs, default profiles, and deterministic merge/resolution entrypoint."
        exports: ["resolveGatePolicy", "GatePolicy", "CI_DEFAULT_POLICY", "LOCAL_DEFAULT_POLICY"]
      - path: "yanote-js/src/gates/policy.schema.ts"
        provides: "Strict schema validator for policy YAML input."
        exports: ["GATE_POLICY_SCHEMA", "validateGatePolicyConfig"]
      - path: "yanote-js/src/gates/exclusions.ts"
        provides: "Compiled exclusion rule matching with wildcard guardrails and stale-rule diagnostics."
        exports: ["compileExclusionRules", "applyExclusionRules"]
      - path: "yanote-js/src/cli.ts"
        provides: "CLI options for policy path/profile and deterministic override plumbing."
        contains: "--policy|--profile|--exclude"
    key_links:
      - from: "yanote-js/src/cli.ts"
        to: "yanote-js/src/gates/policy.ts"
        via: "CLI resolves one effective policy before coverage and gate evaluation."
        pattern: "resolveGatePolicy"
      - from: "yanote-js/src/gates/policy.ts"
        to: "yanote-js/src/gates/policy.schema.ts"
        via: "Policy-file parse path validates shape before merge."
        pattern: "validateGatePolicyConfig"
      - from: "yanote-js/src/gates/exclusions.ts"
        to: "yanote-js/src/coverage/coverage.ts"
        via: "Resolved exclusions apply before threshold/regression scope is evaluated."
        pattern: "exclude|operationKey|pattern"
---


<objective>
Establish deterministic governance policy inputs and exclusion policy mechanics for Phase 3 gate enforcement.

Purpose: Deliver policy resolution, exclusion auditability, and CLI policy plumbing as the contract foundation for downstream regression/threshold evaluation.
Output: Gate policy schema + resolver, auditable exclusion engine, and tested CLI option surface for policy precedence.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-governance-gates/03-CONTEXT.md
@.planning/phases/03-governance-gates/03-RESEARCH.md
@.planning/phases/03-governance-gates/03-VALIDATION.md
@yanote-js/src/cli.ts
@yanote-js/src/coverage/coverage.ts
@yanote-js/src/report/report.ts

<interfaces>
From `yanote-js/src/cli.ts`:
- Existing `report` command already supports typed failure classes and stable `YANOTE_SUMMARY` line contract.

From `yanote-js/src/coverage/coverage.ts`:
- Existing exclusion support is string-based; this plan upgrades to auditable policy records with deterministic diagnostics.

From `yanote-js/src/report/report.ts`:
- Report diagnostics block already exists and can carry stale/unmatched exclusion warnings without breaking deterministic order.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement gate policy schema and deterministic precedence resolver</name>
  <files>yanote-js/package.json, yanote-js/package-lock.json, yanote-js/src/gates/policy.ts, yanote-js/src/gates/policy.schema.ts, yanote-js/src/gates/policy.test.ts</files>
  <behavior>
    - Test: Policy defaults are explicit and stable (`ci`: min coverage 95%; aggregate gate default 85% when enabled; warning band threshold minus 2%).
    - Test: Effective policy resolution precedence is CLI overrides, then policy file, then defaults.
    - Test: Invalid policy file shape fails with actionable input-class diagnostics.
  </behavior>
  <action>Add `yaml` dependency and introduce a dedicated gate policy module + schema validator. Implement deterministic merge behavior for scalar values, list appends, and explicit override flags. Keep resolver pure and side-effect free so downstream evaluator tests can consume one normalized effective policy object.</action>
  <verify>
    <automated>npm -C yanote-js run test -- src/gates/policy.test.ts</automated>
  </verify>
  <done>Policy parser/resolver enforces locked precedence and default threshold policy with passing tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build auditable exclusion rule engine with wildcard guardrails</name>
  <files>yanote-js/src/gates/exclusions.ts, yanote-js/src/gates/exclusions.test.ts, yanote-js/src/coverage/coverage.ts</files>
  <behavior>
    - Test: Exclusion rules require `pattern`, `rationale`, `owner`, and `expiresOn` (or review date alias) and fail validation when missing.
    - Test: Broad wildcard rules require explicit override; otherwise they are rejected deterministically.
    - Test: Unmatched exclusion rules are surfaced as warning diagnostics, while applied exclusions are grouped with rationale and matched-operation counts.
    - Test: Critical operations cannot be excluded unless explicit override is declared for that rule.
  </behavior>
  <action>Create exclusion compile/apply helpers that reuse existing prefix+`*` semantics while adding metadata and guardrails. Preserve deterministic ordering of applied and unmatched rule outputs by rule declaration order then canonical operation key.</action>
  <verify>
    <automated>npm -C yanote-js run test -- src/gates/exclusions.test.ts src/coverage/coverage.test.ts</automated>
  </verify>
  <done>Exclusion policy behavior is deterministic, auditable, and aligned with locked decisions on wildcard restrictions and critical-operation protection.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Plumb policy file and profile overrides into CLI report command</name>
  <files>yanote-js/src/cli.ts, yanote-js/src/cli.test.ts, yanote-js/src/gates/policy.test.ts</files>
  <behavior>
    - Test: CLI exposes policy path and profile controls without breaking existing report invocation contract.
    - Test: CLI-level overrides win against policy file values, and resulting effective thresholds are reflected in gate evaluation inputs.
    - Test: Missing policy file path or parse failure emits deterministic input-class diagnostics.
  </behavior>
  <action>Integrate policy resolver into `yanote report` command flow and produce one normalized policy object before coverage/regression stages. Keep output wording deterministic and avoid introducing governance evaluation logic in this plan.</action>
  <verify>
    <automated>npm -C yanote-js run test -- src/cli.test.ts src/gates/policy.test.ts</automated>
  </verify>
  <done>CLI can resolve governance policy from defaults/file/overrides deterministically with passing contract tests.</done>
</task>

</tasks>

<verification>
- [ ] `npm -C yanote-js run test -- src/gates/policy.test.ts src/gates/exclusions.test.ts src/cli.test.ts` succeeds.
- [ ] Exclusion diagnostics ordering is deterministic across repeated runs.
</verification>

<success_criteria>
- GATE-01 policy configuration prerequisites (threshold defaults + precedence) are implemented and tested.
- GATE-03 auditable exclusion model (metadata + grouped transparency + stale rule warnings) is implemented and tested.
- Foundation policy modules are ready for downstream regression/fail-closed evaluator integration without CLI contract drift.
</success_criteria>

<output>
After completion, create `.planning/phases/03-governance-gates/03-01-SUMMARY.md`
</output>
