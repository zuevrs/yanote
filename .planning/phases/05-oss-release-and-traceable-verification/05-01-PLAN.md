---
phase: 05-oss-release-and-traceable-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - build.gradle.kts
  - gradle.properties
  - yanote-core/build.gradle.kts
  - yanote-recorder-spring-mvc/build.gradle.kts
  - yanote-test-tags-restassured/build.gradle.kts
  - yanote-test-tags-cucumber/build.gradle.kts
  - yanote-gradle-plugin/build.gradle.kts
  - jreleaser.yml
  - scripts/release/preflight.sh
  - scripts/release/maven-central-preflight.contract.test.mjs
  - scripts/release/release-failclosed.contract.test.mjs
autonomous: true
requirements:
  - RELS-01
  - RELS-03
user_setup:
  - service: sonatype-central
    why: "Authenticate Maven Central publication from CI."
    env_vars:
      - name: JRELEASER_MAVENCENTRAL_USERNAME
        source: "Sonatype Central Portal -> Account -> User Token"
      - name: JRELEASER_MAVENCENTRAL_PASSWORD
        source: "Sonatype Central Portal -> Account -> User Token"
  - service: gpg
    why: "Use one consistent signing identity for all v1 release artifacts."
    env_vars:
      - name: JRELEASER_GPG_SECRET_KEY
        source: "Release owner managed armored private key exported to GitHub secret"
      - name: JRELEASER_GPG_PUBLIC_KEY
        source: "Release owner managed armored public key matching the private key"
      - name: JRELEASER_GPG_PASSPHRASE
        source: "Passphrase for the release signing identity"
must_haves:
  truths:
    - "Maintainer can publish only scoped v1 Java modules as signed Maven artifacts with sources, javadocs, and Central-ready metadata."
    - "Release publication fails before upload when semver tag, signing, or publishing prerequisites are invalid or missing."
    - "Pre-release and snapshot versions are blocked from v1 Maven Central publication."
    - "Preflight output records deterministic retry eligibility for transient external publish failures under the same tag."
  artifacts:
    - path: "scripts/release/preflight.sh"
      provides: "Deterministic fail-closed preflight for signed semver tag, main ancestry, release freeze, and publish prerequisites."
      contains: "git verify-tag|v[0-9]+\\.[0-9]+\\.[0-9]+|SNAPSHOT|retry"
    - path: "jreleaser.yml"
      provides: "Central Portal deployment and signing contract for release artifacts."
      contains: "mavenCentral|signing|applyMavenCentralRules"
    - path: "build.gradle.kts"
      provides: "Root publication allowlist and exclusion rules for release-scope modules."
      contains: "yanote-core|yanote-recorder-spring-mvc|yanote-test-tags-restassured|yanote-test-tags-cucumber|yanote-gradle-plugin|examples"
    - path: "yanote-gradle-plugin/build.gradle.kts"
      provides: "Gradle plugin artifact publication contract on Maven Central-first path."
      contains: "java-gradle-plugin|maven-publish|withSourcesJar|withJavadocJar"
  key_links:
    - from: "scripts/release/preflight.sh"
      to: "jreleaser.yml"
      via: "Preflight validates required Central/signing inputs before deployment orchestration executes."
      pattern: "JRELEASER_|mavenCentral"
    - from: "build.gradle.kts"
      to: "yanote-core/build.gradle.kts"
      via: "Root release conventions are applied only to allowlisted publication modules."
      pattern: "yanote-core|withSourcesJar|withJavadocJar"
    - from: "build.gradle.kts"
      to: "examples/springmvc-service/build.gradle.kts"
      via: "Examples remain excluded from Central publication scope."
      pattern: "examples"
---

<objective>
Establish the Maven Central release foundation for v1 with deterministic fail-closed preflight behavior.

Purpose: Satisfy RELS-01 and the publication-precondition portion of RELS-03 by making release publication deterministic, signed, scope-controlled, and auditable.
Output: Publication/signing config, release preflight script, and contract tests that lock fail-closed release behavior.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-oss-release-and-traceable-verification/05-CONTEXT.md
@.planning/phases/05-oss-release-and-traceable-verification/05-RESEARCH.md
@.planning/phases/05-oss-release-and-traceable-verification/05-VALIDATION.md
@.planning/phases/04-java-build-and-ci-delivery-surfaces/04-04-SUMMARY.md
@build.gradle.kts
@gradle.properties
@yanote-core/build.gradle.kts
@yanote-recorder-spring-mvc/build.gradle.kts
@yanote-test-tags-restassured/build.gradle.kts
@yanote-test-tags-cucumber/build.gradle.kts
@yanote-gradle-plugin/build.gradle.kts

<interfaces>
From `build.gradle.kts`:
- All subprojects currently receive `java-library` + `maven-publish`, but there is no release-scope allowlist or signing contract yet.
- Group/version currently come from root project (`dev.yanote`, `0.1.0-SNAPSHOT`) and must be made release-safe for stable tags.

From locked Phase 5 decisions:
- Release tags are stable semver only (`vMAJOR.MINOR.PATCH`), signed, and created from `main`.
- Central publication includes only v1 product modules and excludes `examples/*`.
- Every published artifact must include signatures, sources, javadocs, and Central-ready POM metadata.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock preflight and fail-closed release contracts with executable tests</name>
  <files>scripts/release/maven-central-preflight.contract.test.mjs, scripts/release/release-failclosed.contract.test.mjs</files>
  <behavior>
    - Test: preflight rejects non-stable tags (non-`vMAJOR.MINOR.PATCH`) and any `-beta`/`-rc` prerelease identifiers.
    - Test: preflight rejects unsigned tags and tags not reachable from `main`.
    - Test: preflight rejects snapshot publication (`*-SNAPSHOT`) and missing Central/signing credentials.
    - Test: fail-closed diagnostics render deterministic class ordering and include retry-eligibility output for transient publish failures.
  </behavior>
  <action>Create RED contract tests for release preflight behavior before implementing release scripts/config. Keep assertions scoped to locked phase decisions and deterministic diagnostics; do not include Plugin Portal or non-GitHub channels (deferred scope).</action>
  <verify>
    <automated>node --test scripts/release/maven-central-preflight.contract.test.mjs scripts/release/release-failclosed.contract.test.mjs</automated>
  </verify>
  <done>Contract tests permanently encode signed-tag, stable-semver, main-lineage, and fail-closed prerequisite behavior for release publication.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement root release preflight and Central deploy orchestration contract</name>
  <files>scripts/release/preflight.sh, build.gradle.kts, gradle.properties, jreleaser.yml</files>
  <behavior>
    - Test: `preflight.sh` enforces signed stable tags, main ancestry, no prerelease tags, and no snapshot publication.
    - Test: release freeze and deterministic retry policy are represented as explicit checks/log fields (auditable in CI output).
    - Test: `jreleaser.yml` is configured for Maven Central-first publishing with signing enabled and deterministic checksum/signature behavior.
  </behavior>
  <action>Implement preflight gate and root-level release wiring. Add explicit guardrails for release start version (`v1.0.0` and above), semver classification policy hooks, hotfix patch-policy checks, and deterministic retry logging conditions for external publish outages. Configure JReleaser for Central Portal deployment and signing so publish orchestration uses ecosystem tooling instead of hand-rolled upload logic.</action>
  <verify>
    <automated>bash -n scripts/release/preflight.sh && node --test scripts/release/maven-central-preflight.contract.test.mjs scripts/release/release-failclosed.contract.test.mjs && ./gradlew jreleaserConfig</automated>
  </verify>
  <done>Repository has deterministic preflight + Central deploy orchestration that fails closed before any publish attempt when locked prerequisites are not met.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Apply signed publication metadata and v1 module allowlist across Java artifacts</name>
  <files>yanote-core/build.gradle.kts, yanote-recorder-spring-mvc/build.gradle.kts, yanote-test-tags-restassured/build.gradle.kts, yanote-test-tags-cucumber/build.gradle.kts, yanote-gradle-plugin/build.gradle.kts</files>
  <behavior>
    - Test: each v1 publishable module emits signed main artifact + sources jar + javadocs jar with Central-ready POM metadata.
    - Test: group/artifact coordinates remain stable and deterministic (no arbitrary coordinate drift).
    - Test: `examples/*` modules are excluded from public publication scope.
  </behavior>
  <action>Update module publishing metadata to satisfy Maven Central OSS contract, including signatures, sources/javadocs, and required POM fields. Keep coordinates stable, preserve existing module names, and explicitly prevent `examples/*` publication surfaces from entering release outputs.</action>
  <verify>
    <automated>./gradlew :yanote-core:publishToMavenLocal :yanote-recorder-spring-mvc:publishToMavenLocal :yanote-test-tags-restassured:publishToMavenLocal :yanote-test-tags-cucumber:publishToMavenLocal :yanote-gradle-plugin:publishToMavenLocal</automated>
  </verify>
  <done>All v1 publishable modules satisfy signed-publication contract and example/demo modules remain excluded from release scope.</done>
</task>

</tasks>

<verification>
- [ ] `node --test scripts/release/maven-central-preflight.contract.test.mjs scripts/release/release-failclosed.contract.test.mjs` passes.
- [ ] `./gradlew jreleaserConfig` succeeds with release config resolved.
- [ ] `./gradlew :yanote-core:publishToMavenLocal :yanote-recorder-spring-mvc:publishToMavenLocal :yanote-test-tags-restassured:publishToMavenLocal :yanote-test-tags-cucumber:publishToMavenLocal :yanote-gradle-plugin:publishToMavenLocal` succeeds.
</verification>

<success_criteria>
- RELS-01 publication foundations are in place for signed Maven Central-ready Java artifacts scoped to v1 modules.
- RELS-03 pre-publish behavior is fail-closed and deterministic for invalid tags, missing prerequisites, and snapshot/prerelease attempts.
- Release metadata/signing contract is machine-validated and reusable by release workflow plans.
</success_criteria>

<output>
After completion, create `.planning/phases/05-oss-release-and-traceable-verification/05-01-SUMMARY.md`
</output>
