---
phase: 05-oss-release-and-traceable-verification
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - .github/workflows/release.yml
  - .github/release.yml
  - scripts/release/assemble-release-assets.sh
  - scripts/release/render-release-notes.mjs
  - scripts/release/github-release.contract.test.mjs
  - scripts/release/release-workflow.contract.test.mjs
autonomous: true
requirements:
  - RELS-02
  - RELS-03
user_setup:
  - service: github
    why: "Enforce one explicit manual approval gate before publication."
    dashboard_config:
      - task: "Create protected environment `production-release` with required reviewer(s) limited to designated release owner(s)."
        location: "Repository Settings -> Environments"
      - task: "Allow release workflow to use environment-scoped release secrets only after approval."
        location: "Repository Settings -> Environments -> production-release -> Environment secrets"
must_haves:
  truths:
    - "Tagged stable versions trigger a deterministic release pipeline and no manual-only entrypoint can bypass tag policy."
    - "Release publish job pauses for one explicit manual approval before any external publication."
    - "GitHub Release output contains required sections (Summary, Breaking Changes, Upgrade Notes, Verification Highlights) and changelog scope since previous release tag."
    - "Release asset bundle is deterministic and includes v1 distribution artifacts, SBOM, SHA-256 checksums, per-asset signatures/checksum proofs, and shared manifest."
  artifacts:
    - path: ".github/workflows/release.yml"
      provides: "Tag-only release workflow with preflight, approval-gated publish, and deterministic release execution path."
      contains: "push:|tags:|v[0-9]+\\.[0-9]+\\.[0-9]+|environment: production-release"
    - path: ".github/release.yml"
      provides: "Deterministic release note categorization and changelog policy input."
      contains: "changelog|categories"
    - path: "scripts/release/render-release-notes.mjs"
      provides: "Template-driven release notes renderer enforcing required section structure."
      contains: "Summary|Breaking Changes|Upgrade Notes|Verification Highlights"
    - path: "scripts/release/assemble-release-assets.sh"
      provides: "Deterministic release asset assembly with strict naming and SHA-256 manifest generation."
      contains: "sha256|manifest|sbom"
  key_links:
    - from: ".github/workflows/release.yml"
      to: "scripts/release/preflight.sh"
      via: "Workflow preflight job executes fail-closed tag/signing/version checks before approval/publish."
      pattern: "preflight\\.sh"
    - from: ".github/workflows/release.yml"
      to: "scripts/release/assemble-release-assets.sh"
      via: "Publish stage packages deterministic release assets from build outputs."
      pattern: "assemble-release-assets\\.sh"
    - from: ".github/workflows/release.yml"
      to: "scripts/release/render-release-notes.mjs"
      via: "Release notes are generated from fixed template with required sections."
      pattern: "render-release-notes\\.mjs"
---

<objective>
Deliver the public GitHub release surface for v1 with deterministic tag-driven execution and approval-gated publication.

Purpose: Satisfy RELS-02 and workflow/orchestration portions of RELS-03 by converting release from ad hoc CI activity into an explicit OSS release contract.
Output: Release workflow, release notes template renderer, and deterministic release asset assembly with contract tests.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-oss-release-and-traceable-verification/05-CONTEXT.md
@.planning/phases/05-oss-release-and-traceable-verification/05-RESEARCH.md
@.planning/phases/05-oss-release-and-traceable-verification/05-VALIDATION.md
@.planning/phases/05-oss-release-and-traceable-verification/05-01-PLAN.md
@.github/workflows/yanote-ci.yml
@scripts/ci/yanote-ci-workflow.contract.test.mjs
@scripts/release/preflight.sh
@jreleaser.yml

<interfaces>
From `05-01` outputs:
- `scripts/release/preflight.sh` is the gatekeeper for signed semver tag, main ancestry, stable-version policy, and publish prerequisites.
- `jreleaser.yml` defines Central/GitHub release orchestration inputs and should be invoked, not replaced with bespoke upload logic.

From existing CI contracts:
- Workflow behavior is guarded with Node contract tests (`*.contract.test.mjs`) and this release workflow should follow the same pattern.
- Deterministic artifact/log naming and fail-closed diagnostics are established conventions and should be preserved.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create RED contract tests for tag-only release workflow and release bundle semantics</name>
  <files>scripts/release/github-release.contract.test.mjs, scripts/release/release-workflow.contract.test.mjs</files>
  <behavior>
    - Test: workflow triggers only on `push.tags` matching stable `vMAJOR.MINOR.PATCH`; no `workflow_dispatch` release entrypoint.
    - Test: workflow executes preflight before publish and blocks on explicit environment approval gate.
    - Test: release notes and bundle contracts require Summary, Breaking Changes, Upgrade Notes, Verification Highlights, and deterministic asset naming.
    - Test: release bundle contract requires SBOM, SHA-256 checksums, per-asset proof files, and shared manifest.
  </behavior>
  <action>Add failing contract tests that codify locked release decisions before implementing workflow/scripts. Keep scope strictly v1 stable releases; do not add prerelease channels (`-beta`/`-rc`) or non-GitHub release transports (deferred scope).</action>
  <verify>
    <automated>node --test scripts/release/github-release.contract.test.mjs scripts/release/release-workflow.contract.test.mjs</automated>
  </verify>
  <done>Release workflow and bundle contracts are executable and fail against current repository state until implementation lands.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement deterministic release notes and release bundle assembly scripts</name>
  <files>.github/release.yml, scripts/release/render-release-notes.mjs, scripts/release/assemble-release-assets.sh</files>
  <behavior>
    - Test: notes renderer always outputs required sections in English-first template structure with changelog scope from previous tag.
    - Test: asset assembly script emits deterministic names (`{version}-{artifact-type}`), stable ordering, and SHA-256 manifest.
    - Test: release bundle includes v1 distribution artifacts, SBOM, and checksum/signature proof files.
  </behavior>
  <action>Create deterministic release content tooling driven by fixed templates, not freeform text. Keep naming and ordering stable across reruns for the same tag. Ensure generated assets are release-consumer verifiable (checksums/signatures/manifest) and compatible with downstream GitHub release upload.</action>
  <verify>
    <automated>bash -n scripts/release/assemble-release-assets.sh && node --test scripts/release/github-release.contract.test.mjs</automated>
  </verify>
  <done>Release notes and asset assembly surfaces are deterministic, contract-tested, and satisfy locked release bundle requirements.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement approval-gated release workflow from stable tags</name>
  <files>.github/workflows/release.yml</files>
  <behavior>
    - Test: workflow runs only on stable tag pushes and rejects unsigned/non-main tags through preflight.
    - Test: publish job requires one explicit manual approval (`environment: production-release`) before publication.
    - Test: publish path executes deterministic build/publish sequence (`distAll`, SBOM generation, asset assembly, release notes, JReleaser release).
    - Test: external publish retries are constrained to deterministic same-tag retry conditions with explicit logs.
  </behavior>
  <action>Implement a two-stage release workflow: preflight gate then approval-gated publish. Preserve least-privilege job permissions and fail-closed semantics. Keep release execution reproducible from tag input and ensure logs capture retry decisions for publish platform outages. Use preflight and release tooling from this phase instead of introducing ad hoc shell upload logic.</action>
  <verify>
    <automated>node --test scripts/release/release-workflow.contract.test.mjs scripts/release/github-release.contract.test.mjs</automated>
  </verify>
  <done>Release workflow is deterministic, approval-gated, and produces GitHub release outputs that satisfy RELS-02/RELS-03 contracts.</done>
</task>

</tasks>

<verification>
- [ ] `node --test scripts/release/release-workflow.contract.test.mjs scripts/release/github-release.contract.test.mjs` passes.
- [ ] `bash -n scripts/release/assemble-release-assets.sh` passes.
- [ ] Release workflow contract confirms tag-only trigger + approval gate + deterministic bundle wiring.
</verification>

<success_criteria>
- RELS-02 is satisfied with deterministic GitHub release generation (structured notes + stable assets).
- RELS-03 is satisfied for release workflow reproducibility, explicit approval gate, and fail-closed pre-publication execution.
- Deferred scope remains deferred (no prerelease channels, no non-GitHub release lane, no Plugin Portal co-equal policy).
</success_criteria>

<output>
After completion, create `.planning/phases/05-oss-release-and-traceable-verification/05-02-SUMMARY.md`
</output>
