---
phase: 05-oss-release-and-traceable-verification
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - .planning/traceability/schema.v1.json
  - .planning/traceability/v1-requirements-tests.json
  - .planning/traceability/v1-requirements-tests.md
  - scripts/release/verify-traceability.mjs
  - scripts/release/traceability.contract.test.mjs
  - .github/workflows/release.yml
  - scripts/release/assemble-release-assets.sh
autonomous: true
requirements:
  - QUAL-01
user_setup:
  - service: github
    why: "Assign accountable release owner sign-off to protected release environment approvals."
    dashboard_config:
      - task: "Ensure `production-release` required reviewer list reflects designated release owner role for final sign-off."
        location: "Repository Settings -> Environments -> production-release"
must_haves:
  truths:
    - "Every v1 requirement has explicit automated test traceability entries with runnable verification commands."
    - "Release fails before publish when traceability coverage is below 100% or when mapped tests are flaky/quarantined."
    - "Traceability output is deterministic, schema-versioned JSON plus concise markdown summary using the same snapshot reference."
    - "Traceability artifacts are published both in-repo and inside the release asset bundle."
  artifacts:
    - path: ".planning/traceability/schema.v1.json"
      provides: "Versioned machine schema for requirement-to-test mapping artifact."
      contains: "schemaVersion|requirements|tests|commands"
    - path: ".planning/traceability/v1-requirements-tests.json"
      provides: "Deterministic requirement-to-test mapping snapshot for all v1 requirement IDs."
      contains: "SPEC-01|COVR-01|GATE-01|DELV-01|RELS-01|QUAL-01"
    - path: "scripts/release/verify-traceability.mjs"
      provides: "Fail-closed validator enforcing 100% requirement accounting and flaky-test exclusion."
      contains: "REQUIREMENTS.md|100|flaky|quarantined"
    - path: ".github/workflows/release.yml"
      provides: "Pre-publish traceability gate integrated into release pipeline."
      contains: "verify-traceability"
  key_links:
    - from: "scripts/release/verify-traceability.mjs"
      to: ".planning/REQUIREMENTS.md"
      via: "Validator reads canonical requirement inventory and checks full mapping coverage."
      pattern: "REQUIREMENTS\\.md"
    - from: ".github/workflows/release.yml"
      to: "scripts/release/verify-traceability.mjs"
      via: "Publish path blocks unless traceability validator reports 100% coverage."
      pattern: "verify-traceability\\.mjs"
    - from: "scripts/release/assemble-release-assets.sh"
      to: ".planning/traceability/v1-requirements-tests.json"
      via: "Traceability artifacts are included in deterministic release bundle."
      pattern: "v1-requirements-tests\\.json|v1-requirements-tests\\.md"
---

<objective>
Close QUAL-01 by turning requirement accountability into a deterministic release gate and publishable artifact set.

Purpose: Guarantee that every v1 requirement is traceable to executable automated tests before any public release can be published.
Output: Versioned traceability schema/map artifacts, validator script, contract tests, and release-workflow integration.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-oss-release-and-traceable-verification/05-CONTEXT.md
@.planning/phases/05-oss-release-and-traceable-verification/05-RESEARCH.md
@.planning/phases/05-oss-release-and-traceable-verification/05-VALIDATION.md
@.planning/phases/05-oss-release-and-traceable-verification/05-02-PLAN.md
@.github/workflows/release.yml
@scripts/release/assemble-release-assets.sh

<interfaces>
From `.planning/REQUIREMENTS.md`:
- Canonical v1 requirement inventory is already defined and must be the only source of requirement IDs for traceability validation.
- QUAL-01 requires strict 100% requirement coverage accountability.

From `05-VALIDATION.md`:
- Expected traceability gate command is `node scripts/release/verify-traceability.mjs --requirements .planning/REQUIREMENTS.md --map .planning/traceability/v1-requirements-tests.json`.
- Traceability artifacts must exist before release publication proceeds.

From `05-02` release workflow:
- Preflight and publish stages exist; this plan adds mandatory traceability gate before publication and bundle inclusion.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED contract tests for strict requirement traceability validation</name>
  <files>scripts/release/traceability.contract.test.mjs</files>
  <behavior>
    - Test: every v1 requirement ID from `.planning/REQUIREMENTS.md` must map to at least one concrete test case reference and automated command.
    - Test: mapping fails when a requirement entry is missing, duplicated incorrectly, or references flaky/quarantined tests.
    - Test: output contract requires schema version field and deterministic ordering for stable snapshots.
    - Test: validator exits non-zero when aggregate traceability coverage is below 100%.
  </behavior>
  <action>Create failing contract tests that encode strict QUAL-01 release behavior before implementing the validator/map artifacts. Keep assertions tied to requirement IDs and executable commands, not heuristic filename inference.</action>
  <verify>
    <automated>node --test scripts/release/traceability.contract.test.mjs</automated>
  </verify>
  <done>Executable tests lock strict 100% requirement-accountability semantics and fail against missing/incomplete traceability.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement schema-versioned traceability artifacts and fail-closed validator</name>
  <files>.planning/traceability/schema.v1.json, .planning/traceability/v1-requirements-tests.json, .planning/traceability/v1-requirements-tests.md, scripts/release/verify-traceability.mjs</files>
  <behavior>
    - Test: validator loads canonical requirements list and confirms complete mapping coverage (100%) with no waiver path.
    - Test: JSON map conforms to explicit schema version and deterministic key/array ordering.
    - Test: markdown summary and JSON map share identical snapshot reference/id for publication traceability.
    - Test: flaky/quarantined test entries are rejected and do not count toward requirement coverage.
  </behavior>
  <action>Define the v1 traceability schema, generate the canonical requirement map artifact, and implement a deterministic validator that fails closed for any unmapped requirement or invalid entry. Ensure each requirement entry contains concrete test references and runnable command selectors. Keep artifact structure stable to support release consumers and automated verification.</action>
  <verify>
    <automated>node scripts/release/verify-traceability.mjs --requirements .planning/REQUIREMENTS.md --map .planning/traceability/v1-requirements-tests.json --schema .planning/traceability/schema.v1.json && node --test scripts/release/traceability.contract.test.mjs</automated>
  </verify>
  <done>Traceability artifacts are schema-versioned, deterministic, and validator-enforced at strict 100% requirement coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Integrate traceability as a hard pre-publish release gate and bundled asset</name>
  <files>.github/workflows/release.yml, scripts/release/assemble-release-assets.sh</files>
  <behavior>
    - Test: release workflow runs traceability validator before publish and fails closed on any coverage/accountability violation.
    - Test: release bundle includes both traceability JSON and markdown summary with matching snapshot reference.
    - Test: final publish path records accountable release-owner sign-off context in release logs/summary metadata.
  </behavior>
  <action>Wire traceability validation into the release pipeline as a required pre-publish gate and include traceability artifacts in deterministic asset assembly. Keep gate placement before any Central/GitHub publication step. Ensure release evidence explicitly captures accountable release-owner sign-off context from approval-gated workflow execution.</action>
  <verify>
    <automated>node --test scripts/release/traceability.contract.test.mjs scripts/release/release-workflow.contract.test.mjs && node scripts/release/verify-traceability.mjs --requirements .planning/REQUIREMENTS.md --map .planning/traceability/v1-requirements-tests.json --schema .planning/traceability/schema.v1.json</automated>
  </verify>
  <done>QUAL-01 is enforced as a deterministic release gate and published artifact contract with explicit accountability evidence.</done>
</task>

</tasks>

<verification>
- [ ] `node --test scripts/release/traceability.contract.test.mjs` passes.
- [ ] `node scripts/release/verify-traceability.mjs --requirements .planning/REQUIREMENTS.md --map .planning/traceability/v1-requirements-tests.json --schema .planning/traceability/schema.v1.json` exits 0.
- [ ] `node --test scripts/release/release-workflow.contract.test.mjs` still passes after traceability gate wiring.
</verification>

<success_criteria>
- QUAL-01 is satisfied with 100% requirement traceability enforced automatically before release publication.
- Traceability artifacts are deterministic, schema-versioned, and published both in repo and release bundle with shared snapshot identity.
- Release pipeline remains fail-closed: no publish path exists when requirement accountability is incomplete or invalid.
</success_criteria>

<output>
After completion, create `.planning/phases/05-oss-release-and-traceable-verification/05-03-SUMMARY.md`
</output>
