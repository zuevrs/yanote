---
phase: 05-oss-release-and-traceable-verification
plan: 04
type: execute
wave: 4
depends_on: ["05-03"]
files_modified:
  - .github/workflows/release.yml
  - scripts/release/release-workflow.contract.test.mjs
autonomous: true
requirements:
  - RELS-02
  - RELS-03
gap_closure: true
user_setup:
  - service: github
    why: "Environment required reviewers are configured in GitHub settings, not in repository files."
    dashboard_config:
      - task: "Ensure `production-release` has required reviewer(s) configured for explicit publish approval."
        location: "Repository Settings -> Environments -> production-release -> Required reviewers"
      - task: "Run a semver tag dry run and confirm `Publish` waits for approval before external publication steps."
        location: "Actions -> release workflow run for test tag"
must_haves:
  truths:
    - "Release workflow tag trigger uses GitHub glob semantics and allows stable release tag entry while strict semver remains enforced in preflight."
    - "Release notes changelog scope is wired to resolved previous release tag output, not `${{ github.event.before }}`."
    - "Release workflow contract tests fail if tag trigger syntax regresses to regex-like patterns or previous-tag wiring regresses."
    - "Manual approval remains explicit human verification through `production-release` required reviewers; no fake automation is introduced."
  artifacts:
    - path: ".github/workflows/release.yml"
      provides: "Gap-fixed tag trigger and previous-tag wiring for deterministic release notes scope."
      contains: "push:|tags:|v*.*.*|steps.previous-tag.outputs.previous_release_tag|--previous-tag"
    - path: "scripts/release/release-workflow.contract.test.mjs"
      provides: "Contract tests locking glob-compatible tag trigger and previous-tag output wiring."
      contains: "v\\*\\.\\*\\.\\*|previous_release_tag|github.event.before"
  key_links:
    - from: ".github/workflows/release.yml"
      to: "scripts/release/preflight.sh"
      via: "Workflow tag filter remains GitHub-glob compatible while preflight enforces strict semver and signed-tag policy."
      pattern: "tags:|v\\*\\.\\*\\.\\*|Run release preflight|preflight\\.sh"
    - from: ".github/workflows/release.yml"
      to: "scripts/release/render-release-notes.mjs"
      via: "Resolved previous-tag output is passed to release-notes rendering to scope changelog correctly."
      pattern: "previous_release_tag|render-release-notes\\.mjs|--previous-tag"
    - from: "scripts/release/release-workflow.contract.test.mjs"
      to: ".github/workflows/release.yml"
      via: "Contract tests assert gap-closure invariants for trigger syntax and previous-tag wiring."
      pattern: "stable version tags|previous-tag|github.event.before"
---

<objective>
Close the Phase 05 verification gaps in release workflow trigger semantics and release-notes previous-tag wiring.

Purpose: Satisfy the remaining RELS-02/RELS-03 defects by fixing exactly two workflow wiring issues without expanding release scope.
Output: One focused gap-closure patch plus contract-test guards that prevent regression.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-oss-release-and-traceable-verification/05-CONTEXT.md
@.planning/phases/05-oss-release-and-traceable-verification/05-RESEARCH.md
@.planning/phases/05-oss-release-and-traceable-verification/05-VALIDATION.md
@.planning/phases/05-oss-release-and-traceable-verification/05-VERIFICATION.md
@.planning/phases/05-oss-release-and-traceable-verification/05-03-SUMMARY.md
@.github/workflows/release.yml
@scripts/release/release-workflow.contract.test.mjs
@scripts/release/render-release-notes.mjs

<interfaces>
From `05-VERIFICATION.md` gaps:
- Workflow tag trigger currently uses regex-like syntax (`v[0-9]+\.[0-9]+\.[0-9]+`) that is invalid under GitHub tag-filter glob semantics.
- Release notes wiring currently passes `${{ github.event.before }}` to `--previous-tag` instead of the resolved previous release tag output.

From existing release contracts:
- `scripts/release/preflight.sh` already enforces strict stable-semver and signed-tag checks, so trigger correction must not weaken those guarantees.
- `scripts/release/release-workflow.contract.test.mjs` is the correct place to lock these workflow invariants.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock both verification gaps as RED contract tests</name>
  <files>scripts/release/release-workflow.contract.test.mjs</files>
  <behavior>
    - Test: release workflow trigger asserts a GitHub-compatible glob tag pattern for stable release tags and rejects regex-like trigger syntax.
    - Test: release-notes step wires `--previous-tag` from `steps.previous-tag.outputs.previous_release_tag`.
    - Test: contract explicitly fails if `${{ github.event.before }}` is used for release-notes previous-tag scope.
    - Test: preflight-before-publish and `environment: production-release` assertions remain intact.
  </behavior>
  <action>Update workflow contract tests first so they fail against current gap state and become permanent guards. Keep assertions narrow to trigger syntax and previous-tag wiring; do not broaden coverage to unrelated release behaviors.</action>
  <verify>
    <automated>node --test scripts/release/release-workflow.contract.test.mjs</automated>
  </verify>
  <done>Contract tests fail on the current two defects and codify the exact gap-closure target conditions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix release workflow tag trigger and previous-tag wiring</name>
  <files>.github/workflows/release.yml</files>
  <behavior>
    - Test: `push.tags` uses GitHub-glob-compatible syntax for stable tag entry.
    - Test: release-notes environment/CLI arguments pass previous tag from `steps.previous-tag.outputs.previous_release_tag`.
    - Test: deterministic release sequence and approval gate semantics remain unchanged.
  </behavior>
  <action>Patch `release.yml` to use valid tag-filter glob syntax and rewire release-notes previous-tag input to the already-resolved previous-tag step output. Preserve existing preflight, traceability gate, and publish approval flow exactly as-is.</action>
  <verify>
    <automated>node --test scripts/release/release-workflow.contract.test.mjs scripts/release/github-release.contract.test.mjs</automated>
  </verify>
  <done>Both workflow defects are fixed and guarded by deterministic contract tests.</done>
</task>

</tasks>

<verification>
- [ ] `node --test scripts/release/release-workflow.contract.test.mjs scripts/release/github-release.contract.test.mjs` passes.
- [ ] Manual verification: `production-release` environment has required reviewer(s) configured in GitHub settings.
- [ ] Manual verification: a tag dry run pauses at publish approval and records explicit approver before external publication steps.
</verification>

<success_criteria>
- Tag-trigger gap is closed with valid GitHub workflow trigger semantics and preserved strict semver enforcement in preflight.
- Previous-tag wiring gap is closed so release notes/changelog scope uses the resolved previous release tag output.
- Human-needed approval-gate proof remains explicit setup/verification work in GitHub environment settings (not automated in-repo).
</success_criteria>

<output>
After completion, create `.planning/phases/05-oss-release-and-traceable-verification/05-04-SUMMARY.md`
</output>
