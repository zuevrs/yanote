# S02: Header-backed correlation and reply truth

**Goal:** Extend the existing declared async semantics into truthful header-backed runtime evaluation so Kafka AsyncAPI `correlationId` and `reply.address` are proven from retained header evidence, malformed declaration shells fail closed, and JSON/HTML/CLI/gate surfaces expose the same redaction-safe runtime truth without changing canonical `kafka <action> <channel>` identities or legacy channel/operation/message coverage numerators.
**Demo:** After this: Run `async-report` on Kafka evidence with retained correlation or reply headers and see additive runtime truth plus typed async gate/CLI failures when required header-backed semantics are missing, unavailable, mismatched, or unsupported.

## Tasks
- [x] **T01: Hardened AsyncAPI extraction to retain reply-channel address metadata and fail closed on malformed header-backed declaration shells.** — Harden the existing AsyncAPI extraction seam for header-backed semantics by keeping declared `correlationId` / `reply` metadata additive, adding optional resolved reply-channel address capture, and converting malformed declaration shells into explicit invalid diagnostics while preserving inline-vs-trait parity and canonical `kafka <action> <channel>` keys.
  - Estimate: 1h15m
  - Files: yanote-js/src/model/operationKey.ts, yanote-js/src/spec/asyncapi.ts, yanote-js/src/spec/asyncapi.test.ts, yanote-js/src/spec/asyncapi.parity.test.ts, yanote-js/test/fixtures/asyncapi/header-runtime-inline-v3.yaml, yanote-js/test/fixtures/asyncapi/header-runtime-trait-v3.yaml, yanote-js/test/fixtures/asyncapi/header-runtime-malformed-v3.yaml, yanote-js/test/fixtures/asyncapi/header-runtime-unsupported-v3.yaml
  - Verify: `npm -C yanote-js test -- src/spec/asyncapi.test.ts src/spec/asyncapi.parity.test.ts`
- [x] **T02: Added header-backed async runtime semantics coverage with truthful correlationId/reply diagnostics.** — Add a dedicated runtime evaluator for declared correlation and reply semantics that supports only `$message.header#/...` against retained `AsyncHeaderEvidence`, reuses `resolveAsyncMessageContract()`, and threads additive satisfied/missing/unavailable/unsupported/mismatched runtime truth into async coverage without changing legacy coverage summaries or leaking retained header values.
  - Estimate: 1h30m
  - Files: yanote-js/src/coverage/asyncSemanticConformance.ts, yanote-js/src/coverage/asyncSemanticConformance.test.ts, yanote-js/src/coverage/asyncCoverage.ts, yanote-js/src/coverage/asyncCoverage.test.ts, yanote-js/src/coverage/asyncCoverage.diagnostics.test.ts, yanote-js/src/coverage/asyncCoverage.parity.test.ts, yanote-js/test/fixtures/async-events/header-runtime-covered.fixture.jsonl, yanote-js/test/fixtures/async-events/header-runtime-failures.fixture.jsonl
  - Verify: `npm -C yanote-js test -- src/coverage/asyncSemanticConformance.test.ts src/coverage/asyncCoverage.test.ts src/coverage/asyncCoverage.diagnostics.test.ts src/coverage/asyncCoverage.parity.test.ts`
- [x] **T03: Added typed async gate failures for header-backed correlationId and reply.address runtime drift** — Promote coverage-layer runtime semantic diagnostics into typed async gate failures so malformed declarations still surface as `ASYNC_SEMANTIC_SPEC_INVALID`, correlation/reply runtime failures sort deterministically ahead of threshold and regression logic, and stderr reasons stay redaction-safe.
  - Estimate: 50m
  - Files: yanote-js/src/gates/asyncEvaluator.ts, yanote-js/src/gates/failureOrder.ts, yanote-js/src/gates/asyncEvaluator.test.ts, yanote-js/src/gates/failureOrder.test.ts
  - Verify: `npm -C yanote-js test -- src/gates/asyncEvaluator.test.ts src/gates/failureOrder.test.ts`
- [x] **T04: Added additive runtimeSemantics reporting to async JSON/HTML artifacts with strict schema validation, deterministic ordering, and redaction-safe runtime diagnostics.** — Make runtime truth durable and machine-readable by adding an additive `runtimeSemantics` section to the canonical async report, keeping it separate from declared truth and coverage math, and rendering the same state in async-only HTML with strict schema validation and deterministic ordering.
  - Estimate: 1h20m
  - Files: yanote-js/src/report/asyncReport.ts, yanote-js/src/report/asyncSchema.ts, yanote-js/src/report/asyncNormalize.ts, yanote-js/src/report/asyncReportHtml.ts, yanote-js/src/report/asyncReport.test.ts, yanote-js/src/report/asyncReport.contract.test.ts, yanote-js/src/report/asyncReport.remote-spec.contract.test.ts, yanote-js/src/report/writeAsyncReport.determinism.test.ts
  - Verify: `npm -C yanote-js test -- src/report/asyncReport.test.ts src/report/asyncReport.contract.test.ts src/report/asyncReport.remote-spec.contract.test.ts src/report/writeAsyncReport.determinism.test.ts`
- [x] **T05: Surfaced header-backed async runtime semantics through async-report stdout, stderr, and machine tokens while preserving the JSON-centered report contract.** — Close the supported user path by wiring runtime semantics and the new typed gate failures through `yanote async-report` stdout, stderr, and `YANOTE_ASYNC_SUMMARY` without breaking the JSON-centered `report=` contract or supported local/remote spec handling.
  - Estimate: 1h
  - Files: yanote-js/src/cli.ts, yanote-js/src/cli.async-report.contract.test.ts, yanote-js/src/cli.async-report.test.ts, yanote-js/src/cli.remote-spec.contract.test.ts
  - Verify: `npm -C yanote-js test -- src/cli.async-report.contract.test.ts src/cli.async-report.test.ts src/cli.remote-spec.contract.test.ts && npm -C yanote-js run build`
