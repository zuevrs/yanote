# M006 Context

Status: complete.

Scope covered:
- repair the public `run-v1-e2e.sh` demo/runtime path
- move delivery-sensitive proof earlier into CI while preserving required job names
- remove private maintainer/runtime trees from the default branch and keep docs truthful
- harden the dedicated push-only `v1-e2e` path with explicit runtime dependency prewarm before cutting `v1.0.125`
