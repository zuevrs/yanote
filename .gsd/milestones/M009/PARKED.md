# Parked

`M009` is intentionally parked in the repo-local `.gsd` state.

Reason:
- recovered future-milestone planning artifacts exist on disk, but the last coherent active project state closed at `M008`
- GSD should not derive an active milestone from these future artifacts until the milestone is explicitly resumed
