# Decisions Register

<!-- Migrated from `.planning` summaries and state. This is the normalized GSD-2 register. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? |
|---|------|-------|----------|--------|-----------|------------|
| 1 | 2026-03-04 | M001/S01 | Canonical operation identity | Use `METHOD + normalized templated route ({param})` as the canonical OpenAPI operation key across runtimes. | Downstream matching, reporting, and governance are only trustworthy if Java and Node resolve the same operation identity. | yes |
| 2 | 2026-03-04 | M001/S01 | Invalid and ambiguous semantic handling | Fail closed on invalid or ambiguous semantic states and never auto-select among multiple fallback candidates. | Silent best-effort behavior would create false coverage and make CI gates untrustworthy. | yes |
| 3 | 2026-03-04 | M001/S01 | Cross-runtime parity enforcement | Protect semantic and matcher behavior with one shared fixture corpus using canonical `METHOD /route` strings. | Shared fixtures prevent Java/Node drift and keep adapters thin. | yes |
| 4 | 2026-03-04 | M001/S02 | Coverage-dimension semantics | Treat status coverage against declared OpenAPI responses, count path parameters with operation observation, and require explicit evidence for query/header coverage. | Coverage must reflect contract behavior instead of raw endpoint-hit counts. | yes |
| 5 | 2026-03-04 | M001/S02 | Report boundary | Enforce a strict schema and centralized deterministic write boundary for report artifacts. | Local and CI consumers need byte-stable, machine-readable outputs. | yes |
| 6 | 2026-03-04 | M001/S02 | CLI output contract | Keep `generatedAt` deterministic and standardize CLI behavior as summary stdout plus typed stderr failures. | CI automation and humans both need predictable output surfaces. | yes |
| 7 | 2026-03-04 | M001/S03 | Governance precedence | Resolve effective policy as `CLI override > policy file > defaults`. | One precedence order avoids contradictory gate behavior between environments. | yes |
| 8 | 2026-03-04 | M001/S03 | Failure ordering | Order failures deterministically as `input > semantic > gate > runtime`, with gate precedence `regression > threshold`. | Stable ordering makes failures auditable and easy for future agents to localize. | yes |
| 9 | 2026-03-04 | M001/S04 | Gradle compatibility surface | Treat `yanoteReport` and `yanoteCheck` task names plus extension fields as locked v1 API. | Java-first adoption depends on a stable plugin surface. | yes |
| 10 | 2026-03-04 | M001/S04 | Adapter strategy | Implement Gradle and CI surfaces as thin wrappers over the existing analyzer contract rather than re-implementing coverage semantics. | One engine is safer than duplicating logic in multiple delivery channels. | yes |
| 11 | 2026-03-04 | M001/S04 | Aggregate execution model | Use root aggregate tasks with deterministic path-sorted module discovery and explicit excludes. | Teams need both per-module debugging and one top-level CI command. | yes |
| 12 | 2026-03-04 | M001/S04 | Required-check contract | Freeze required GitHub checks to `build-and-test` and `yanote-validation`, with `v1-e2e` only on main/release flows. | Branch protection and merge-queue behavior depend on stable job names. | yes |
| 13 | 2026-03-04 | M001/S04 | CI diagnostics surface | Render a concise deterministic top-5 GitHub summary and keep artifact collection/render/upload/enforce steps running under `always()`. | Failure triage must remain available even when validation fails. | yes |
| 14 | 2026-03-04 | M001/S04 | Java runtime enforcement | Enforce Java 21 with an explicit `assert-java21.sh` check after setup. | Runtime intent in workflow config alone is too easy to misread or regress. | yes |
| 15 | 2026-03-04 | M001/S05 | Release hardening order | Lock release behavior with contract tests first, then implement fail-closed preflight and publication wiring. | Release mistakes are expensive; tests must define the safe boundary before automation changes land. | yes |
| 16 | 2026-03-04 | M001/S05 | Publication scope | Allowlist only v1 product modules for Maven Central publication and disable broader publish surfaces. | Prevents accidental exposure of demo/example modules and keeps OSS scope intentional. | yes |
| 17 | 2026-03-04 | M001/S05 | Release gating | Use a tag-only workflow, split preflight from approval-gated publish with `environment: production-release`, and assemble deterministic `{version}-{artifact}` assets with SHA-256 proofs. | Public release is a governed product surface, not an ad hoc CI side effect. | yes |
| 18 | 2026-03-04 | M001/S05 | Requirement traceability gate | Treat `.planning/REQUIREMENTS.md` as the canonical requirement inventory and fail closed below 100% traceability or on flaky/quarantined mappings. | QUAL-01 requires strict accountability before any public release. | yes |
| 19 | 2026-03-04 | M001/S05 | Release workflow gap closure | Use GitHub glob tag filters (`v*.*.*`) and resolved `previous_release_tag` output for release-notes scope. | GitHub workflow syntax and changelog scoping must be deterministic and platform-correct. | yes |
| 20 | 2026-03-05 | M001 | Final release proof | Treat successful run `22712608803` publishing GitHub release `v1.0.122` as the completion proof for the milestone. | The project moved from code-complete to release-verified only after the real publish path succeeded end to end. | no |
| 21 | 2026-03-12 | M002 | Public documentation language | Keep public repository documentation Russian-first while allowing internal planning artifacts to remain English-first. | The target audience is a Russian-speaking engineer audience, so comprehension and adoption matter more than generic global positioning. | yes |
| 22 | 2026-03-12 | M002 | Repository operating posture | Treat Yanote as a maintained product repo for users, not a community-first contribution hub. | The primary goal is trustworthy adoption by engineers integrating the tool, while development remains maintainer-driven. | yes |
| 23 | 2026-03-12 | M002 | Primary onboarding journey | Lead with concept and the verified recorder → events → analyzer path before deep implementation detail. | Trust depends on engineers understanding what Yanote is and how to reach a real result without wading through fragmented setup notes. | yes |
| 24 | 2026-03-12 | M002 | Agent instruction publication boundary | Keep `AGENTS.md` as a local-only untracked maintainer artifact and do not publish it as a tracked repo file. | Agent workflow instructions are useful for private development, but they should not become part of the public product surface. | yes |
| 25 | 2026-03-12 | M002/S01 | Recorder onboarding proof surface | Back the primary Spring recorder guide with an executable dependency-based proof script and external-style fixture that write and inspect `events.jsonl`; keep flatDir out of the primary proof path. | S01 must prove a real adoption path instead of relying on example-only prose or the offline bundle. | yes |
| 26 | 2026-03-12 | M002/S01 | Recorder evidence contract | Treat S01 success as a non-empty `events.jsonl` plus sample-line inspection of method, templated route, status, optional service, and truthful test metadata/export guidance. | File existence alone is too weak; users and future agents need an inspectable evidence contract they can rerun and debug. | yes |
| 27 | 2026-03-12 | M002/S02 | Primary analyzer proof path | Use the repo example service plus RestAssured-generated events with the source-built `yanote-js` CLI as the primary analysis proof path; keep `dist/node-analyzer` as an offline fallback. | This is the shortest truthful route that exercises real tagged events, real report output, and current repo assets without depending on release packaging before S04 formalizes version visibility. | yes |
| 28 | 2026-03-12 | M002/S02 | Example analysis command hygiene | Drop `--exclude /health` from the primary analysis example because the demo spec does not define `/health` and the current RestAssured test deletes readiness traffic before writing the final events file. | Keeping the flag creates an unmatched exclusion warning and obscures the real coverage interpretation surface. | yes |
| 29 | 2026-03-12 | M002/S02 | Canonical analyzer documentation surface | Keep `docs/guides/analyzer-coverage.md` as the single authoritative analyzer guide, with `README.md` acting as navigation and `dist/node-analyzer/README.md` scoped to offline fallback delivery only. | One canonical explanation prevents drift between source-built usage, fallback bundle usage, and the verified proof path. | yes |
| 30 | 2026-03-12 | M002/S02 | Canonical test-tagging documentation surface | Keep `docs/guides/test-tagging.md` as the authoritative RestAssured/Cucumber metadata guide and enforce its vocabulary with `scripts/docs/verify-s02-doc-links.sh`. | Suite/run metadata can drift across demo env bridges, shared properties, HTTP headers, recorded events, and analyzer report fields; one guide plus machine checks keeps those names and handoff boundaries aligned. | yes |
| 31 | 2026-03-12 | M002/S03 | Landing verification boundary | Protect the concept-first landing with `scripts/docs/verify-s03-landing.sh`, enforcing required root sections, direct canonical guide links, directory landing clauses, and example backlinks. | S01/S02 verifiers protect detailed guides, but they do not catch regressions in the root onboarding contract or directory-level navigation. | yes |
| 32 | 2026-03-12 | M002/S03 | Landing reorganization strategy | Improve navigation by adding thin `docs/README.md` and `examples/README.md` landing pages while keeping `README.md` as the primary product landing and the S01/S02 guides as the canonical detailed sources. | S03 needs better first-run navigation without pre-empting the broader docs reorganization owned by S05. | yes |
| 33 | 2026-03-12 | M002/S03 | Root landing copy strategy | Keep the root `README.md` concept-first: explain product value and the recorder → `events.jsonl` → analyzer → `yanote-report.json` loop up front, preserve direct canonical guide links, and demote maintainer/history/module inventory to secondary surfaces. | The repo already has truthful detailed docs; the missing piece was first-touch framing for a new engineer, not more setup detail or stronger version/support claims. | yes |
| 34 | 2026-03-13 | M002/S03 | Directory landing navigation boundary | Treat `docs/README.md` and `examples/README.md` as stable navigation maps: primary user/demo path first, deeper reference second, maintainer/history/fallback surfaces clearly secondary, and leaf example READMEs linked back to the examples landing. | Directory browsing should extend the concept-first product story instead of dropping readers into raw files, fallback bundles, or dead-end example leaves. | yes |
| 35 | 2026-03-13 | M002/S04 | Public release/support owner surface | Use `docs/release-and-support.md` as the single public owner for the current release line, latest stable tag, GitHub Releases pointer, compatibility assumptions, and limitation boundaries, with `README.md` and `docs/README.md` providing only thin pointers. | S04 needs one authoritative user-facing boundary doc without reopening the broader docs architecture or scattering version claims across landings and maintainer files. | yes |
| 36 | 2026-03-13 | M002/S04 | Boundary drift verification | Protect the release/support surface with `scripts/docs/verify-s04-boundaries.sh`, dynamically resolving the latest stable `v*` tag and rejecting wording that treats workspace snapshot or current analyzer `0.0.0` markers as public release truth. | Current release truth lives in tags and GitHub Releases, while local version markers and repository HEAD can drift; a dynamic verifier localizes regressions before boundary docs go stale. | yes |
| 37 | 2026-03-13 | M002/S04 | Boundary contract scope | Treat `docs/release-and-support.md` as a machine-checked owner not only for version wording, but also for published module, plugin, compatibility, limitation, and fallback-boundary clauses; leave README/docs discoverability as the only expected S04 failure until T03. | The slice needs one authoritative user-facing answer that can drift in many small ways; verifying only the version line would miss regressions in the support envelope that matter just as much. | yes |
| 38 | 2026-03-13 | M002/S04 | Landing release/support pointer contract | Keep `README.md` and `docs/README.md` as thin release/support pointers, but require both landings to name the current stable line and delegate the latest tag, GitHub Releases, compatibility, and limitation details to `docs/release-and-support.md`. | Users need version/support visibility from the two main landings without turning those pages into duplicate changelog or policy surfaces; machine-checking the stable-line pointer keeps that balance from drifting. | yes |
| 39 | 2026-03-12 | M002/S05 | Documentation IA hardening strategy | Improve navigation additively with secondary landing pages, owner backlinks, and a dedicated `scripts/docs/verify-s05-navigation.sh` contract; do not re-home tool-coupled reference docs. | S05 needs recoverable browsing and machine-checked structure without breaking S03/S04 contracts or release-coupled paths such as `docs/requirements.md`, `docs/traceability/*`, and `docs/release-and-support.md`. | yes |
| 40 | 2026-03-12 | M002/S05/T01 | Secondary landing ordering | Put the owner-map return path before local leaf links on secondary directory READMEs, and keep `dist/` explicitly below canonical recorder/analyzer guides as an offline fallback only. | Direct entry into maintainer, traceability, plans, or dist should recover back to the stable map before readers commit to a secondary surface; `dist/` in particular must not turn into a competing onboarding path. | yes |
| 41 | 2026-03-12 | M002/S05/T02 | Primary map secondary-surface wiring | Expose maintainer, traceability, plans, and `dist/` through owner-map links on `README.md`, `docs/README.md`, and `examples/README.md`, while keeping the concept-first user flow, thin `docs/release-and-support.md` pointer, and runnable demo path ahead of them. | Secondary discoverability improved in S05, but those branches still must not compete with the canonical recorder/analyzer onboarding story or the single public release/support owner. | yes |
| 42 | 2026-03-12 | M002/S05/T03 | Secondary leaf recovery contract | Require direct-entry secondary leaf docs to open with an audience/owner clause plus a backlink to their owning map; `dist/` leaf docs must also point back to the canonical guide they do not replace. | Search/GitHub deep links should recover readers back to the correct navigation surface instead of trapping them inside maintainer/reference/fallback leaves or turning offline bundles into competing onboarding paths. | yes |
| 43 | 2026-03-13 | M002/S06 | Trust-surface hardening order | Add `scripts/docs/verify-s06-trust-surfaces.sh` first and fix canonical repo/license identity before adding public policy or intake files. | Identity and licensing contradictions undermine every later trust signal; verifier-first hardening localizes drift and keeps S06 additive instead of cosmetic. | yes |
| 44 | 2026-03-13 | M002/S06 | Public GitHub intake scope | Keep GitHub-native trust surfaces narrow: maintainer-owned `CODEOWNERS`, bug-report and integration/docs-guidance issue templates, and policy-routed PR intake; do not add feature-marketplace or community-governance surfaces. | S06 must improve repo trust without implying broad community bandwidth or reopening the maintained-product posture settled for M002. | yes |
| 45 | 2026-03-13 | M002/S06/T01 | S06 verifier progression model | Structure `scripts/docs/verify-s06-trust-surfaces.sh` as incremental `identity`, `policy`, and GitHub intake checks, with full mode composing all three. | T01 needs to lock and verify repo/license identity now while keeping the still-missing later trust surfaces visible instead of forcing an all-or-nothing slice completion check. | yes |
| 46 | 2026-03-13 | M002/S06/T02 | Root policy source-of-truth strategy | Keep `SECURITY.md`, `SUPPORT.md`, and `CONTRIBUTING.md` intentionally thin and machine-check them for concrete public channels plus backlinks to canonical docs instead of duplicating release/support/scope truth. | GitHub visitors need visible trust surfaces, but the maintained-product boundary stays reliable only if those root files route back to `docs/release-and-support.md`, `docs/README.md`, and `docs/requirements.md` rather than drifting into parallel policy owners. | yes |
| 47 | 2026-03-13 | M002/S06/T03 | GitHub intake shaping boundary | Enforce GitHub-native trust through explicit maintainer CODEOWNERS, support/security contact links, two narrow issue templates, and a PR checklist tied to the existing merge-blocking checks. | The repo needs native GitHub guidance, but it should route users into reproducible bug/docs paths and existing policy surfaces instead of implying community-governance or feature-marketplace bandwidth. | yes |
| 48 | 2026-03-12 | M002/S07 | Local AGENTS bootstrap boundary | Keep the real `AGENTS.md` at the repo root as a local-only file and bootstrap its ignore rule through `git rev-parse --git-path info/exclude` with anchored `/AGENTS.md`, not through tracked `.gitignore` or a published root surrogate. | Git documents `info/exclude` as the repo-specific unshared ignore surface, which preserves local tool discoverability while keeping private agent instructions out of tracked repo state. | yes |
| 49 | 2026-03-12 | M002/S07 | S07 proof split | Prove S07 in two layers: a tracked verifier (`scripts/docs/verify-s07-local-agent.sh`) for public boundary/doc wiring and a clone-local Git proof (`git check-ignore -v`, `git status --ignored`, `git ls-files`) for the actual ignored root `AGENTS.md`. | A clean clone or CI job cannot truthfully assert clone-local `info/exclude` state, so the slice must separate public contract verification from local operational proof. | yes |
| 50 | 2026-03-13 | M002/S08 | Final milestone proof strategy | Compose `scripts/docs/verify-s08-entry-paths.sh` from the existing S01-S07 verifiers plus the clone-local `AGENTS.md` Git checks, and keep Docker Compose optional/secondary rather than part of the required gate. | S08 is a final-assembly slice; re-implementing recorder/analyzer/doc assertions or requiring Docker would create a second drift surface and fail in valid environments without a Docker daemon. | yes |
| 51 | 2026-03-13 | M002/S08 | Final proof evidence source | Record S08 acceptance in `S08-UAT.md` and `S08-SUMMARY.md` from live command output and task evidence instead of relying on placeholder slice summaries. | The milestone needs a trustworthy handoff surface grounded in fresh proof, and the recovered S01-S06 placeholders are not authoritative enough for final acceptance. | yes |
| 52 | 2026-03-13 | M003+ | Async expansion sequencing | Accept M003 → M004 → M005 sequentially, while allowing research and prototypes to proceed in parallel only when they do not lock recorder/runtime contracts prematurely. | Recorder and runtime work need a stable async contract target; fully parallel acceptance would create analyzer/recorder drift and rework. | yes |
| 53 | 2026-03-13 | M003 | First async report shape | Ship the first async release with a separate async report and gate path alongside HTTP instead of one mandatory combined report. | Separate surfaces keep semantics legible while async coverage matures and prevent one rollout from obscuring the other. | yes |
| 54 | 2026-03-13 | M003/M004 | First async runtime scope | Keep the first async rollout Kafka-only and Spring Kafka-first. | One deep, trustworthy broker/runtime path is more valuable than shallow broker-agnostic coverage in the first release. | yes |
| 55 | 2026-03-13 | M003/M004 | Async evidence truth model | Distinguish producer and consumer evidence explicitly and surface unmatched or mismatched async evidence as first-class diagnostics; do not silently count best-effort matches. | The user wants honest coverage for services that both publish and consume, and trust depends on fail-closed async diagnostics. | yes |
| 56 | 2026-03-13 | M003/M004/M005 | Async quality bar | Protect the async capability with fixture, unit, integration, end-to-end, and CI proof at the same depth as the existing OpenAPI path. | A second-class async test story would make the new capability untrustworthy even if the happy path works. | no |
| 57 | 2026-03-13 | M003 | AsyncAPI version strategy | Design one internal async identity model that prefers support for both AsyncAPI v2 and v3, but allows v3-first delivery if v2 support would compromise semantic clarity or test depth. | The user prefers both versions, but trust and deterministic semantics matter more than broad version claims. | yes |
| 58 | 2026-03-13 | M003+ | Payload-validation scope boundary | Defer payload validation against AsyncAPI message schemas until after the first trustworthy async contract/evidence path exists, while preserving an architectural seam for it. | Payload validation is valuable, but it should not delay the first release of Kafka/AsyncAPI contract coverage. | yes |
