# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? | Made By |
|---|------|-------|----------|--------|-----------|------------|---------|
| D001 | 2026-03-22 | release-boundary | What defines public release truth for Yanote | Signed release tags and GitHub Releases define public version truth; workspace snapshot markers do not. | `docs/release-and-support.md` explicitly separates release truth from local build markers. | No | agent |
| D002 | 2026-03-22 | report-contract | How HTTP and async reporting surfaces are represented today | Keep HTTP and async reporting on separate public artifact surfaces (`yanote-report.json` and `yanote-async-report.json`) until a future milestone explicitly proves a combined contract. | The current product boundary is intentionally split and documented that way in the public requirements and release-support docs. | Yes | agent |
| D003 | 2026-03-22 | gsd-storage | How `.gsd` should be stored for this repository | Use the `gsd-2` shared-project model: commit durable `.gsd` docs and milestone artifacts, keep runtime/state/worktrees local-only. | This preserves project memory without polluting git with ephemeral session data. | Yes | agent |
| D004 | M010 planning | product-boundary | What the next contract-depth milestone should optimize for | Focus M010 on supported core HTTP/OpenAPI and Kafka/AsyncAPI contract surfaces (undeclared HTTP statuses, supported parameter/header conformance, and public Kafka header diagnostics) while explicitly deferring full-spec constructs and broker expansion. | This captures the highest real-user value without turning the milestone into an unfinishable attempt at full OpenAPI/AsyncAPI compliance. It also preserves the current separate HTTP/async report boundary and keeps the product promise truthful. | Yes | collaborative |
| D005 | M010 S01 planning | architecture | How S01 should represent richer HTTP evidence without breaking existing coverage consumers | Extend `HttpEvent` additively with compatibility `queryKeys`/`headerKeys` plus value-bearing path/query/request-header/response-header evidence that carries explicit capture/redaction/omission state and multi-value-safe arrays. | S02 needs recorder-proven value evidence for undeclared statuses and parameter/header conformance, existing JS/report code still depends on key arrays, and explicit state/reason markers are required to avoid leaking secrets or inferring from missing values. | Yes | agent |
| D006 | M010 S02 planning | architecture | How S02 should surface new HTTP core drift without redefining legacy coverage percentages | Introduce an additive `httpCoreConformance` pipeline that keeps declared coverage metrics unchanged while surfacing undeclared statuses, parameter-value drift, and response-header drift as typed analyzer/report/gate diagnostics. | S02 must satisfy R031-R033 while preserving the already-proven HTTP baseline from R001. A dedicated typed pipeline matches the existing payload and async patterns, keeps report and CLI behavior deterministic, and makes fail-closed semantics explicit instead of hiding new drift inside legacy percentage fields. | Yes | agent |
| D007 | M010 S02 T03 | report-contract | How buildReport should behave before all callers are updated to pass httpCoreConformance | Emit a required but neutral httpCoreConformance section by default, then replace it with full typed analyzer output when httpCoreConformance is provided. | T03 needs the report schema, normalization, and writer contract to land now, but T04 is the task that updates downstream gate/CLI call sites. A neutral fallback keeps yanote-report.json schema-valid and byte-stable immediately without faking diagnostics, while preserving an additive path for real per-operation and per-diagnostic truth once callers pass the aggregated analyzer result. | Yes | agent |
| D008 | M010 S03 planning | proof-strategy | How S03 should promote Kafka header diagnostics into the supported async surface | Require fixture-backed proof for all public Kafka header diagnostics first, then extend the live Spring Kafka proof bundle additively with retained header-drift sidecars while keeping the existing happy path green. | S03 already has most analyzer logic in code, but the public contract is not truthful until missing, invalid, unavailable, and unverifiable headers are reachable from authored AsyncAPI inputs and exported live-proof artifacts. Proving the real-input path first prevents docs-only overclaiming, and additive sidecars preserve the existing green proof denominator while exposing redacted failure context for the new header diagnostics. | Yes | agent |
| D009 | M010 S04 planning | planning | How S04 should close M010 after S02/S03 partial assembly | Treat S04 as the milestone assembly slice that first completes unfinished HTTP core gate/CLI/proof wiring, then widens the retained Kafka header bundle and public async boundary, and only then refreshes release/support docs plus a final assembly verifier. | Research and dependency slice summaries showed that docs-only hardening would overclaim both boundaries: HTTP still lacked live CLI/gate/proof wiring for R031-R033, and the retained Kafka bundle still omitted header sidecars despite stronger code/test coverage. Sequencing S04 as runtime wiring -> retained proof -> public owner verifier keeps the milestone truthful and ties every public claim to rerunnable artifacts. | Yes | agent |
| D010 | M014/S02 planning | architecture | How header-backed async runtime truth is represented and delivered in S02 | Implement S02 via a dedicated `asyncSemanticConformance` evaluator that produces additive header-backed `runtimeSemantics` truth inside `yanote-js`, then thread that truth through async coverage, gate failures, and JSON/HTML/CLI delivery while preserving canonical `kafka <action> <channel>` identity and legacy coverage numerators; defer `scripts/ci/*` and live-proof wiring to S04 unless a blocker appears. | R002/R003/R005 require fail-closed correlation/reply evaluation from retained Kafka headers only, with one consistent redaction-safe story across JSON, HTML, CLI, and stderr. Keeping runtime truth additive and separate from declared semantics and legacy coverage math avoids synthetic green coverage, preserves existing operation identities, and contains scope to the currently supported `yanote-js` delivery path. | Yes | agent |
| D011 | M016 planning | release-automation | What event should define the authoritative release automation path | Use the release tag as the single fail-closed trigger for validation and publication of all intended shipping surfaces. | The user wants one deterministic automation path: cut the release tag and let the system validate and publish everything that is actually part of the product. That reduces manual release drift and keeps release truth explicit. | No | collaborative |
| D012 | M016 planning | repo-boundary | How internal GSD/process/proof material should relate to the public repository surface | Treat public `main` as a product-facing repository surface only; keep GSD, process, proof, and similar maintainer-only surfaces local/private instead of part of the public face. | The user explicitly wants a clean top-tier repository without visible “внутренняя кухня” or repo-level clutter. Internal surfaces may remain useful locally, but they should not dominate the product-facing default branch. | Yes — if a future milestone needs a minimal public maintainer surface again. | collaborative |
| D013 | M016 planning | publication | How analyzer shipping should balance public UX and internal implementation | Publish the analyzer as an official versioned CLI artifact through the release system, while hiding the internal Node build seam from the user-facing install/run story. | The user wants the analyzer published like the other shipped dependencies, but does not want the public path to require source-building `yanote-js` or understanding Node internals. Shipping a versioned official CLI surface preserves implementation freedom without leaking internals into docs. | Yes — if the implementation or packaging model changes later. | collaborative |
| D014 | M016 planning | release-automation | What event should define the authoritative release automation path | Use the release tag as the single fail-closed trigger for validation and publication of all intended shipping surfaces. | The user wants one deterministic automation path: cut the release tag and let the system validate and publish everything that is actually part of the product. That reduces manual release drift and keeps release truth explicit. | No | collaborative |
| D015 | M016 planning | delivery | What the public user-facing entrypoint for the analyzer should be | Make the analyzer’s canonical public entrypoint a standalone CLI surface; keep Gradle as a secondary Java-team path rather than the main README happy path. | A standalone CLI gives the cleanest product story in README and short guides, while Gradle remains valuable for existing Java teams without becoming the only understandable analyzer path. | Yes — if future usage data shows a different dominant entrypoint. | collaborative |
