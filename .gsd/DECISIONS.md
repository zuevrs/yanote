# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? | Made By |
|---|------|-------|----------|--------|-----------|------------|---------|
| D001 | 2026-03-22 | release-boundary | What defines public release truth for Yanote | Signed release tags and GitHub Releases define public version truth; workspace snapshot markers do not. | `docs/release-and-support.md` explicitly separates release truth from local build markers. | No | agent |
| D002 | 2026-03-22 | report-contract | How HTTP and async reporting surfaces are represented today | Keep HTTP and async reporting on separate public artifact surfaces (`yanote-report.json` and `yanote-async-report.json`) until a future milestone explicitly proves a combined contract. | The current product boundary is intentionally split and documented that way in the public requirements and release-support docs. | Yes | agent |
| D003 | 2026-03-22 | gsd-storage | How `.gsd` should be stored for this repository | Use the `gsd-2` shared-project model: commit durable `.gsd` docs and milestone artifacts, keep runtime/state/worktrees local-only. | This preserves project memory without polluting git with ephemeral session data. | Yes | agent |
| D004 | M010 planning | product-boundary | What the next contract-depth milestone should optimize for | Focus M010 on supported core HTTP/OpenAPI and Kafka/AsyncAPI contract surfaces (undeclared HTTP statuses, supported parameter/header conformance, and public Kafka header diagnostics) while explicitly deferring full-spec constructs and broker expansion. | This captures the highest real-user value without turning the milestone into an unfinishable attempt at full OpenAPI/AsyncAPI compliance. It also preserves the current separate HTTP/async report boundary and keeps the product promise truthful. | Yes | collaborative |
| D005 | M011 planning | architecture | HTTP request evidence retention model for M011 | HTTP request evidence will evolve additively with multi-value-safe path/query/header/cookie fields carrying captured/redacted/omitted provenance. Canonical route templates remain unchanged, legacy queryKeys/headerKeys stay derivable for existing coverage numerators, and Yanote metadata headers are excluded from contract semantics. | Cookie and serialization semantics require value-bearing evidence, but the product contract also requires backward compatibility, privacy boundaries, and continuity for existing coverage/report consumers. | Yes | agent |
| D006 | M011 S01 planning | architecture | How S01 exposes first HTTP request semantics without overclaiming broader OpenAPI support | Deliver S01 through an additive `httpRequestConformance` truth surface backed by multi-value-safe request evidence and a separate focused Spring MVC proof route, while keeping legacy `coverage.parameters` numerators and the existing green `run-v1-e2e.sh` boundary unchanged until later slices widen support. | S01 must prove safe request evidence and first scalar truth end to end without redefining existing coverage percentages, leaking sensitive values, or contaminating the stable happy-path proof bundle before S02/S04 formalize the wider supported subset and public contract. | Yes | agent |
| D007 | M011/S01/T01 | contract | Shape of additive HTTP request evidence in the JVM JSONL contract | Represent each retained path/query/request-header/cookie key as a tri-state `{ state, values[], reason }` object; preserve repeated values in order; lowercase only request-header keys; keep the canonical templated `route` unchanged. | This reuses the existing captured/redacted/omitted pattern already established elsewhere in Yanote, gives downstream analyzers honest provenance without guessing, preserves multi-value request facts for later scalar/serialization work, and avoids leaking secrets while keeping old HTTP events readable. | Yes | agent |
| D008 | M011/S01/T02 | architecture | How to represent first-scalar HTTP request-parameter semantics without redefining legacy parameter coverage | Keep legacy `parameters` for existing coverage math and add an additive `requestParameters` contract for path/query/header/cookie scalar semantics and cookie support | This preserves the validated `coverage.parameters` numerator/denominator and backward-compatible report/gate assumptions while giving the request-conformance analyzer an explicit place to carry cookie parameters, style metadata, and supported-vs-unsupported scalar schema details. | Yes | agent |
| D009 | M011 S01 T03 | observability | How request-conformance truth is exposed on public report and CLI surfaces | Publish raw retained request truth counts and sanitized issue text on CLI stdout/YANOTE_SUMMARY, while keeping retained observedValues only in yanote-report.json diagnostics. | CLI output is a broad human-facing surface and must not leak raw parameter/header/cookie values through diagnostic reason strings, but the retained JSON report artifact still needs enough detail for focused investigation of captured-valid/captured-invalid/unsupported request truth. | Yes | agent |
| D010 | M011 S01 closeout | verification | How focused live HTTP proof bundles should wait for service readiness | Use TCP-port readiness checks instead of recorded HTTP readiness routes for focused proof/verifier flows unless the readiness endpoint is declared in the proof spec. | The recorder retains every live HTTP request, so HTTP readiness probes can inject undeclared traffic into focused proof bundles and falsely turn request-conformance reports partial. TCP readiness keeps recorder artifacts limited to declared proof traffic while preserving honest failure localization. | Yes | agent |
| D011 | M011 S02 planning | architecture | Supported request serialization subset for M011 S02 | S02 will publish a shape-aware request support contract with `scalar` / `array` / `unsupported` semantics, support repeated arrays only for `query=form` + `explode=true` with scalar items, and keep path/header/cookie arrays plus `content`, non-whitelisted styles, and delimiter-reconstructed forms explicitly unsupported and fail-closed. | S01 truthfully retains repeated query values, but path arrays and header/cookie delimiter forms are ambiguous from retained evidence. A shape-aware public contract plus explicit unsupported reasons lets Yanote widen request semantics honestly, preserve legacy coverage numerators, and fail closed instead of implying blanket OpenAPI serialization support. | Yes | agent |
| D012 | M011/S02/T03 | governance | How request-conformance drift is represented in fail-closed governance | Map request drift to dedicated semantic codes (`SEMANTIC_HTTP_INVALID_REQUEST_PARAMETER`, `SEMANTIC_HTTP_UNAVAILABLE_REQUEST_PARAMETER`, `SEMANTIC_HTTP_UNSUPPORTED_REQUEST_PARAMETER`) and rank those request semantic failures ahead of HTTP payload and gate threshold/regression failures. | Request parameter truth now needs to stop CI through the same semantic-failure path as payload drift without leaking retained values. Dedicated codes keep governance/report/CLI surfaces typed, while explicit precedence makes mixed request+payload failures deterministic for downstream summary and gate consumers. | Yes | agent |
| D013 | M011 S02 closeout | architecture | How the M011 S02 request-serialization contract should migrate without breaking existing evaluator behavior | Publish shape-aware request support via declaredSupport/declaredSupportShape/declaredSupportReason additively, while keeping the legacy scalarSupport evaluator contract in place until request-conformance and governance enforcement fully switch over. | S02 needs to widen the public request contract to include honest repeated query-array support and explicit unsupported reasons without redefining validated coverage numerators or breaking in-flight request-conformance/report/CLI behavior during the slice. The additive migration keeps backward-compatible report/evaluator surfaces while the later S02 tasks move enforcement onto the new support matrix. | Yes | agent |
| D014 | M011 S03 planning | architecture | Supported HTTP payload format policy and media-type selection for M011 S03 | Support only an explicit allowlisted HTTP payload format subset starting with `email`; surface any other declared/custom payload format as fail-closed unsupported-schema-format semantics; and choose the most specific matching declared media type at evaluation time while keeping normalized declared-media presentation order stable. | Ajv can validate many formats only when plugins are registered, but Yanote must publish a product boundary instead of inheriting whatever validator behavior happens to be available. An explicit allowlist avoids overclaiming support, fail-closed unsupported-format semantics make custom or unprovable declarations visible, and matching-time specificity fixes wildcard shadowing without destabilizing report ordering or legacy deterministic outputs. | Yes | agent |
| D015 | M011/S03/T01 | architecture | How unsupported HTTP payload schema formats are represented in the analyzer | Register only allowlisted payload formats (starting with email) in Ajv and surface any other declared payload format as a dedicated UNSUPPORTED_SCHEMA_FORMAT skipped diagnostic. | This keeps Yanote’s published payload-format boundary explicit, prevents Ajv from silently degrading unknown/custom formats to plain type checks, and preserves legacy payload coverage numerators while downstream governance fails closed on the dedicated diagnostic. | Yes | agent |
| D016 | M011 S03 T02 | pattern | How HTTP payload media specificity should be resolved when multiple declared media types match the observed content type | Keep declared media extraction and report ordering lexicographically stable, but choose the matched contract at evaluation time by specificity (`exact` > structured-suffix wildcard > broader wildcard). | Report and contract surfaces already rely on stable sorted declared-media lists, while payload conformance truth must not let broader declarations like `application/*+json` shadow more specific siblings such as `application/problem+json`. Separating extraction ordering from matching ranking preserves backward-stable output while making analyzer truth honest. | Yes | human |
| D017 | M011 S03 T03 | observability | How report governance diagnostics are ordered for HTTP payload semantics | Sort report governance diagnostics with the same failure precedence model used by gate selection instead of lexicographic code order. | This keeps yanote-report.json aligned with fail-closed primary selection, ensures SEMANTIC_HTTP_UNSUPPORTED_SCHEMA_FORMAT stays ahead of SEMANTIC_HTTP_UNSUPPORTED_SCHEMA, and prevents report/gate/CLI surfaces from disagreeing about the dominant payload semantic failure. | Yes | agent |
| D018 | M011 S04 planning | verification | How S04 closes the public HTTP semantics contract on retained CI/proof surfaces | Extend `scripts/ci/run-v1-e2e.sh` additively with a request-semantics sidecar derived from the existing live `events.jsonl`, and keep the full S03 format/media red matrix on the focused `verify-m011-s03-format-media.sh` proof path referenced by docs. | The compose-based public proof already records the focused request-evidence route, so an additive sidecar can widen the standard CI entrypoint without a second live runtime harness. Keeping the broader S03 fixture matrix on the focused verifier avoids bloating the stable public bundle while still giving docs and operators an authoritative deep proof path. | Yes | agent |
| D019 | M012 roadmap planning | architecture | Which broader OpenAPI objects M012 will support first | M012 will implement OpenAPI security schemes and effective security requirements as the only new first-class broader-object family, while examples, links, callbacks, webhooks, and unsupported security subtypes stay explicitly deferred. | Security requirements attach to existing HTTP operations, reuse the current retained request-evidence substrate, and can be given a truthful fail-closed story. The other broader OpenAPI objects either drift toward spec-lint-only behavior or require a new runtime identity/correlation model that would overexpand the milestone. | Yes | agent |
| D020 | M012 roadmap planning | reporting | How broader OpenAPI security support will appear in reporting and gating | Security truth in M012 will ship as a dedicated additive report/conformance surface routed through governance diagnostics, CLI summary/stderr, and CI summary rendering, while existing coverage.operations/status/parameters/aggregate numerators remain unchanged. | M011 established that widening semantics safely requires additive surfaces rather than mutating legacy coverage numerators. Reusing the same pattern preserves determinism, keeps the current HTTP core contract stable, and satisfies the requirement that new support remain visible through report, CLI, and CI rather than only internal code or tests. | Yes | agent |
| D021 | M012 S01 T04 | observability | How CLI and CI human-readable security summary blocks are rendered | Emit additive security machine tokens on every HTTP report, but render the human-facing HTTP security summary block only when the report contains non-zero security observations/declared operations. | This preserves legacy zero-security HTTP summary contracts and avoids noisy empty sections, while still making security truth machine-readable and surfacing a dedicated human block whenever security semantics are actually in play. | Yes | agent |
| D022 | M012 S01 closeout | governance | How HTTP security semantics fail closed through governance and human-facing diagnostics | Map additive HTTP security conformance drift to dedicated semantic codes derived from structured security diagnostics, and rank those failures ahead of request, payload, and threshold/regression gate failures. | Security drift must stop CI truthfully without leaking retained secret values or depending on free-form diagnostic strings. Dedicated codes plus structured reason/hint generation keep report, CLI, and CI summaries deterministic and make security semantics the primary failure when present. | Yes | agent |
| D023 | M012 S02 planning | public-proof | How M012 S02 will retain and publish the new HTTP security proof | Publish HTTP security proof as a fixture-backed `security-semantics.*` sidecar and focused verifier, record fixture provenance in bundle notes, and do not copy the raw security fixture JSONL into the public `.yanote-ci/v1-e2e/` bundle. | The supported security matrix is already proven by deterministic S01 fixtures rather than the live Spring MVC demo. Reusing those fixtures keeps the public proof truthful, avoids widening the example-service surface just for documentation, and prevents fake secret-like fixture values from being redistributed inside the retained public bundle while still leaving a rerunnable provenance trail. | Yes | agent |
| D024 | M012 S02 T01 | observability | How the M012 public v1 bundle should publish security proof evidence | Retain fixture-backed security stdout/stderr/report sidecars plus manifest/source-path provenance, but do not copy the raw http-security-api-key.fixture.jsonl artifact into .yanote-ci/v1-e2e/. | This keeps the public proof rerunnable and explicit about provenance while preserving the truthful boundary: the live Spring MVC demo is not expanded to fake full security coverage, and retained public artifacts do not leak fake captured security values or redistribute the raw fixture corpus. | Yes | agent |
| D025 | M013 roadmap planning | architecture | Analyzer delivery boundary for remote sources, deprecated operations, and human-facing reports in M013 | Support explicit local file/local directory/remote single-document URL inputs with sanitized provenance and no credential-bearing URL support; keep deprecated operations inside legacy coverage numerators by default while surfacing them additively; ship separate static offline HTML artifacts for HTTP and async reports rather than a combined dashboard. | This retires the highest-risk remote loading problem without broadening into repository integrations, preserves continuity of existing coverage contracts, satisfies report UX goals without violating R005 or R030, and keeps R004 release/support truth explicit across CLI, Gradle, and CI delivery surfaces. | Yes | agent |
| D026 | M013 S01 planning | architecture | Supported remote spec URL subset and persisted provenance policy for M013 S01 | Accept only single-document public `http(s)` spec URLs without userinfo, query strings, or fragments; materialize them for analysis and persist only sanitized source-kind provenance on retained command/report surfaces. | This keeps the new remote-input path deterministic, aligns with the existing no-credential-bearing-URL boundary from D025, gives CLI and Gradle runs a narrow subset that can be proven on localhost, and prevents uploaded artifacts or command-argument files from leaking token-like URL parts. | Yes | agent |
| D027 | M013 S01 T02 | observability | How retained report and summary surfaces publish spec-source provenance | Use a shared top-level `specSource { kind, reference }` contract sourced directly from `ResolvedSpecSource.provenance`, and expose the same values in CLI machine summaries as `spec_source_kind` and `spec_source_ref`. | This keeps local-file, local-directory, and remote-url provenance truthful across HTTP reports, async reports, and CLI summaries without duplicating formatting logic, while relying on the resolver’s existing remote URL sanitization so credential-bearing fragments never reach persisted artifacts. | Yes | agent |
| D028 | M013 S01 T03 | observability | How Gradle command-argument sidecars should represent remote spec inputs | Persist Gradle command-argument files with a `<remote-url>` display placeholder plus explicit `spec_source_kind` / `spec_source_ref` lines, while the real analyzer invocation still receives the sanitized remote URL. | This keeps retained `yanote-check-command.args` and `yanote-report-command.args` useful for debugging source kind and sanitized provenance without preserving replayable raw remote `--spec` values on the human-facing sidecar surface. | Yes | agent |
| D029 | M013 S01 closeout | architecture | How CLI spec inputs are resolved and cleaned up across local and remote runs | Resolve analyzer spec inputs once into a shared `ResolvedSpecSource` object and clean any remote temp materialization in the CLI `finally` block so local-file, local-directory, and remote-url runs share one entrypoint contract. | This keeps source classification, provenance, and cleanup logic centralized across `report` and `async-report`, prevents divergent local vs remote behavior, and ensures temporary remote copies are removed even on analyzer failures. | Yes | agent |
| D030 | M013 S02 planning | architecture | How deprecated OpenAPI operation truth is represented in M013 S02 | Publish deprecated-operation truth additively on the HTTP path as `summary.deprecatedOperations` counts plus `coverage.perOperation[].deprecated`, while keeping existing coverage dimensions, gate math, report status, async surfaces, and dashboard boundaries unchanged. | This makes deprecation explicit product truth for JSON and CLI consumers without silently shrinking legacy denominators, avoids inventing a new policy dimension before product chooses one, preserves R005/R030 by keeping async and dashboard surfaces untouched, and lets the real supported analyzer path prove denominator stability with one canonical metadata flow. | Yes | agent |
| D031 | M013/S02/T03 | observability | How deprecated HTTP CLI truth is surfaced on machine and human summary outputs | Publish additive deprecated summary truth only on `yanote report` via a dedicated human summary line, `YANOTE_SUMMARY` `deprecated_*` tokens, and explicit uncovered-deprecated Top Issues wording; leave legacy `operations`/`covered` tokens and all async summary surfaces unchanged. | Operators need explicit deprecated-operation visibility without denominator drift or async-surface leakage, and downstream proof/CI work needs stable token names that do not overload existing machine fields. | Yes | agent |
| D032 | M013 S02 closeout | public-proof | How the M013 S02 deprecated-operations proof bundle is retained | Retain the deprecated-operations proof as a fixed HTTP-only bundle with a manifest that records the rerun command plus the preserved legacy denominator and deprecated-operation counts. | The slice’s main regression risk is silent numerator drift or accidental async/dashboard leakage. Pinning the file layout and restating the key claims in artifact-manifest.txt makes future drift obvious from one retained surface without re-reading verifier internals. | Yes | agent |
| D033 | M013 S03 planning | architecture | Human-facing static report delivery contract for M013 S03 | Emit self-contained sibling HTML files `yanote-report.html` and `yanote-async-report.html` directly from the normalized canonical report DTOs, keep CLI `Report Path` and machine `report=` tokens JSON-centered, and let successful Gradle report execution pass through analyzer-created HTML artifacts without inventing stub/skip HTML placeholders. | This satisfies R003/R004/R005/R030 by making the human artifact available on real supported analyzer paths while preserving separate HTTP vs async retained surfaces, explicit sanitized provenance truth, offline/self-contained viewing, and the current JSON-centered automation contract. It also avoids esbuild asset-loader churn and secret-risky template imports by keeping the HTML renderer inline, static, and derived only from canonical report data. | Yes | agent |
| D034 | M013/S03/T01 | reporting | How static HTML report artifacts should be generated from HTTP canonical report truth | Render sibling HTML directly from the normalized, schema-valid report DTO and whitelist section content; escape all dynamic text and exclude retained request observedValues/raw dumps from human-facing tables. | This keeps JSON and HTML derived from the same canonical truth, preserves deterministic output, supports offline self-contained artifacts, and prevents credential or evidence leakage through human-readable surfaces. | Yes | agent |
| D035 | M013/S03/T04 | public-proof | How static HTML proof artifacts are retained for future slice and milestone verification | Retain the S03 proof bundle as separate `http-report/` and `async-report/` subtrees containing stdout/stderr/exit-code sidecars plus sibling JSON/HTML artifacts and an `artifact-manifest.txt` that restates the rerun commands and high-signal contract claims. | This gives future agents one rerunnable bundle that proves separate offline HTML delivery, sanitized provenance, and no secret/external-asset/combined-report drift without rereading verifier internals or recomputing the whole feature story from tests alone. | Yes | agent |
| D036 | M013/S03/T02 | reporting | How async static HTML artifacts stay aligned with the shared shell without inheriting HTTP-only semantics | Reuse the shared inline HTML document shell for async reports, but constrain the rendered sections to async-native overview, provenance, channel/operation/message coverage, and diagnostics while keeping `writeAsyncYanoteReport()` JSON-centered. | This keeps the HTTP and async human artifacts visually consistent and offline-safe while preserving the product boundary that async reporting is a separate surface with no HTTP payload/request/security or combined-dashboard wording. | Yes | agent |
| D037 | M013 S04 planning | delivery | How M013 S04 closes the delivery/support boundary without widening CI topology | Use the existing build-and-test and yanote-validation jobs, artifact collectors/exporters, GitHub summaries, and public docs/verifiers as the only S04 closeout surfaces; widen them to publish separate JSON+HTML artifacts, sanitized spec provenance, and additive deprecated truth without adding new required jobs or any combined report/dashboard surface. | Branch protection and workflow contract already treat the current job IDs as a stable interface, while earlier slices established the canonical JSON/HTML/provenance truth inside the existing artifact seams. Reusing those seams keeps CI, docs, and support wording aligned with the proven delivery path, preserves the local-first/remote-opt-in boundary, and avoids inventing a second delivery contract that would violate the separate HTTP/async and no-dashboard constraints. | Yes | agent |
| D038 | M013/S04 closeout | delivery-observability | How CI-retained bundles and GitHub summaries publish widened M013 delivery truth | Derive spec-source provenance and deprecated-operation facts from canonical report JSON, publish explicit separate HTTP/async JSON+HTML artifact names in manifests and step summaries, and fail closed when required HTML siblings are missing from success bundles. | This keeps CI, retained artifacts, and GitHub summaries aligned to the same canonical analyzer truth, prevents stale hand-maintained metadata, preserves secret-safe sanitized provenance, and makes missing human-facing report artifacts visible immediately instead of silently degrading the supported delivery surface. | Yes | agent |
| D039 | M013/S04 closeout | support-boundary | How public docs and support surfaces describe analyzer delivery after M013 | Keep public support wording local-first: local file/directory `--spec` is the stable baseline, remote single-document `http(s)` `--spec` is a narrow opt-in path with sanitized provenance, HTTP and async report families stay separate JSON+HTML surfaces, and dashboard/combined-report claims remain out of scope. | The slice only remains truthful if docs, release/support guidance, CI verifiers, and retained bundles all describe the same supported boundary. Explicitly pinning the local-vs-remote and separate-surface constraints prevents future docs drift from overpromising broader remote loading, combined reporting, or hosted UI support. | Yes | agent |
| D040 | M014 planning | product-boundary | How M014 treats Kafka bindings that outrun current runtime evidence | Treat Kafka bindings surgically: allow only truthful additive topic-level support or visibility now, keep groupId, clientId, key, schema-registry metadata, partitions, replicas, and topic configuration as explicit declared-only or deferred surfaces, and never count them as covered semantics without runtime evidence. | Most Kafka binding fields need recorder facts the current evidence model does not retain. Publishing an explicit support matrix prevents silent overclaiming, preserves R005's Kafka-first honesty, and avoids synthetic green coverage for declaration-only metadata. | Yes | agent |
| D041 | M014 planning | architecture | How M014 widens AsyncAPI semantics without breaking existing async contracts | Ship richer async semantics additively on the existing Kafka report path: preserve canonical `kafka <action> <channel>` identity and current channel/operation/message numerators, require trait-applied parity for supported fields, and expose new truth through additive report/CLI/CI surfaces instead of redefining legacy coverage math. | The current async path has a closed parser/model/report/gate contract and prior milestones already established additive widening as the safe pattern. Preserving operation identity and legacy numerators prevents proof-bundle drift while still giving users real richer-semantic visibility on the supported async surface. | Yes | agent |
| D042 | M014 planning | product-boundary | How M014 treats Kafka bindings that outrun current runtime evidence | Treat Kafka bindings surgically: allow only truthful additive topic-level support or visibility now, keep groupId, clientId, key, schema-registry metadata, partitions, replicas, and topic configuration as explicit declared-only or deferred surfaces, and never count them as covered semantics without runtime evidence. | Most Kafka binding fields need recorder facts the current evidence model does not retain. Publishing an explicit support matrix prevents silent overclaiming, preserves R005's Kafka-first honesty, and avoids synthetic green coverage for declaration-only metadata. | Yes | agent |
| D043 | M014 planning | product-boundary | Which richer AsyncAPI constructs M014 supports first | Prioritize header-backed `correlationId` and `reply.address` semantics on the retained Kafka header path, support only the runtime-expression subset resolvable from retained headers, and fail closed on unsupported locations or unavailable evidence rather than inferring truth. | Current Spring Kafka proof artifacts already retain headers and message-selection metadata, making header-address semantics the highest-value truthful next step. The recorder does not yet provide a general correlation graph or broad runtime-expression inputs, so narrower fail-closed support keeps the milestone honest and usable. | Yes | agent |
| D044 | M014/S01/T02 | reporting | How async declared semantics are published in the canonical report | Build the async report's additive declaredSemantics section from canonical Kafka operation contracts and expose only sanitized declaration locations plus counts. | AsyncCoverageResult preserves coverage math and diagnostics but does not retain declared correlationId/reply metadata. Sourcing declaredSemantics from operationContractsByKey keeps existing channel/operation/message numerators unchanged, preserves canonical kafka <action> <channel> identities, and avoids leaking raw parser trait blobs or retained header values. | Yes | agent |
| D045 | M014/S01/T03 | observability | How async-report CLI should expose declared AsyncAPI semantics | Render sanitized per-operation declared-semantics detail lines in stdout, but keep YANOTE_ASYNC_SUMMARY to additive counts and keep report= pointed at yanote-async-report.json. | Human operators need visible declared correlationId/reply locations, while machine consumers must keep a stable JSON-centered contract without HTML paths, raw retained values, or verbose per-operation blobs in the machine summary token. | Yes | agent |
| D046 | M014/S01 closeout | async-contract | How trait-applied AsyncAPI declaration metadata is normalized for Kafka async-report semantics | Trust the AsyncAPI parser's merged trait output as the canonical source for supported declaration fields, retain message `correlationId` on `KafkaMessageContract` and operation `reply` on `KafkaOperationContract`, and keep `serializeOperationKey()` fixed to `kafka <action> <channel>`. | Trait parity is only truthful if Yanote consumes the parser-resolved AsyncAPI model instead of inventing its own merge layer. Storing supported declaration metadata beside the canonical Kafka identity lets JSON/HTML/CLI report surfaces publish additive declared semantics without changing routing keys or legacy async coverage numerators. | Yes | agent |
| D047 | M014/S02 planning | architecture | How S02 should model and publish header-backed correlation/reply runtime truth | Evaluate header-backed correlation and `reply.address` semantics in a dedicated `yanote-js/src/coverage/asyncSemanticConformance.ts` layer that reuses canonical async message selection, then publish the results additively as a separate `runtimeSemantics` surface in async reports and CLI output rather than folding them into declared semantics or legacy coverage math. | The existing schema/routing coverage path already proves canonical Kafka operation identity and message selection, but it does not retain positive runtime truth for correlation/reply semantics. A dedicated evaluator keeps the supported subset narrow (`$message.header#/...` only), reuses existing runtime selection logic, preserves channel/operation/message coverage numerators, and lets JSON/HTML/CLI distinguish declared truth from retained-header runtime truth without leaking header values. | Yes | agent |
| D048 | M014/S02/T02 | architecture | Where additive header-backed async runtime diagnostics live before gate/report/CLI surfacing | Expose header-backed correlationId and reply.address truth under AsyncCoverageResult.runtimeSemantics while leaving the existing top-level coverage diagnostics limited to routing and schema conformance. | T02 needs truthful runtime observability immediately without perturbing legacy coverage numerators, diagnostic counts, or existing gate/report contracts ahead of T03-T05. Nesting the new diagnostics under runtimeSemantics keeps the surface public and testable now, while giving downstream tasks a stable seam to promote into gates and report artifacts intentionally. | Yes | agent |
| D049 | M014/S02/T03 | observability | How async header-backed runtime semantic gate failures are surfaced and prioritized | Map correlationId and reply.address runtime diagnostics to dedicated ASYNC_SEMANTIC_* failure codes plus an ASYNC_SEMANTIC_RUNTIME_FAIL_CLOSED fallback, ranked immediately after ASYNC_SEMANTIC_SPEC_INVALID and before generic async drift/gate failures. | Typed codes make stderr and machine primary= tokens stable without text matching, preserve redaction-safe runtime truth, and ensure malformed runtime diagnostics cannot disappear or be masked by threshold/regression logic. | Yes | agent |
| D050 | M014/S02/T04 | report-contract | How async runtime semantic truth is exposed in async report artifacts | Publish additive runtime truth under report.runtimeSemantics with summary, per-operation rows, and typed diagnostic counts/items while leaving legacy coverage, declaredSemantics, and canonical kafka operation keys unchanged. | T04 needed machine-readable runtime truth in JSON and HTML, but mutating legacy coverage or declaredSemantics would break byte-stable numerators and blur declared-vs-runtime meaning. Grouping runtime proof by canonical operation row keeps the surface deterministic for HTML/CLI consumers, preserves redaction-safe diagnostics, and lets status/report validation account for runtime semantic drift explicitly. | Yes | agent |
| D051 | M014 S02 T05 | observability | How `yanote async-report` exposes header-backed runtime semantics on human and machine surfaces | Render sanitized per-operation runtime detail rows from `report.runtimeSemantics`, keep `YANOTE_ASYNC_SUMMARY` additive and counts-only, and preserve the JSON-centered `report=` contract. | S02 T05 needed runtime proof visible on stdout/stderr without leaking retained header values or breaking machine consumers that already rely on `yanote-async-report.json` as the canonical artifact. | Yes | agent |
| D052 | M014 S02 T01 | async-contract | How reply-channel address metadata and malformed header-backed declaration shells are handled during AsyncAPI extraction | Keep canonical `kafka <action> <channel>` keys unchanged, retain resolved reply-channel addresses additively under `declaredReply.channel.address`, and fail closed on malformed `correlationId` / `reply.address.location` shells. | S02 T01 showed richer AsyncAPI semantics must stay additive to the proven Kafka identity model; resolved reply-channel metadata is useful for downstream runtime proof, but malformed header-backed declarations must surface as explicit invalid diagnostics instead of being silently dropped. | Yes | agent |
| D053 | M014/S03 planning | product-boundary | How S03 surfaces Kafka binding fields without destabilizing canonical async identity | Treat `channel.bindings.kafka.topic` as additive binding-support metadata only, classify `groupId` / `clientId` / `key` / schema-registry metadata as explicit declared-only fields, classify `partitions` / `replicas` / `topicConfiguration` as deferred, and never let any binding field rewrite `kafka <action> <channel>` identities or legacy async coverage numerators. | S03 needs a truthful support matrix, not a second routing contract. Keeping topic visibility additive and the rest explicitly declared-only or deferred preserves R025 honesty, prevents false green coverage, and avoids accidental operation-key drift on specs that already have stable channel addresses. | Yes | agent |
| D054 | M014/S03/T01 | architecture | Canonical source for Kafka binding support classification | Store flat `bindingSupport` rows on `KafkaOperationContract` and derive report `bindingSupport` JSON directly from those contracts. | This keeps Kafka binding truth in one additive canonical surface, preserves stable `kafka <action> <channel>` identities, and prevents report/CLI layers from reparsing AsyncAPI bindings independently. | Yes | agent |
| D055 | M014/S03/T02 | reporting | How human-facing async surfaces should present Kafka binding support | Render a dedicated Kafka Binding Support section in async HTML and CLI output instead of folding binding truth into coverage or declared/runtime semantics. | Binding support is additive product truth, not legacy coverage math or runtime-semantic proof. A separate section keeps supported, declared-only, deferred, and invalid rows visible without implying synthetic coverage or conflating bindings with other async semantics. | Yes | agent |
| D056 | M014/S03/T02 | reporting | How human-facing async surfaces should present Kafka binding support | Render a dedicated Kafka Binding Support section in async HTML and CLI output instead of folding binding truth into coverage or declared/runtime semantics. | Binding support is additive product truth, not legacy coverage math or runtime-semantic proof. A separate section keeps supported, declared-only, deferred, and invalid rows visible without implying synthetic coverage or conflating bindings with other async semantics. | Yes | agent |
| D057 | M014/S04 planning | architecture | How S04 should prove richer AsyncAPI semantics on the authoritative live Spring Kafka path | Extend the existing live Spring Kafka proof bundle with non-sensitive retained headers and Kafka binding declarations so the happy-path bundle itself becomes the authoritative source for additive binding support plus declared/runtime semantics, then derive exporter, CI summary, and docs surfaces from that bundle while keeping machine output counts-only and JSON-centered. | S02 and S03 already proved runtime semantics and Kafka binding support on authored fixtures and canonical report contracts, but M014 is not closed until those truths survive the real Spring Kafka delivery path. Reusing the authoritative live proof bundle avoids fixture-only closeout, keeps canonical Kafka identities and legacy coverage numerators stable, and preserves the repo's redaction-safe Kafka-only boundary on CI and docs surfaces. | Yes | agent |
| D058 | M014 S04 T02 | delivery-observability | How collected build-and-test async metadata and summaries should be derived after the live Kafka bundle widened | Derive collected async bundle metadata from the authoritative live bundle manifest and require the async summary renderer to fail closed when required report fields or retained companion artifacts are missing. | The live Kafka bundle is the authoritative proof surface for widened binding, declared, and runtime semantics. Copying its manifest/source-path breadcrumbs into collected artifacts keeps CI summaries and retained bundles aligned, while fail-closed summary validation prevents stale or malformed async inputs from being rendered as implied success. | Yes | agent |
| D059 |  | requirement | R025 | validated | M014 S04 reran the authoritative live Kafka proof (`bash scripts/ci/verify-m004-s03-live-kafka-proof.sh`), the async artifact/summary workflow contract suite (`node --test scripts/ci/export-async-proof-artifacts.test.mjs scripts/ci/collect-yanote-artifacts.test.mjs scripts/ci/render-yanote-summary.test.mjs scripts/ci/yanote-ci-workflow.contract.test.mjs`), and the async boundary doc verifiers (`bash scripts/docs/verify-m005-s01-async-path.sh && bash scripts/docs/verify-m005-s01-async-boundaries.sh && bash scripts/docs/verify-s04-boundaries.sh`), proving the richer Kafka binding, declared semantics, and header-backed runtime semantics survive the real Spring Kafka bundle, collected CI surfaces, and public Kafka-first docs. | Yes | agent |
| D060 |  | async-proof-observability | How the authoritative live Kafka proof chooses retained correlation and reply headers | Emit stable non-sensitive `correlation_id` and `reply_to` values derived from the proof message/topic on the existing live send/receive flow. | This makes header-backed runtime semantics provable end to end on the real Spring Kafka path without introducing secret-bearing or flaky retained values, while preserving canonical `kafka <action> <channel>` identities and redaction-safe artifacts. | Yes | agent |
| D061 |  | support-boundary | How public docs and branch-protection wording describe the widened live async proof surface | Anchor public wording to the redaction-safe CI summary families (`binding support`, `declared semantics`, `runtime semantics`) and mirror the same literals in drift verifiers. | This keeps README/docs/branch protection, collected CI summaries, and shell boundary verifiers aligned to the actual live Kafka bundle without implying broader broker-agnostic or combined-report promises. | Yes | agent |
| D062 | M015 planning | milestone-ordering | What M015 should prove before shipping the combined surface | Order the milestone as protocol-aware async generalization first, live RabbitMQ runtime proof second, combined report/gate third, and CI/docs boundary closure last. | The deepest risk is the Kafka-only assumption embedded across parser, event, and coverage seams. Combined reporting is only trustworthy after the widened async path works through a real runtime, and public docs/CI should close only after both technical contracts are real. | Yes | agent |
| D063 | M015 planning | product-boundary | What non-Kafka async scope M015 will target first | Deliver one concrete RabbitMQ/AMQP async path first instead of attempting a fully broker-agnostic async abstraction. | The repository already pins RabbitMQ/AMQP as the current unsupported boundary, while parser, event, coverage, and report seams are deeply Kafka-specific. A single real broker keeps the milestone demoable and provable without forcing speculative abstractions across every future async runtime. | Yes | agent |
| D064 | M015 planning | milestone-ordering | What M015 should prove before shipping the combined surface | Order the milestone as protocol-aware async generalization first, live RabbitMQ runtime proof second, combined report/gate third, and CI/docs boundary closure last. | The deepest risk is the Kafka-only assumption embedded across parser, event, and coverage seams. Combined reporting is only trustworthy after the widened async path works through a real runtime, and public docs/CI should close only after both technical contracts are real. | Yes | agent |
| D065 | M015 planning | report-contract | How the combined HTTP+async surface will preserve split truth | Implement the combined report/gate as an aggregation of canonical HTTP and async child reports with explicit child attribution; do not invent one blended coverage denominator. | Current operator trust depends on split HTTP and async truth surfaces. Aggregating canonical subreports preserves drill-down, lets overall status be derived deterministically, and avoids hiding whether failures came from HTTP or async semantics. | Yes | agent |
| D066 | M015/S01 planning | async-report-contract | How Kafka-only additive async sections behave on AMQP inputs | Ship RabbitMQ/AMQP routing, message, and payload truth first, while keeping Kafka binding and other Kafka-only additive semantics explicitly protocol-scoped instead of emulating parity on AMQP. | The current async report already contains Kafka-specific additive sections. Pretending RabbitMQ already supports the same extras would weaken operator trust, so the widened boundary should expose only what the AMQP path can prove truthfully. | Yes | agent |
| D067 | M015/S01 planning | async-identity | How widened async analyzer identities and JSONL event kinds will name the first non-Kafka path | Use protocol-scoped async identities and event kinds (`kafka`, `amqp`) while treating RabbitMQ as the first supported AMQP runtime. | AsyncAPI already exposes `protocol`, existing Kafka keys stay unchanged, and protocol-scoped identities let the analyzer admit AMQP fixtures now without locking the contract to a RabbitMQ-only abstraction. | Yes | agent |
| D068 | M015/S01/T01 | architecture | Supported AsyncAPI protocols for protocol-aware normalization in M015/S01/T01 | Normalize only single-protocol AsyncAPI documents with protocol exactly `kafka` or `amqp`, serialize operation keys as `<protocol> <action> <channel>`, and fail closed on mixed servers or unsupported aliases like `rabbitmq`/`mqtt`. | This preserves stable Kafka keys for existing artifacts, admits the first RabbitMQ path through standard AMQP AsyncAPI metadata, and keeps unsupported or ambiguous protocol declarations explicit instead of silently mapping them onto Kafka semantics. | Yes | agent |
| D069 | M015/S01/T02 | async-event-contract | How the first non-Kafka async evidence path is represented across Node and JVM JSONL contracts | Model AMQP evidence as a first-class sibling async kind/subtype (`kind: "amqp"`) that mirrors the Kafka async JSONL metadata shape instead of overloading Kafka-specific event classes. | Protocol-scoped sibling contracts preserve existing Kafka JSONL round-trip behavior and exact `kind: "kafka"` compatibility while letting readers, tests, and future analyzer stages distinguish RabbitMQ/AMQP evidence truthfully without inventing a broker-agnostic blob or faking Kafka semantics on non-Kafka input. | Yes | agent |
| D070 | M015/S01/T03 | architecture | How async coverage and conformance match protocol-scoped evidence after AMQP support | Route async coverage/schema matching by `<protocol, action, channel>` and keep runtime semantic proof Kafka-only until another protocol has explicit retained-semantic rules. | AMQP specs and events must not cross-match Kafka operations that share action/channel names, but RabbitMQ support in this slice only extends truthful routing/schema coverage, not Kafka-specific reply/correlation semantics. | Yes | agent |
| D071 | M015/S01/T04 | reporting | How async report artifacts should expose protocol attribution and Kafka-only additive metadata | Publish a top-level `protocols` array in the async report contract and keep `bindingSupport` explicitly Kafka-scoped, yielding zero/empty Kafka binding data for AMQP reports instead of fabricating parity. | Operation keys already encode protocol, but a dedicated protocol list makes JSON and HTML artifacts self-describing, and explicit Kafka-only empty states prevent RabbitMQ/AMQP reports from implying unsupported binding semantics. | Yes | agent |
| D072 | M015/S02 planning | architecture | How S02 should implement the first live non-Kafka runtime proof without blurring the AMQP-vs-RabbitMQ boundary | Build a reusable `yanote-recorder-spring-amqp` integration on Spring AMQP abstractions, but keep the end-to-end runtime proof and retained artifact bundle explicitly RabbitMQ-named (`RabbitMq...` tests and `live-rabbitmq-proof`). | The reusable Java recorder seam is Spring AMQP rather than a RabbitMQ-only client API, while the first supported live broker in M015 remains RabbitMQ. Separating abstraction naming from runtime-proof naming keeps the code reusable for future AMQP work without overstating broker-agnostic support, and it aligns with S01's protocol-scoped `amqp` evidence contract plus RabbitMQ as the first proven runtime. | Yes | agent |
| D073 | M015/S02/T01 | architecture | How the new Spring AMQP recorder should hook producer and listener seams without clobbering user customization | Implement Spring AMQP recorder hooks by combining additive RabbitTemplate before-publish processors with a BeanPostProcessor-created proxy for send outcome recording, and by proxying listener container factories so created containers receive appended after-receive processors and listener advice. | Spring AMQP 3.1.1 exposes additive RabbitTemplate post-processors publicly but keeps listener advice access protected and factory customizers single-slot. This combination preserves existing RabbitTemplate processors and listener factory customizers while still giving Yanote truthful send/receive recording and fail-visible seam attachment. | Yes | agent |
| D074 | M015 S02 closeout | delivery-contract | How live RabbitMQ async proof bundles represent Kafka-only companion artifacts | Allow AMQP happy-path bundles to omit Kafka-only companions while recording each absent file explicitly as `none` in the manifest and source-path notes. | M015 S02's exporter tests and live RabbitMQ proof passed only when the AMQP success bundle retained the real producer/consumer JSONL, merge logs, stdout/stderr, and async JSON/HTML artifacts while marking `single-service-proof.log`, `runtime-selected-*`, and `schema-failure-*` companions as intentional absences instead of failing closed or fabricating Kafka-only outputs. | Yes | agent |
| D075 | M015/S03 planning | report-contract | How the combined HTTP+async surface should serialize child truth and derive overall status in S03 planning | Represent the combined surface as an attributed wrapper over canonical HTTP and async child report summaries plus child artifact paths, and derive the overall combined status from those child status/diagnostic extracts instead of duplicating full child report bodies or inventing blended coverage math. | The canonical child reports remain the source of truth and already carry different additive details: HTTP has governance/request/security/payload sections while async has protocol/binding/declared/runtime-semantic sections. Copying full child DTOs would bloat the artifact and invite drift, while a summary-and-path wrapper preserves drill-down, keeps attribution explicit, and still lets the combined surface compute one deterministic overall status without fabricating a new denominator. | Yes | agent |
| D076 | M015 S03 T01 | reporting | Combined HTTP+async report contract shape | Model the combined artifact as a top-level overview plus explicit children.http and children.async sections with child-specific provenance, drill-down artifact refs, summaries, and issue lists; do not synthesize a blended cross-surface denominator. | The slice requires one combined surface without flattening HTTP and async truth. Child-scoped sections preserve canonical attribution, keep AMQP-only async facts visible, allow fail-closed validation per child, and give downstream CLI/proof work stable paths for human and machine summaries. | Yes | agent |
| D077 | M015 S03 | requirement | R020 | validated | S03 verification passed the combined report/unit contract tests, the combined CLI contract tests, the verifier contract test, and the dist proof (`npm -C yanote-js exec vitest run ./src/report/combinedReport.test.ts ./src/report/combinedReport.contract.test.ts ./src/report/writeCombinedReport.determinism.test.ts`, `npm -C yanote-js exec vitest run ./src/cli.combined-report.test.ts ./src/cli.combined-report.contract.test.ts`, `node --test ./scripts/ci/verify-m015-s03-combined-report.contract.test.mjs`, `npm -C yanote-js run build && bash ./scripts/ci/verify-m015-s03-combined-report.sh`), proving Yanote can compose canonical HTTP and async child reports into an attributed combined JSON/HTML + CLI surface without inventing a blended denominator. | Yes | agent |
| D078 | M015 S03 | proof-contract | How the M015 S03 dist proof composes the combined report surface | Generate the HTTP child from a dedicated green fixture and combine it with the retained S02 RabbitMQ async report, retaining stdout, stderr, exit codes, manifest, and source-path notes under .tmp/m015-s03-combined-proof. | The successful proof stack (`npm -C yanote-js run build && bash ./scripts/ci/verify-m015-s03-combined-report.sh`) only stays diagnosable when the combined artifact points back to canonical subreports and the retained bundle preserves enough context to localize HTTP-child, retained-async, or combined-aggregation drift. | Yes | agent |
| D079 | M015 S03 | requirement | R020 | validated | S03 verification passed the combined report/unit contract tests, the combined CLI contract tests, the verifier contract test, and the dist proof (`npm -C yanote-js exec vitest run ./src/report/combinedReport.test.ts ./src/report/combinedReport.contract.test.ts ./src/report/writeCombinedReport.determinism.test.ts`, `npm -C yanote-js exec vitest run ./src/cli.combined-report.test.ts ./src/cli.combined-report.contract.test.ts`, `node --test ./scripts/ci/verify-m015-s03-combined-report.contract.test.mjs`, `npm -C yanote-js run build && bash ./scripts/ci/verify-m015-s03-combined-report.sh`), proving Yanote can compose canonical HTTP and async child reports into an attributed combined JSON/HTML + CLI surface without inventing a blended denominator. | Yes | agent |
| D080 | M015 S04 | requirement | R021 | validated | M015 S04 reran and passed the collected-artifact summary tests, widened workflow contract test, and focused public-doc verifier, proving the first RabbitMQ/AMQP path is now carried through CI artifacts, GitHub summaries, docs, and support surfaces rather than remaining a Kafka-only boundary. | Yes | agent |
| D081 | M015 S04 | ci-contract | How the widened M015 proof stack fits the required GitHub check topology | Keep build-and-test as the stable required job and widen it in place with Kafka, RabbitMQ, and combined proof steps plus collected summary publication. | The workflow contract test and branch-protection docs passed only when the existing required job name stayed unchanged and all three proof families plus exit-code enforcement were wired into it. | Yes | agent |
| D082 | M015 S04 | support-boundary | How public docs and support intake describe the widened async and combined surfaces | Document combined-report as a child-attributed additive proof family and keep support intake surface-specific by requesting Kafka, RabbitMQ, or combined bundles according to the failing path. | The focused S04 doc verifier passed only when docs avoided blended-denominator/dashboard wording and told operators which retained bundle(s) to attach per failing surface. | Yes | agent |
| D083 | M015 S04 | requirement | R021 | validated | M015 S04 reran and passed the collected-artifact summary tests, widened workflow contract test, and focused public-doc verifier, proving the first RabbitMQ/AMQP path is now carried through CI artifacts, GitHub summaries, docs, and support surfaces rather than remaining a Kafka-only boundary. | Yes | agent |
